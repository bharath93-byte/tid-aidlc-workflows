[
  {
    "size": 33,
    "representative": "I don't think we need this to be a GraphQL error but I can change it just in case",
    "top_paths": [
      "graphql/apollo-server/src/blu-lambda.ts",
      "graphql/apollo-server/test/resolvers.test.ts",
      "setup/infra/sam/configuration/dev/us/us-west-2/groups-env.json",
      "applications/src/appsync_handler.py",
      "tests/automation/steps/groups_graphql.py"
    ],
    "comments": [
      "@mariasekartrimble As the graphql calls the service layer directly instead of app layer, we couldn't move to app layer",
      "Hmm this is concerning that we are using completely different data model inputs for the same API endpoint.\r\n\r\nAdditionally if this is actually the case it will not be possible to extend it to graphQL",
      "The graphql-core dependency should specify a version constraint instead of using '*' to ensure reproducible builds and avoid potential breaking changes.\n```suggestion\ngraphql-core = \"^3.2.3\"\n```",
      "i dont think this will be needed in graphql as this is a post call sweatha",
      "The function it is introduced in and used in is not exposed in graphql Sweatha",
      "can be done, i'll be making a bit more changes for GraphQL along this Makefile, i'll make the changes there and resovle this comment for noe",
      "`GroupsDataSource` initializes several DataLoader instances and exposes methods (`getAccountGroups`, `getUserGroups`, `getDeviceGroups`, `getGroupChildren`) that aren\u2019t used anywhere in the apollo-server code and don\u2019t correspond to any `groups`/`children` fields in `graphql/schema.graphql` (only `Query.group`, `Query.groups`, and `Application.groups` exist). Consider removing the unused loaders/methods for now (or add the missing schema fields/resolvers in the same PR) to avoid carrying dead code and extra test surface area.",
      "This module starts with a blank line and lacks the module-level docstring header used by the other `common.graphql_util` modules. Adding a short top-level docstring (and removing the leading blank line) keeps the package consistent and improves maintainability when browsing generated docs and stack traces.",
      "Are these not a part of the REST API? The GraphQL READ operations should be analogous to that as they are also a wrapper around the service layer.",
      "I just captured the fields defined in the existing graphql schema",
      "This needs to be a follow up for the service layer as graphql is only a wrapper around that",
      "This resolver is `aggregate=True` and must return a list matching the input size. With `@graphql_error_handler`, exceptions will return a single `{errors: [...]}` dict instead, which will break batch callers. Prefer returning a per-item error list (or handling exceptions inside the loop and returning `Error`/`{errors:...}` for the affected items only).",
      "`graphql_error_handler` always returns a single `{errors: [...]}` dict on exceptions. When this decorator is applied to AppSync batch resolvers (aggregate=True), the handler will return a dict instead of the required list of per-item results, which will break callers expecting an array (e.g., the Apollo DataLoader dispatch uses `result.map(...)`). Consider either (a) making the decorator batch-aware (return a list of error items matching the input length) or (b) not using this decorator on aggregate batch resolvers and instead formatting errors per batch item.",
      "`get_account_external_systems` returns a list for a batch invocation, but `@graphql_error_handler` will return a single `{errors: [...]}` dict on exceptions. That breaks the expected batch response shape (and any consumer that does `result.map(...)`). Consider handling exceptions per batch item (returning an error entry for each requested account) or avoiding this decorator on list-returning batch resolvers.\n```suggestion\n\n```",
      "The BLU error shape from `format_error` uses `errors[0].extensions`, but this code reads `err.extension` and spreads it into the GraphQLError options. `GraphQLError` expects extensions under `options.extensions`, so the current code will drop the error metadata (code/type/title) and may not behave as intended. Update to read `err.extensions` and pass it via `new GraphQLError(err.message, { extensions: err.extensions })` (and validate `errors` is an array before accessing `.length`).\n```suggestion\n\t\tif (\n\t\t\tObject.hasOwn(result, \"errors\") &&\n\t\t\tArray.isArray(result.errors) &&\n\t\t\tresult.errors.length > 0\n\t\t) {\n\t\t\tconst err = result.errors[0];\n\t\t\tthrow new GraphQLError(\n\t\t\t\terr.message,\n\t\t\t\t{ extensions: err.extensions },\n```",
      "The double type assertion `as ErrorExtensions as GraphQLErrorExtensions` is unnecessary and can hide type mismatches. Prefer typing the `extensions` object directly as `GraphQLErrorExtensions` (or remove the custom `ErrorExtensions` type if it's only used for this cast).",
      "PR description mentions a new BLU `graphql_error_handler` decorator that returns an `errors` list for Apollo to introspect, but the Apollo server-side detection implemented here only treats `{status,type,title,detail}` as an error. If BLUs are (or will be) returning `{ errors: [...] }`, `handleBluResponse` will currently return that object as successful data instead of throwing a `GraphQLError`. Please align the implementation and/or PR description on the exact error payload shape.",
      "Do we still need to have a GraphQLError here for a general server error?",
      "I don't think we need this to be a GraphQL error but I can change it just in case",
      "On line 28 of `blu-lambda.test.ts` there is a test that verifies that the GraphQLError is thrown. It doesn't introspect the error however. I can add a test that does that if necessary.",
      "I remove the duplicated function in groups_graphql.py file and instead call query_group_users_graphql from users_graphql.py in the groups tests",
      "The code no longer raises AttributeError when the GraphQL server returns {\"data\": null, \"errors\": [...]}. Eager evaluation of has_errors and groups_is_none remains safe because both expressions now handle null data correctly.",
      "Changes made:\r\n\r\n1. Removed query_invalid_group_graphql from steps/groups_graphql.py.\r\n2. Updated test_groups_graphql_resolvers.py:\r\n       a) Dropped query_invalid_group_graphql from imports.\r\n       b) Replaced its use in test_query_group_invalid_id with query_group_graphql.\r\n\r\nFor invalid IDs, the server returns either {\"data\": {\"group\": null}} or {\"data\": null, \"errors\": [...]}. The test only checks for errors or a null group, so the extra metadata field in query_group_graphql does not affect the assertions.",
      "`ApplicationsInGroupsResolver` is asserting the resolver returns an array, but `Group.applications` is a connection type (`GroupApplicationConnection`) in the schema. Please update the mock and expectation to return the correct `{ edges, pageInfo }` shape so this test matches the schema contract.",
      "There is an empty test case (`test(\"ApplicationInGroupEdge\", async () => {});`) which doesn\u2019t assert anything and can mask missing coverage. Please either implement assertions for the `ApplicationInGroupEdge.node` resolver behavior or remove this test.\n```suggestion\n\n```",
      "This test\u2019s mocked BLU response shape (`edges: [{ node: { relation: { applicationId }}}]`) doesn\u2019t match the groups BLU implementation in this PR, which returns edges with `applicationId/status/roleIds` at the edge level (and relies on a separate `ApplicationInGroupEdge.node` resolver). Please update the mock/expectations to the actual connection shape so the test validates the real contract.\n```suggestion\n\t\t\t\t\tedges: [\n\t\t\t\t\t\t{\n\t\t\t\t\t\t\tapplicationId: \"app-1\",\n\t\t\t\t\t\t\tstatus: \"ACTIVE\",\n\t\t\t\t\t\t\troleIds: [\"role-1\"],\n\t\t\t\t\t\t},\n\t\t\t\t\t],\n```",
      "This test constructs `parent` as `{ node: { relation: { applicationId }}}`, but the applications BLU batch resolver added in this PR expects the source to contain `applicationId` at the top level (i.e., `source.applicationId`). As written, this test will pass an incompatible payload. Please adjust the parent shape to match the edge object produced by the groups resolver and remove the `console.log`, which adds noise to test output.\n```suggestion\n\t\t\tapplicationId: \"app-123\",\n\t\t};\n\t\tconst result = await dataSource.getApplicationInGroupEdge(\n\t\t\tparent,\n\t\t\t{} as any,\n\t\t);\n```",
      "`args.first`/`args.after` may be `undefined` when the caller omits these optional GraphQL arguments. Consider normalizing to `null` (e.g., `args.first ?? null`, `args.after ?? null`) so the data source consistently receives `number | null` / `string | null` and the downstream payload doesn\u2019t silently drop fields due to `undefined`.",
      "`include` is optional, but this resolver unconditionally calls `plain_validate(include)` and then `include.get(...)`. If the GraphQL client omits `include` (so it\u2019s `None`), this will raise (invalid include / AttributeError) and break `Group.users` for the default case. Guard the include handling with `if include is not None:` (and only validate/build `include_query` then), or update the validator to accept `None` and skip validation when absent.\n```suggestion\n    include_query = None\n    if include is not None:\n        include_query_builder.plain_validate(include)\n        fields: list = include.get(\"fields\") or []\n        include_query = \",\".join(fields) if fields else None\n```",
      "`handle_group_users` always requests only `firstName,lastName,email` from `get_user_ref_from_group`, but the `UserToGroupWithRef` model also supports `userName` and GraphQL clients can query `userName` on `Group.users.edges.node`. Consider adding `userName` to the default include list (or deriving includes from the GraphQL selection set) so requested fields don\u2019t come back null due to under-fetching.\n```suggestion\n    include_fields: list = [\"firstName\", \"lastName\", \"email\", \"userName\"]\n```",
      "`Group.users` resolver passes `args.include` into `getGroupUsers(...)`, but the GraphQL schema for `Group.users` doesn\u2019t define an `include` argument and `GroupsDataSource.getGroupUsers` only accepts `(groupId, filter, first, after, identity)`. This will fail type-checking/compilation and sends an unexpected payload shape.",
      "`GroupsDataSource.getGroupUsers` is defined without an `include` parameter, but this test calls it with an extra `null` argument and asserts an invocation payload containing `include`. Update the test (and payload expectations) to match the actual method signature and GraphQL schema (filter/first/after only).",
      "This test assumes `Group.users` accepts an `include` argument and that `getGroupUsers` is called with an extra `include` parameter. The schema change removed `include` from `Group.users`, so this test no longer matches the resolver signature / generated types and will fail once types are regenerated or strict typing is applied."
    ]
  },
  {
    "size": 31,
    "representative": "I see the env variable in included in the lambda. just use it.",
    "top_paths": [
      "users/template.yaml",
      "accounts/template.yaml",
      "tam-authz/src/template.yaml",
      "graphql/template.yaml",
      "common/template.yaml"
    ],
    "comments": [
      "What is the diff between `ENV` and `environment` if both are same means plz remove lambda level config",
      "ENV can be removed. I will remove it",
      "@Sweatha1609 please include the env as well so that we can prompt the changes.",
      "Can we refer the same in SAM template?",
      "Please remove the regions from all the env files.",
      "@prakashsahdsb Can we move these to Environment variables so that it can be added into .env file. The command line is too big to handle",
      "dont hard code the env name at the end , plz change it in other places too. like based on the Environment variable in the SAM template instead .",
      "i think it is outdatd. some of the common env variables are moved to global. need rebase",
      "this should be taken from env variable",
      "Can we move this to global so that it wont be repeated for the every lambda. also move the common items to global .",
      "common env variables has been moved to globals. you may need to rebase the main branch",
      "Can we move this to global? so that based on the requirement of the individual lambdas will set higher or lower apart from default.",
      "With this setup we can pass the custom values in the config files if individual lambdas require a value apart from the default.",
      "Adding environment name as well part of the SAM template would help not only here but also for other purposes. So please derive it via parameter.",
      "@Sweatha1609 I have updated the env files for all environments",
      "As this lambda does not have common layer, this cant be done.",
      "I see the env variable in included in the lambda. just use it.",
      "If more than one lambda is going to have this means better move it to the \"Global\" function section to avoid repeated env variables",
      "we can add missing env variables here",
      "Good to know. Okay, let's do that then. We should define and document the use of these lambdas, their purpose, and prevent rewriting the same code.",
      "While loading the lambda itself we can load these templates one time loading instead of every time. So better remove these methods and have only constants of those loaded templates.",
      "Most of the env variables are available at \"Global\" section itself. so plz revalidate and remove those repeated variables unless its been overridden by this lambda alone.",
      "i think, this needs to be changed. default template is must be configured properly. we should raise error during lambda loading if default template is missing ?",
      "I'm not sure I understand where env gets set",
      "A separate lambda is setup",
      "I have added the environment variable in all the accounts env.json",
      "I guess these things should come from config. Not via template. Plz use the config_layer and load your required configs in the lambda using config_layer.py",
      "@Ragupathi-veludurai remove \"LambdaMemory\" this from variable , should be same for all env .",
      "Why not as a constant instead of referring the global env_vars, just initialize it at the top itself and refer it.",
      "@Ragupathi-veludurai this change is for not lower env",
      "@Ragupathi-veludurai this is not for lower env , undo this change ."
    ]
  },
  {
    "size": 29,
    "representative": "The refactoring from fixtures to test methods creates implicit test execution order dependencies. Tests like `test_verify_dealer_user_can_initiate_opt_in_request` and subsequent tests rely on `self.__class__.dealer_account_info` and `self.__class__.customer_account_info` being set by earlier tests (`test_create_dealer_account` and `test_create_customer_account`). Without explicit dependency markers (e.g., `@pytest.mark.dependency`), these tests may fail if run in isolation or in a different order. Consider adding dependency markers or ensuring tests can run independently.",
    "top_paths": [
      "tests/automation/tests/devices/test_device_dealer_customer_transfer.py",
      "users/tests/test_users_app.py",
      "tests/automation/tests/organizations/test_organizations_account_relations.py",
      "tests/automation/tests/accounts/dealer_optin/test_dealer_optin.py",
      "roles/tests/test_service.py"
    ],
    "comments": [
      "Could you please try out the pytest.parameterize annotation for easy of usage with the reusage in mind.",
      "Mocker is a builtin fixture in pytest-mock",
      "Please rename test files with the prefix \"test\" otherwise pytest won't be able to run it.",
      "In general we should avoid the use of global variables. Also could these tests have docstrings describing what they're testing?",
      "These assertions are inside the `with pytest.raises(ParseException):` block, which means they will never execute if the exception is raised as expected. Move these assertions outside the `with pytest.raises` block to ensure they run after the exception is caught.\n```suggestion\n        mock_get_searchable_fields_for_relation.assert_called_once_with(context_ref, resource_ref, relation_name)\n        mock_get_searchable_fields_for_entity.assert_called_once_with(resource_ref)\n```",
      "The fixture scope changed from 'session' to 'class' which may cause the dealer account to be created multiple times if this fixture is used across multiple test classes, potentially impacting test performance and isolation.\n```suggestion\n@pytest.fixture(scope=\"session\")\n```",
      "Using global variables in test methods can lead to test dependencies and make tests harder to maintain. Consider passing this data through fixtures or test parameters instead.",
      "Make it as \"pytest.mark.parameterize\" for reduced lines handling explicit test case handling.",
      "The dependency name 'create_organization' is reused here, but it was already defined on line 39. This will cause a conflict. This test should depend on the existing 'create_organization' dependency rather than defining a new one with the same name.\n```suggestion\n    @pytest.mark.dependency()\n```",
      "The dependency 'create_account_as_types' does not exist. No test in this file is marked with this dependency name. This should likely depend on the individual account creation tests: 'create_account_as_customer', 'create_account_as_reseller', 'create_account_as_distributor', or 'create_account_as_dealer'.\n```suggestion\n    @pytest.mark.dependency(depends=[\"create_organization\", \"create_account_as_customer\", \"create_account_as_reseller\", \"create_account_as_distributor\", \"create_account_as_dealer\"])\n```",
      "Duplicate dependency name \"test_add_existing_user_to_account\" in the dependency list. Remove the duplicate entry.\n```suggestion\n    @pytest.mark.dependency(depends=[\"test_add_existing_user_to_account\"])\n```",
      "The test function parameter order doesn't match the decorator and parametrize order. Pytest applies decorators bottom-up and parametrize top-down, so the order should be: parametrized args first (mock_external_rv, mock_account_rv, payload, expected_id, expected_name), then patch mocks (mock_account_get_entity, mock_external_get_entity - reversed due to bottom-up decorator application), then fixtures (account_to_external_system_parser).\n```suggestion\n    mock_external_rv,\n    mock_account_rv,\n    payload,\n    expected_id,\n    expected_name,\n    mock_account_get_entity,\n    mock_external_get_entity,\n```",
      "Using global variables in test methods is problematic and can cause test isolation issues. Consider storing group_id as an instance variable or returning it from a fixture instead.",
      "Using global variables in test methods is an anti-pattern that creates hidden dependencies between tests and can lead to unpredictable behavior if tests run in different orders. Consider using pytest fixtures or returning values from helper methods to share data between tests.\n```suggestion\n\n```",
      "Using global variables is an anti-pattern in test code. These variables should be declared as instance variables (e.g., self.accountId, self.token, etc.) or local variables within the test method. Global variables can cause test pollution and make tests harder to maintain and debug.\n```suggestion\n\n```",
      "Using global variables is an anti-pattern in test code. The token3 variable should be declared as an instance variable (self.token3) or local variable within the test method to avoid test pollution and improve maintainability.\n```suggestion\n\n```",
      "The fixture `account_relation_handler_fixture` is defined at the end of the file after the test functions that use it. In pytest, fixtures should be defined before the tests that use them, or in a conftest.py file. Move this fixture definition to the top of the file, before the first test that uses it (around line 45).",
      "The test catches a generic `Exception` instead of the specific `ApplicationError` that should be raised when an account is already linked to an organization. This makes the test less precise and could mask unexpected failures. Use `pytest.raises(ApplicationError)` and verify the error code matches `ErrorCode.ACCOUNT_ALREADY_LINKED_TO_ORGANIZATION`.",
      "Using `global` tokens inside tests can introduce order-dependence and makes parallel execution harder to reason about. Prefer local variables (or fixtures) and pass them through function scope to keep tests isolated.\n```suggestion\n\n```",
      "The refactoring from fixtures to test methods creates implicit test execution order dependencies. Tests like `test_verify_dealer_user_can_initiate_opt_in_request` and subsequent tests rely on `self.__class__.dealer_account_info` and `self.__class__.customer_account_info` being set by earlier tests (`test_create_dealer_account` and `test_create_customer_account`). Without explicit dependency markers (e.g., `@pytest.mark.dependency`), these tests may fail if run in isolation or in a different order. Consider adding dependency markers or ensuring tests can run independently.",
      "`test_create_another_dealer_account` relies on `self.__class__.dealer_account_owner` being set by `test_create_dealer_account`, but it is not marked with a pytest dependency. Since this suite already uses `pytest.mark.dependency` elsewhere, add an explicit dependency (or otherwise ensure ordering) to avoid intermittent failures when tests are reordered/parallelized.",
      "The assertions inside this `with pytest.raises(...)` block will never run because the exception exits the context manager immediately. Move the `assert_not_called()` checks outside the `with` block (after the exception assertion) so the test actually verifies no downstream methods were invoked.",
      "Same issue here: the `assert_not_called()` lines are inside the `with pytest.raises(...)` block and will never execute. Move them after the `with` so the test verifies the side effects you intend.",
      "There are several instances in your tests where you define a basic group again instead of using this fixture you created it.",
      "`pytest` is imported but never used in this test module. Since Ruff enables `F401` for tests, this will fail linting; remove the unused import (or use it if you intended to add exception assertions).",
      "Several tests in this class depend on side effects from earlier \"Prerequisite\" test methods (subscribe/authorize) and later \"Cleanup\" test methods. This makes the suite brittle when running individual tests, reruns-on-failure, or when ordering changes (even with ordering plugins), and can leave state behind if a prerequisite fails. Prefer doing setup/teardown via fixtures (e.g., a fixture that subscribes/authorizes and yields, then cleans up) or explicitly declare dependencies with `pytest-dependency` markers if ordering is required.",
      "The `get_trn_prefix(resource_type=\"user\")` / `resource_type=\"application\"` calls are inside f-strings delimited with double quotes, resulting in nested double quotes and a syntax error. This will prevent pytest from collecting this test module. Use single quotes for the string literals (e.g., `resource_type='user'`) or otherwise adjust quoting.",
      "The test name/docstring says `AddressNotFoundException` is raised when the account is missing, but the test actually expects reconciliation via `insert_from_modify` and no exception. Update the name/docstring (or add `pytest.raises(...)` if raising is the intended behavior).\n```suggestion\ndef test_update_account_not_found_calls_insert_from_modify() -> None:\n    \"\"\"\n    Test that when the account is not found, the update handler calls `insert_from_modify`\n    instead of raising AddressNotFoundException.\n```",
      "The test name/docstring says `AddressNotFoundException` is raised when the account is missing, but the assertions only verify reconciliation is called and no exception is expected. Update the name/docstring (or add `pytest.raises(...)` if raising is the intended behavior) to keep the test intent clear.\n```suggestion\ndef test_update_account_not_found_triggers_reconciliation() -> None:\n    \"\"\"\n    Test that when the account is not found, reconciliation is triggered via `insert_from_modify`.\n```"
    ]
  },
  {
    "size": 29,
    "representative": "Remove the unnecessary empty line. This extra whitespace serves no purpose and should be eliminated for cleaner code formatting.\n```suggestion\n\n```",
    "top_paths": [
      "common/src/db/iam_database.py",
      "users/src/appsync_handler.py",
      "accounts/cdh-migration/export_scripts/export/address_reference_dangling_pointers_export.py",
      ".github/workflows/release_common_sam_apply.yml",
      "roles/src/appsync_handler.py"
    ],
    "comments": [
      "[nitpick] Extra blank line between the closing brace and return statement should be removed for consistency with the rest of the codebase.\n```suggestion\n\n```",
      "Extra blank line should be removed to maintain consistent code formatting.\n```suggestion\n\n```",
      "Extra blank line should be removed to maintain consistent formatting in the schema definition.\n```suggestion\n\n```",
      "[nitpick] Remove extra blank line between lines 141 and 142 to maintain consistent spacing.\n```suggestion\n\n```",
      "[nitpick] Remove trailing whitespace on line 176.\n```suggestion\n\n```",
      "Remove the unnecessary empty line. This extra whitespace serves no purpose and should be eliminated for cleaner code formatting.\n```suggestion\n\n```",
      "Remove extra blank line at line 1198. There should be only one blank line between the variable assignment and the comment.\n```suggestion\n\n```",
      "[nitpick] There's an empty line (109) that adds unnecessary whitespace. Consider removing it for cleaner code formatting.\n```suggestion\n\n```",
      "[nitpick] Extra blank line before the next method creates inconsistent spacing. Remove this line to maintain consistent formatting.\n```suggestion\n\n```",
      "Remove trailing whitespace after the return statement.\n```suggestion\n\n```",
      "The echo command is incomplete and will output nothing useful. This appears to be a debugging leftover that should be removed.\n```suggestion\n\n```",
      "Trailing whitespace found on line 2393. Remove the extra spaces.\n```suggestion\n\n```",
      "Incomplete echo statement. This will output nothing useful and should either be removed or completed with the intended content.\n```suggestion\n\n```",
      "Multiple consecutive blank lines. Remove extra blank lines to maintain consistent formatting (lines 137-138).\n```suggestion\n\n```",
      "Unnecessary blank line before closing brace. Remove line 21 to maintain consistency with other configuration files.\n```suggestion\n\n```",
      "[nitpick] Extra blank line should be removed for consistency with the rest of the code formatting.\n```suggestion\n\n```",
      "Trailing whitespace on line 31.\n```suggestion\n    \"opensearchHost\": \"obwrnibfvakxvlm5ajze.us-west-2.aoss.amazonaws.com\",\n```",
      "Excessive blank lines before closing brace. Remove the extra blank lines on lines 21-22 to maintain consistent formatting.\n```suggestion\n\n```",
      "Extra blank line with trailing whitespace before closing brace. Remove the blank line on line 21 to maintain consistent formatting.\n```suggestion\n\n```",
      "Trailing whitespace should be removed.\n```suggestion\n\n```",
      "[nitpick] Unnecessary blank line added. This extra whitespace does not improve readability and should be removed.\n```suggestion\n\n```",
      "Remove trailing whitespace after the closing brace.\n```suggestion\n        }\n```",
      "Remove trailing whitespace on line 234. This violates Python style guidelines and may be caught by linting tools.\n```suggestion\n\n```",
      "This is an incomplete echo command that will produce an error. This line should either be removed or completed with the intended content to echo.\n```suggestion\n\n```",
      "Empty line before closing brace is unnecessary and inconsistent with other conditional blocks in the file.\n```suggestion\n\n```",
      "There are extra blank lines (241-242) that should be removed to maintain consistent spacing in the codebase.\n```suggestion\n\n```",
      "There are extra blank lines (line 106-107) that should be removed to maintain consistent spacing in the codebase.\n```suggestion\n\n```",
      "Remove excessive blank lines. The codebase style appears to use single blank lines between methods.\n```suggestion\n\n```",
      "This blank line contains trailing whitespace, which will typically be flagged by formatters/linters. Remove the spaces so the line is empty.\n```suggestion\n\n```"
    ]
  },
  {
    "size": 27,
    "representative": "Have ran ruff check, and the formatting is correct",
    "top_paths": [
      "accounts/src/accounts/service/addresses/create.py",
      "accounts/src/accounts/dynamodb/account_repository/account_repository.py",
      "common/src/db/parser/Parser.py",
      "tests/automation/tests/sync/test_cdh_iam_sync.py",
      "accounts/src/accounts/routers/accounts.py"
    ],
    "comments": [
      "Can we run `ruff` on these files for consistent formatting?",
      "I don't think this is formatted correctly can you run:\r\n```\r\ncd accounts\r\npoetry run ruff format .\r\n```\r\nto make sure",
      "Looks like you may need to run ruff",
      "can you run?\r\n```\r\ncd accounts\r\npoetry run ruff format .\r\n```\r\nTo make sure formatting is correct",
      "Have ran ruff check, and the formatting is correct",
      "This will pass ruff check. But please also run the above commands to standardize the format with the rest of the BLU code.",
      "Looks like ruff formatter needs to be run again",
      "Did we run `ruff` on this?",
      "Some of these lines look too long, even for our generous ruff formatting.   Might try running ruff locally or set up precommit hook.",
      "did ruff check and fixed for this file. Will be doing for all in upcoming PRs.",
      "This indentation looks wonky.  Please consider `ruff format`",
      "Ruff format, line too long.",
      "Please run `ruff` to format this file so it stays consistent with the rest of the account's src code.",
      "Are you using a precommit hook to `ruff format` or maybe just manually applying it?",
      "I assume ruff was barking at this one?  Probably fine, though might be worth a configuration change to ruff \ud83e\udd14 \r\n\r\nAnyway, no issue on my end for this PR.",
      "Can you run `poetry run ruff format .` to make sure all your changes are properly formatted. I'm pretty sure their should be two newlines here",
      "`poetry run ruff format .`\r\nhas made some changes to formatting though, wasn't aware of standard of ruff being different on accounts BLU\r\nmade changes accordingly",
      "Yeah, ruff format is an auto-formatter(similar to black) that maintains consistent formatting. Even if some blocks of code pass lint checks the formatting can still vary if formatting is not applied. The standard is still consistent across BLUs. Other BLUs just need to be auto-formatted.",
      "hmm strange, I don't manually make or modify this file. Everything is through poetry. So poetry must be adding these for some reason.",
      "Might have missed formatting with ruff on this one.",
      "Why did we do these formatting changes? I can see these are conflicting with our current convention/ ruff configuration.",
      "The formatting changes are due to this file being autogenerated from the grammar rsql.ebnf. The ruff linter checks have passed.",
      "yeah okay. \r\n\r\nWe have not enforced these in check. Running ruff format would have fixed it though.",
      "This must be just on the border of our ruff format rule. \ud83e\udd14",
      "You should run ruff format over changed files to make sure formatting is consistent",
      "can you run ruff format on all of these files please? this looks like its not formatted in a standard way",
      "Lets run ruff format to get consistent formatting"
    ]
  },
  {
    "size": 27,
    "representative": "The DataRegion is still set to 'us' but this is an EU region configuration (eu-west-1). Based on the pattern in other files and the PR's purpose, this should be 'eu' to match the regional deployment.\n```suggestion\n  \"DataRegion\" : \"eu\",\n```",
    "top_paths": [
      "users/src/service.py",
      "setup/infra/sam/configuration/qa1/eu/eu-west-1/events-env.json",
      "setup/infra/sam/configuration/qa/eu/eu-west-1/events-env.json",
      "setup/infra/sam/configuration/qa/eu-gb/eu-west-2/graphql-env.json",
      "setup/infra/sam/configuration/prod/us-west-2/authorizers-env.json"
    ],
    "comments": [
      "\"Region\": \"uw2\"  ?? also the variable naming conventions",
      "File is us-west-2, should this be uw2?",
      "us-east-1 region suffix to be updated as ue1",
      "[nitpick] The region 'us' is hardcoded. Consider making this configurable through an environment variable or parameter to support multi-region deployments.",
      "The region value 'us' is hardcoded. Consider making this configurable or deriving it from the current context to avoid potential issues in multi-region deployments.",
      "Region code 'uw2' for us-west-2 is inconsistent with the 'ue1' code used for us-east-1. Consider using a consistent naming convention like 'usw2' and 'use1' or standardizing on the abbreviated format.\n```suggestion\n  \"Region\": \"usw2\",\n```",
      "The indentation for the `DataRegion` property has an extra space (5 spaces instead of 4). This inconsistency with other properties in the file and across other configuration files should be corrected to maintain consistent formatting.\n```suggestion\n    \"DataRegion\": \"us\"\n```",
      "The regional database name references 'qa2' but this is a qa1 configuration. It should be 'iam-regional-qa1-eu-gb' to match the qa1 environment.\n```suggestion\n  \"IamRegionalDb\": \"iam-regional-qa1-eu-gb\"\n```",
      "The regional database name references 'iam-regional-qa2-eu' but this is the eu-gb region. It should be 'iam-regional-qa2-eu-gb' to match the eu-gb data region.\n```suggestion\n  \"IamRegionalDb\": \"iam-regional-qa2-eu-gb\"\n```",
      "The regional database name references \"qa2\" but this is a qa1 configuration. It should be \"iam-regional-qa1-eu\" to match the environment.\n```suggestion\n  \"IamRegionalDb\": \"iam-regional-qa1-eu\"\n```",
      "The regional database name should be \"iam-regional-qa-eu\" to match the eu-west-1 region configuration, not \"iam-regional-qa-eu-gb\".\n```suggestion\n  \"IamRegionalDb\": \"iam-regional-qa-eu\"\n```",
      "The DataRegion is still set to \"us\" but should be \"eu\" based on the file path (qa/eu/eu-west-1). This inconsistency could cause data residency or routing issues.\n```suggestion\n  \"DataRegion\" : \"eu\",\n```",
      "The DataRegion is still set to \"us\" but should be \"eu-gb\" based on the file path (qa/eu-gb/eu-west-2). This inconsistency could cause data residency or routing issues.\n```suggestion\n  \"DataRegion\" : \"eu-gb\",\n```",
      "The DataRegion is still set to 'us' but this is an EU region configuration (eu-west-1). Based on the pattern in other files and the PR's purpose, this should be 'eu' to match the regional deployment.\n```suggestion\n  \"DataRegion\" : \"eu\",\n```",
      "Typo in region code: 'eu-gu1' should be 'eu-gb1' to match the directory structure and other configuration files.\n```suggestion\n    \"Region\": \"eu-gb1\",\n```",
      "The region value 'eu-gu1' appears to be a typo. Based on the file path (eu-gb/eu-west-2) and other similar configurations, this should likely be 'eu-gb1'.\n```suggestion\n    \"Region\": \"eu-gb1\",\n```",
      "DataRegion is set to 'us' but this is a qa/eu/eu-west-1 configuration. Based on the file path and consistent with the events-env.json change in the same directory, this should be 'eu'.\n```suggestion\n    \"DataRegion\": \"eu\",\n```",
      "@Ragupathi-veludurai why referring suffix_region from DATA_REGION , it should be uw2,ue1,eu-gb1,eu-gb2 .\nDATA_REGION is us,eu-gb",
      "The \"DataRegion\" configuration was removed from this environment configuration but not from others. This inconsistency suggests \"DataRegion\" may have been mistakenly removed only from this file. Verify if this removal is intentional or if it should be retained.",
      "The available region value changed from 'us-east-1' to 'eu-west-1', but the default remains 'us-west-2'. This inconsistency means the default is not in the available values list. Consider adding 'us-west-2' to available_values or changing the default to 'eu-west-1'.\n```suggestion\n      \"default\": \"eu-west-1\"\n```",
      "The state file key uses \"eu-gb\" suffix but other eu-west-2 configurations in this PR use \"eu-gb1\" suffix (e.g., prod, dev). The naming should be consistent across all environments for the same region.\n```suggestion\nkey = \"qa2/config-management/iam-qa2-configs-eu-gb1.state\"\n```",
      "The state file key uses \"eu-gb\" suffix but other eu-west-2 configurations in this PR use \"eu-gb1\" suffix (e.g., prod, dev). The naming should be consistent across all environments for the same region.\n```suggestion\nkey = \"qa1/config-management/iam-qa1-configs-eu-gb1.state\"\n```",
      "The state file key uses \"eu-gb\" suffix but other eu-west-2 configurations in this PR use \"eu-gb1\" suffix (e.g., prod, dev). The naming should be consistent across all environments for the same region.\n```suggestion\nkey = \"qa/config-management/iam-qa-configs-eu-gb1.state\"\n```",
      "The state file key uses \"eu-gb\" suffix but other eu-west-2 configurations in this PR use \"eu-gb1\" suffix (e.g., prod, dev). The naming should be consistent across all environments for the same region.\n```suggestion\nkey = \"dev4/config-management/iam-dev4-configs-eu-gb1.state\"\n```",
      "The DataRegion value is inconsistent with other configuration files. In this file, DataRegion is set to \"eu-gb1\", but all other files in the same directory use \"eu-gb\" as the DataRegion value. This should be \"eu-gb\" to match the pattern used throughout the codebase where DataRegion represents the geographic region and Region represents the specific suffix.\n```suggestion\n    \"DataRegion\": \"eu-gb\"\n```",
      "The regional database name appears incorrect for the UK region. It should be \"iam-regional-prod-eu-gb\" instead of \"iam-regional-prod-us\" to match the eu-gb region configuration.\n```suggestion\n    \"IamRegionalDb\": \"iam-regional-prod-eu-gb\"\n```",
      "This resource will get deployed only in the US region as it has condition enabled 'IsUSRegion'"
    ]
  },
  {
    "size": 26,
    "representative": "Remove print statements or replace with logger if needed.",
    "top_paths": [
      "tests/automation/tests/groups/test_groups_apptoken.py",
      "accounts/cdh-migration/export_scripts/export/iam_cdh_validation_snowflake_export.py",
      "common/src/db/app.py",
      "groups/src/service.py",
      "common/src/db/iam_database.py"
    ],
    "comments": [
      "Replace prints with logs if needed.",
      "Remove prints from code.  If you need to have this info, please use a logger.",
      "Remove print statements from code",
      "logger function used instead of print function.",
      "Remove print statements/debug prints from code.  If logging is needed, please use logger.",
      "Avoid `print()` in favor of logging.",
      "Remove print statements or replace with logger if needed.",
      "Remove the print statements. use logger.",
      "Prefer logger to print, or remove print statements if they were just for debugging.",
      "I know there is a reason for using `print()` vs `logger`, but do we need to keep these?",
      "print statement to be removed?",
      "print statements to be removed?",
      "use logger instead of print - in all places",
      "Can we remove print statements and replace it with logger?",
      "Please use logger instead of print",
      "Debug print statements should be removed from production code. Consider using proper logging with appropriate log levels instead of print statements.",
      "print statements to be removed",
      "Debug print statement should be removed from production code. Consider using proper logging with appropriate log levels instead.",
      "replace print with logger in all places",
      "These commented-out lines appear to be debugging/development code that should be removed before merging to production.\n```suggestion\n\n```",
      "Remove debug logging statement before merging to production. This appears to be temporary debugging code that should not be committed.\n```suggestion\n\n```",
      "Remove verbose debug logging statements before merging to production. These detailed logs (lines 149-154) should be removed as they appear to be temporary debugging code and could impact performance in production.\n```suggestion\n\n```",
      "Remove verbose debug logging statements before merging to production. These detailed logs appear to be temporary debugging code and could leak sensitive data structures in production logs.\n```suggestion\n\n```",
      "Debug logging statement should be removed before merging to production. This appears to be leftover debugging code that adds no value in production and may impact performance in high-traffic scenarios.\n```suggestion\n\n```",
      "Debug print statement should be removed before merging to production. Use the existing logging framework instead.",
      "do we really need to pass logger here? we could go with simple \"print\" statements right? Because loggers are some what heavier I feel."
    ]
  },
  {
    "size": 26,
    "representative": "The DynamoDB table ARN references 'iam-regional-dev-us' in the eu-west-1 region, which appears inconsistent. If this is a US-specific table, it should likely be in a US region or renamed to reflect the EU region.\n```suggestion\ndynamodb_resource = [\"arn:aws:dynamodb:eu-west-1:387675059062:table/iam-central-dev\",\"arn:aws:dynamodb:eu-west-1:387675059062:table/iam-regional-dev-eu\"]\n```",
    "top_paths": [
      "events/template.yml",
      "setup/infra/sam/configuration/dev/us/us-east-1/events-env.json",
      "setup/infra/sam/configuration/prod/eu/eu-west-1/events-env.json",
      "setup/infra/terraform_iac/data/configuration/prod/us-west-2/prod.tfvars",
      "setup/infra/sam/configuration/qa1/eu/eu-west-1/events-env.json"
    ],
    "comments": [
      "@Sweatha1609 please proceed enabling global table for prod dynamodb",
      "The FileServiceEventSync value is changed from 'true' to 'false'. This change should be verified to ensure it's intentional and won't break existing functionality, as it appears to be a behavioral change rather than just cleanup of v1 resources.\n```suggestion\n  \"FileServiceEventSync\": \"true\",\n```",
      "The DynamoDB stream ARNs reference 'qa2' tables but this is qa1 configuration. Should reference 'iam-regional-qa1-us' and 'iam-central-qa1' tables.",
      "The DynamoDB stream ARNs reference 'qa2' tables but this is qa1 configuration. Should reference 'iam-regional-qa1-eu-gb' and 'iam-central-qa1' tables.\n```suggestion\n  \"RegionalTableStreamArn\": \"arn:aws:dynamodb:eu-west-2:387675059062:table/iam-regional-qa1-eu-gb/stream/2025-05-11T14:16:13.894\",\n  \"CentralTableStreamArn\": \"arn:aws:dynamodb:eu-west-2:387675059062:table/iam-central-qa1/stream/2025-05-11T14:16:13.849\",\n```",
      "The region in the ARN is 'us-west-2' but the table name suggests 'eu-gb'. The ARN should use 'eu-west-1' or 'eu-west-2' region to match the eu table location.\n```suggestion\n  \"RegionalTableStreamArn\": \"arn:aws:dynamodb:eu-west-1:387675059062:table/iam-regional-qa-eu-gb/stream/2025-05-11T14:16:13.894\",\n```",
      "The RegionalTableStreamArn is a placeholder '<to-be-updated>'. This should be replaced with the actual stream ARN for EU production before deployment.\n```suggestion\n    \"RegionalTableStreamArn\": \"arn:aws:dynamodb:eu-west-1:824749077548:table/iam-regional-prod-eu/stream/2025-11-20T14:49:12.625\",\n```",
      "The FileServiceEventSync is a placeholder '<to-be-updated>'. This should be replaced with the actual value for EU production before deployment.\n```suggestion\n    \"FileServiceEventSync\": \"arn:aws:dynamodb:eu-west-1:824749077548:table/file-service-events-prod/stream/2025-11-20T14:49:12.625\",\n```",
      "The DynamoDB table ARN references 'iam-regional-stage-us' in the eu-west-1 region, which appears inconsistent. If this is a US-specific table, it should likely be in a US region or renamed to reflect the EU region.\n```suggestion\ndynamodb_resource = [\"arn:aws:dynamodb:eu-west-1:751019381194:table/iam-central-stage\",\"arn:aws:dynamodb:eu-west-1:751019381194:table/iam-regional-stage-eu\"]\n```",
      "The DynamoDB table ARN references 'iam-regional-stage-us' in the eu-west-2 region, which appears inconsistent. If this is a US-specific table, it should likely be in a US region or renamed to reflect the EU region.\n```suggestion\ndynamodb_resource = [\"arn:aws:dynamodb:eu-west-2:751019381194:table/iam-central-stage\",\"arn:aws:dynamodb:eu-west-2:751019381194:table/iam-regional-stage-eu\"]\n```",
      "The DynamoDB table ARN references 'iam-regional-prod-us' in the eu-west-1 region, which appears inconsistent. If this is a US-specific table, it should likely be in a US region or renamed to reflect the EU region.\n```suggestion\ndynamodb_resource = [\"arn:aws:dynamodb:eu-west-1:824749077548:table/iam-central-prod\"]\n```",
      "The DynamoDB table ARN references 'iam-regional-prod-us' in the eu-west-2 region, which appears inconsistent. If this is a US-specific table, it should likely be in a US region or renamed to reflect the EU region.\n```suggestion\ndynamodb_resource = [\"arn:aws:dynamodb:eu-west-2:824749077548:table/iam-central-prod\",\"arn:aws:dynamodb:eu-west-2:824749077548:table/iam-regional-prod-eu\"]\n```",
      "The DynamoDB table ARN references 'iam-regional-dev-us' in the eu-west-1 region, which appears inconsistent. If this is a US-specific table, it should likely be in a US region or renamed to reflect the EU region.\n```suggestion\ndynamodb_resource = [\"arn:aws:dynamodb:eu-west-1:387675059062:table/iam-central-dev\",\"arn:aws:dynamodb:eu-west-1:387675059062:table/iam-regional-dev-eu\"]\n```",
      "The DynamoDB table ARN references 'iam-regional-dev-us' in the eu-west-2 region, which appears inconsistent. If this is a US-specific table, it should likely be in a US region or renamed to reflect the EU region.\n```suggestion\ndynamodb_resource = [\"arn:aws:dynamodb:eu-west-2:387675059062:table/iam-central-dev\",\"arn:aws:dynamodb:eu-west-2:387675059062:table/iam-regional-dev-eu\"]\n```",
      "The hardcoded placeholder ARN 'arn:aws:dynamodb:us-east-1:123456789012:table/YourTableName' should be removed or replaced with a meaningful comment explaining why it's required. The comment on line 65 indicates it doesn't matter, but this placeholder could cause confusion.\n```suggestion\n            # The 'dynamodb.tableArn' field is required by the API, but its value is ignored when 'dynamodb.export' is set to 's3'.\n            # Using a dummy value to avoid confusion with a real ARN.\n            \"dynamodb.tableArn\": \"DUMMY_ARN_NOT_USED\",\n```",
      "Corrected field name from 'accounts_id' to 'account_id' to match the expected CSV column name in the script.\n```suggestion\n- `account_id` - The UUID of the account to be removed from the DynamoDB table\n\nExample:\n```csv\naccount_id\n```",
      "The 'REGIONAL_TABLE' environment variable is loaded but never used in service.py. Only 'CENTRAL_TABLE' is used for DynamoDB operations. Consider removing this unused variable.\n```suggestion\n\n```",
      "The DynamoDB stream ARNs are set to empty strings for us-east-1, which will likely cause errors when the events service attempts to process streams. Either populate these with valid stream ARNs or remove these properties if streams are not needed for this region.\n```suggestion\n\n```",
      "The documented environment variable DYNAMODB_TABLE_NAME does not match the actual variables used in the script (REGIONAL_TABLE and CENTRAL_TABLE on lines 18-19 of delete_accounts.py). Update the documentation to reflect the correct variable names.\n```suggestion\nREGIONAL_TABLE=your-regional-dynamo-table-name\nCENTRAL_TABLE=your-central-dynamo-table-name\n```",
      "The environment variable assignments appear to be reversed. IAM_CENTRAL_DB is assigned the value of IamRegionalDb, and IAM_REGIONAL_DB is assigned IamCentralDb. For a central-to-regional sync function, IAM_CENTRAL_DB should reference IamCentralDb and IAM_REGIONAL_DB should reference IamRegionalDb.\n```suggestion\n          IAM_CENTRAL_DB: !Ref IamCentralDb\n          IAM_REGIONAL_DB: !Ref IamRegionalDb\n```",
      "The FileServiceEventSync configuration value is marked as \"<to-be-updated>\" in production. This should be resolved before deployment to ensure proper event synchronization behavior.\n```suggestion\n    \"FileServiceEventSync\": \"DISABLED\",\n```",
      "`IAM_CENTRAL_DB` and `IAM_REGIONAL_DB` appear swapped (central points to `IamRegionalDb` and regional points to `IamCentralDb`). This will route writes/reads to the wrong DynamoDB tables for the central\u2192regional sync; swap the references so central uses `IamCentralDb` and regional uses `IamRegionalDb`.\n```suggestion\n          IAM_CENTRAL_DB: !Ref IamCentralDb\n          IAM_REGIONAL_DB: !Ref IamRegionalDb\n```",
      "The DB environment variables look swapped: `IAM_CENTRAL_DB` points to `IamRegionalDb` and `IAM_REGIONAL_DB` points to `IamCentralDb`. For a central\u2192regional sync function, this appears inverted and can cause writes/reads against the wrong table(s). Recommend swapping these refs to match the variable names (or rename the variables if the inversion is intentional).\n```suggestion\n          IAM_CENTRAL_DB: !Ref IamCentralDb\n          IAM_REGIONAL_DB: !Ref IamRegionalDb\n```",
      "`ExpressionAttributeNames` values must be *attribute names*, not runtime values. Passing `id` and `entity_type` here will produce invalid expressions (and won\u2019t check the intended attributes). Map the placeholders to the actual attribute keys in the item (e.g., `\\\"id\\\"` and `\\\"_ref\\\"`, or the table\u2019s PK/SK attribute names if different).\n```suggestion\n        # Map expression attribute name placeholders to actual DynamoDB attribute names\n        expression_names = {\"#pk\": \"id\", \"#sk\": \"_ref\"}\n```",
      "Hardcoded placeholder ARN. The tableArn on line 31 contains a placeholder value \"arn:aws:dynamodb:us-east-1:123456789012:table/YourTableName\". While the comment states it's required but not used, having placeholder values in production code can be confusing and may lead to issues if the behavior changes. Consider using a more clearly marked placeholder or documenting why this specific value is safe.\n```suggestion\n        # Required by Glue for DynamoDB export, but this job reads from S3 and does not\n        # actually use the table ARN. Use an obviously placeholder ARN to avoid\n        # accidentally referencing a real table if behavior changes.\n        \"dynamodb.tableArn\": \"arn:aws:dynamodb:REGION:ACCOUNT_ID:table/PLACEHOLDER_TABLE\",\n```",
      "`dynamodb.tableArn` is set to a placeholder ARN (`123456789012:table/YourTableName`). In practice Glue\u2019s DynamoDB connector will validate/use this value, so this job will fail or target the wrong table. Make the table ARN (or table name) a required job argument and pass it here instead of hardcoding a dummy value.",
      "`FileServiceEventSync` is set to the placeholder value `\"<to-be-updated>\"`. This will either break deployments that validate env vars or lead to undefined runtime behavior if the service expects a boolean/string flag. Please replace it with the intended value for eu-central-1 prod (and consider failing CI if placeholders remain).\n```suggestion\n  \"FileServiceEventSync\": \"false\",\n```"
    ]
  },
  {
    "size": 26,
    "representative": "Please add dedicated unit tests for this function",
    "top_paths": [
      "accounts/src/accounts/dynamodb/account_repository/account_repository_impl.py",
      "accounts/src/accounts/service/get.py",
      "applications/src/service.py",
      "accounts/src/accounts/cdh_sync/util.py",
      "roles/tests/test_app.py"
    ],
    "comments": [
      "Function name is misleading with the test cases",
      "I ended up changing the test to be >=",
      "This is duplicate test case",
      "Add unit tests for this method",
      "Add unit tests for this method.",
      "It looks like we might still need to cover the \"application\" case in unit testing for this function. Or am I missing it?",
      "This change is breaking the test",
      "@ragulkarthi Add proper unit tests",
      "If possible can we add corresponding unit tests for this update",
      "Kindly add unit test for the function",
      "update unit test to support this changes",
      "@ragulkarthi Add separate test cases for each",
      "I think this function needs unit tests still",
      "Make sure we have coverage for all exception handlers",
      "This function is missing unit tests.",
      "Kindly add unit test for this validation",
      "Can you add some dedicated unit tests for this",
      "Can you add unit test coverage for this check?",
      "Please add dedicated unit tests for this function",
      "This function needs a dedicated unit test",
      "Why have we defined this function if it is not used in any of the tests?",
      "You are already executing both cases in the above tests",
      "I just saw this in the other PR. But can we add a dedicated unit test for this function before merging?",
      "We will need a quick unit test to add coverage for this exception",
      "Unit test for this as well",
      "I think we might be missing coverage for this exception"
    ]
  },
  {
    "size": 22,
    "representative": "The query contains duplicate `aws-env:profiles-${var.stack}` tags, which will cause the query to be invalid. Remove the duplicate tag.\n```suggestion\n                \"query\": \"sum:aws.lambda.errors{functionname:iam-users-sync-cdh-${var.stack}-*,aws-env:profiles-${var.stack},region:us-west-2}.as_count()\"\n```",
    "top_paths": [
      "setup/infra/terraform_iac/dd-monitors/cdh_to_iam_sync_metrics_dashboard.tf",
      "setup/infra/terraform_iac/dd-monitors/profiles_to_iam_sync_metrics_dashboard.tf",
      "setup/infra/terraform_iac/dd-monitors/sre_golden_metrics_dashboard.tf",
      "setup/infra/terraform_iac/dd-monitors/iam_user_sync_dashboard.tf"
    ],
    "comments": [
      "Inconsistent spacing around the comma in the query filters. Line 2842 has a space before the comma while line 2848 does not. This should be consistent throughout the file.\n```suggestion\n                \"query\":\"sum:aws.aoss.4xx{aws-env:profiles-${var.stack},$region} by {collectionname}.as_count()\",\n```",
      "Inconsistent spacing around the comma in the query filters. Line 2842 has a space before the comma while line 2848 does not. This should be consistent throughout the file.\n```suggestion\n                \"query\":\"sum:aws.aoss.5xx{aws-env:profiles-${var.stack} ,$region} by {collectionname}.as_count()\",\n```",
      "The tag name 'awsrulename' appears to be incorrect. Based on similar queries in the file, it should be 'rulename' to match the pattern used elsewhere.\n```suggestion\n                                \"query\": \"sum:aws.events.ingestion_to_invocation_success_latency{aws-env:profiles-${var.stack} ,rulename:iam-users-sync-profiles-rule-* , region:us-west-2}\"\n```",
      "This query uses a different pattern 'iam-accounts-sync-*-*' compared to other similar queries in the file which use patterns like 'iam-accounts-sync-profiles-*'. Consider using a consistent naming pattern for better maintainability.\n```suggestion\n                                    \"query\": \"sum:aws.sqs.number_of_messages_received{aws-env:profiles-${var.stack},queuename:iam-accounts-sync-profiles-*} by {queuename}.as_count()\"\n```",
      "The query contains duplicate `aws-env:profiles-${var.stack}` tags, which will cause the query to be invalid. Remove the duplicate tag.\n```suggestion\n                \"query\": \"sum:aws.lambda.throttles{functionname:iam-users-roles-sync-cdh-${var.stack}-*,aws-env:profiles-${var.stack},region:us-west-2}.as_count()\"\n```",
      "The query contains duplicate `aws-env:profiles-${var.stack}` tags, which will cause the query to be invalid. Remove the duplicate tag.\n```suggestion\n                \"query\": \"sum:aws.lambda.errors{functionname:iam-users-sync-cdh-${var.stack}-*,aws-env:profiles-${var.stack},region:us-west-2}.as_count()\"\n```",
      "The query contains duplicate `aws-env:profiles-${var.stack}` tags, which will cause the query to be invalid. Remove the duplicate tag.\n```suggestion\n                \"query\": \"sum:aws.lambda.throttles{functionname:iam-users-sync-cdh-${var.stack}-*,aws-env:profiles-${var.stack} ,region:us-west-2}.as_count()\"\n```",
      "The query contains duplicate `aws-env:profiles-${var.stack}` tags, which will cause the query to be invalid. Remove the duplicate tag.\n```suggestion\n                \"query\": \"avg:aws.lambda.duration{functionname:iam-users-sync-cdh-${var.stack}-*,aws-env:profiles-${var.stack},region:us-west-2}\"\n```",
      "The query contains duplicate `aws-env:profiles-${var.stack}` tags, which will cause the query to be invalid. Remove the duplicate tag.\n```suggestion\n                \"query\": \"sum:aws.events.ingestion_to_invocation_success_latency{rulename:cdh-to-iam-live-account-sync-rule-${var.stack}*,aws-env:profiles-${var.stack},region:us-west-2}\"\n```",
      "The query contains duplicate `aws-env:profiles-${var.stack}` tags, which will cause the query to be invalid. Remove the duplicate tag.\n```suggestion\n                \"query\": \"sum:aws.events.matched_events{rulename:cdh-to-iam-live-account-sync-rule-${var.stack}*,aws-env:profiles-${var.stack} ,region:us-west-2}.as_count()\"\n```",
      "The query contains duplicate `aws-env:profiles-${var.stack}` tags, which will cause the query to be invalid. Remove the duplicate tag.\n```suggestion\n                \"query\": \"sum:aws.lambda.throttles{functionname:iam-accounts-sync-cdh-${var.stack}-*,aws-env:profiles-${var.stack} , region:us-west-2}.as_count()\"\n```",
      "There's an inconsistency in the rule name pattern. This query uses 'iam-users-sync-profiles-rule-*-*' but it's in the Device sync section which should use device-related rule names like 'iam-device-sync-profiles-rule-*' as seen in other parts of the dashboard.\n```suggestion\n                                \"query\": \"sum:aws.events.ingestion_to_invocation_success_latency{aws-env:profiles-${var.stack} , rulename:iam-device-sync-profiles-rule-* , region:us-west-2}\"\n```",
      "Incorrect function name pattern for Device sync section. This query uses 'iam-users-sync-profiles-*-*' but should use 'iam-devices-sync-profiles-*' to match the device sync context and other queries in this section.\n```suggestion\n                                \"query\": \"sum:aws.lambda.throttles{aws-env:profiles-${var.stack} , functionname:iam-devices-sync-profiles-* , region:us-west-2}.as_count()\"\n```",
      "Wrong rule name in Device sync section. This query uses 'iam-users-sync-profiles-rule-*' but should use 'iam-device-sync-profiles-rule-*' to match the section's purpose and maintain consistency with other device sync queries.\n```suggestion\n                                \"query\": \"sum:aws.events.matched_events{aws-env:profiles-${var.stack} , rulename:iam-device-sync-profiles-rule-* ,region:us-west-2}.as_count()\"\n```",
      "This query is missing the aws-env and region filters that are consistently used throughout the rest of the dashboard. Add 'aws-env:profiles-${var.stack}' and 'region:us-west-2' for consistency.\n```suggestion\n                                \"query\": \"sum:aws.events.invocations_sent_to_dlq{rulename:iam-users-sync-profiles-rule-*,aws-env:profiles-${var.stack},region:us-west-2}\"\n```",
      "This query is missing the 'region:us-west-2' filter that is consistently applied to other queries in the dashboard. Add the region filter for consistency.\n```suggestion\n                \"query\": \"sum:aws.events.matched_events{rulename:cdh-to-iam-live-account-sync-rule-${var.stack}*,aws-env:profiles-${var.stack},region:us-west-2}.as_count()\"\n```",
      "[nitpick] Inconsistent spacing around commas in query parameters. Some queries have spaces after commas while others don't. Consider standardizing the formatting for better readability and consistency.\n```suggestion\n                \"query\": \"sum:aws.lambda.invocations{functionname:iam-accounts-sync-cdh-${var.stack}-*,aws-env:profiles-${var.stack},region:us-west-2}.as_count()\"\n```",
      "[nitpick] Inconsistent spacing around commas in query filters. Some filters have spaces around commas while others don't. Consider standardizing the spacing for better readability and consistency.\n```suggestion\n                                \"query\": \"sum:aws.lambda.invocations{aws-env:profiles-${var.stack},functionname:iam-devices-sync-profiles-*,region:us-west-2}.as_count()\"\n```",
      "Inconsistent spacing in function name filter. Remove the space after the colon to match the pattern used throughout the rest of the dashboard (e.g., line 167, 273, 321).\n```suggestion\n                \"query\": \"sum:aws.lambda.errors{functionname:iam-to-cdh-sync-users-roles-${var.stack}-uw2}.as_count()\"\n```",
      "Missing space after comma in the query string. The pattern is inconsistent with line 92 which has a space after the comma between 'profiles-${var.stack}' and 'apiname'. Consider adding a space after the comma for consistency: 'profiles-${var.stack}, apiname'.\n```suggestion\n                \"query\": \"avg:aws.apigateway.latency{aws-env:profiles-${var.stack}, apiname:iam-gateway-${var.stack}-*,$region}\",\n```",
      "Inconsistent spacing: there is a space before the comma in '${var.stack}-* ,$region'. The space should be after the comma, not before it. Update to '${var.stack}-*,$region'.\n```suggestion\n                \"query\": \"avg:aws.lambda.enhanced.invocations{service:iam-devices,env:${var.stack},resource:iam-devices-devicessyncfunction-${var.stack}-*,$region}\"\n```",
      "The resource filter changed from 'iam-devices-devicessyncfunction-${var.stack}-*' to 'iam-devices-devicessync*', which removes the stack variable and function name specificity. This may unintentionally broaden the query scope and include resources from different stacks. Additionally, there is a space before the comma that should be removed.\n```suggestion\n                \"query\": \"avg:aws.lambda.enhanced.errors{service:iam-devices,env:${var.stack},resource:iam-devices-devicessyncfunction-${var.stack}-*,$region}\"\n```"
    ]
  },
  {
    "size": 21,
    "representative": "This will be checked above Dharma",
    "top_paths": [
      "groups/src/service.py",
      "common/src/common/context/context_graph_dal.py",
      "roles/src/service.py",
      "users/tests/test_users_service.py",
      "users/src/service.py"
    ],
    "comments": [
      "Have added the doc for the new and existing methods Dharma.",
      "this is old code Dharma have removed this model and added separately not sure why this is not updated",
      "In /users/{userId}/groups also we are supporting only for self user Dharma",
      "Resource only dharma since all other blus give same response this will throw when groupid is invalid",
      "It is there Dharma, git diff is showing wrong get is in line 30-26",
      "This line is in a api which is not exposed Dharma, in next iteration this will be fixed",
      "This will be checked above Dharma",
      "will change the naming Dharma",
      "I added but dharma needed only on global variable I will conform once",
      "we could identify this in during creation of virtual group - we could catch the unique constraint error and ignore",
      "oh ok will change Dharma",
      "But Dharma, upon catching the unique constraint error, don't the group details",
      "I have introduced this as we changed the operations transactional Dharma",
      "It is being handled in authorizer helper file Dharma",
      "This is the existing code, is it fine to change in this PR Dharma?",
      "This will be taken care in Patch user account API Dharma",
      "It is for update call Dharma, this PR change is for patch",
      "We have a separate task to remove this Dharma post this release",
      "We will not delete the relations Dharma",
      "Sorry misu nderstood, have refactored now Dharma",
      "Not required Dharma. Will remove it."
    ]
  },
  {
    "size": 20,
    "representative": "I will be raising an incremental PR. I will include this change there.",
    "top_paths": [
      "tests/automation/tests/conftest.py",
      "accounts/cdh-migration/migrate_scripts/migrate/addresses/address_writer.py",
      "accounts/template.yaml",
      "tests/automation/utils/printhelpers.py",
      "roles/src/app.py"
    ],
    "comments": [
      "If you can link the updated change here in comments (or ticket that demonstrates need to do so), then I will approve this PR as is.",
      "For this small change again with the same line why we need separate PR? I don't know whats the plan with that separate PR for that change alone.",
      "This could probably all be refactored.  Please consider for future PR.",
      "Make sense. Will update this function in my next PR.",
      "I will be raising an incremental PR. I will include this change there.",
      "Please update in follow-up PR",
      "are these changes meant to be included in the PR?",
      "is this change intended in this PR ?",
      "Have updated the PR accordingly",
      "We will plan to work on this as part of subsequent PRs.",
      "Will fix this in the next PR",
      "Will add in a followup PR",
      "We will be refactoring this setup as part of a subsequent PR",
      "If just refactor, maybe we can have change in separate PR.  Just better in my opinion to not \"sneak\" changes in.",
      "We will refactor this as part of a subsequent PR.",
      "Will refactor as part of a subsequent PR",
      "will add this in upcoming PR",
      "This will be removed on my next PR",
      "This will be resolved on the next PR",
      "This code already existed, and was not added in this PR, if required will be fixed in the future."
    ]
  },
  {
    "size": 20,
    "representative": "Line 483 incorrectly assigns 'line1' to 'addressLine2'. It should use 'line2' from the streetAddress object.\n```suggestion\n        address[\"payload\"][\"addressLine2\"] = dnb_address.get(\"streetAddress\").get(\"line2\")\n```",
    "top_paths": [
      "users/src/identities_wrapper.py",
      "accounts/src/accounts/service/search.py",
      "graphql/schema.graphql",
      "common/src/common/geo/services/__init__.py",
      "common/src/common/geo/models.py"
    ],
    "comments": [
      "Instantiating a Country value object just to access its .value property is redundant. Since searchable_address.country is already a string and Country validation happens in the constructor, consider either keeping the validation (and using the object) or removing this wrapper entirely if validation is not needed at this point.\n```suggestion\n            country=searchable_address.country,\n```",
      "i dont think there's any need to keep street address now",
      "The documentation comment only appears on addressLine1. Each field should have its own description, or the comment should be placed before all four fields as a group comment. Additionally, the existing `streetAddress` field at line 118 is not removed, creating a breaking inconsistency where both old and new fields coexist in the Address type.\n```suggestion\n\t\"\"\"\n\tAddress lines 1-4 describe the address of the place, as per the conventions of its country or region.\n\tFor example, addressLine1 might be \"10368 Westmoor Dr\", addressLine2 could be \"Suite 200\", etc.\n\t\"\"\"\n\taddressLine1: String\n\taddressLine2: String\n\taddressLine3: String\n\taddressLine4: String\n```",
      "The new addressLine fields are all optional (String vs String!), while the original streetAddress at line 118 remains in the schema. This creates API inconsistency. If streetAddress is being deprecated, it should be marked as deprecated with @deprecated directive and scheduled for removal. If these fields are meant to replace streetAddress, the old field should be removed to prevent confusion about which fields clients should use.",
      "I'm assuming that leaving this in is for backwards compatibility/reading the existing index?\n\n@Sakthi1305ganesh might we consider rebuilding the index in the future to remove the need to support street address?",
      "> I'm assuming that leaving this in is for backwards compatibility/reading the existing index?\r\n> \r\n> @Sakthi1305ganesh might we consider rebuilding the index in the future to remove the need to support street address?\r\n\r\nif address_line_1 is an alias of street_address then we wouldn't need to reindex since a geocode for the address would remain the same. @jfrench-trimble \r\n\r\nIf a migration is being done for the existing data, it would be best to add a filter(on the event rule if viable) to skip ingestion on address updates with an addition of address_line_1 (with or without removal of street_address)\r\n@jfrench-trimble @samoconnell",
      "Direct dictionary access `entity.payload[\\\"region\\\"]` will raise a KeyError if the 'region' key is missing. Use `.get(\\\"region\\\")` with a default value or add validation to handle cases where the region field might be absent from the payload.\n```suggestion\n            region_mapping[entity.id] = entity.payload.get(\"region\")\n```",
      "The `street_address` field is still present in the common Address model but has been removed everywhere else in the codebase. This inconsistency will cause issues when this model is used alongside the accounts service models. Either remove this field to match the deprecation, or document why it remains in the common module.\n```suggestion\n\n```",
      "The `street_address` field remains in `SearchableAddress` but has been removed from all other address models and the GraphQL schema. This creates an inconsistency where search criteria can specify a field that no longer exists in the address model, potentially causing runtime errors or unexpected behavior.\n```suggestion\n\n```",
      "The address construction in search only includes `address_line_1` but the Address model supports `address_line_1` through `address_line_4`. If users provide `address_line_2-4` in the search criteria (via the still-present `SearchableAddress` fields), these values will be ignored during geocoding, leading to inaccurate search results.\n```suggestion\n            address_line_1=searchable_address.address_line_1,\n            address_line_2=getattr(searchable_address, \"address_line_2\", None),\n            address_line_3=getattr(searchable_address, \"address_line_3\", None),\n            address_line_4=getattr(searchable_address, \"address_line_4\", None),\n```",
      "The `address_line_2`, `address_line_3`, and `address_line_4` fields were removed from this model, but these fields are actively used in the accounts service (as evidenced by test assertions checking all four address lines). This creates an inconsistency between the common model and the accounts-specific implementation.\n```suggestion\n    address_line_1: Optional[str] = None\n    address_line_2: Optional[str] = None\n    address_line_3: Optional[str] = None\n    address_line_4: Optional[str] = None\n```",
      "@DharmarajanRajendran We have deprecated streetAddress, we should use addressLines here.\nMay be we have this as a tech debt and revisit again.",
      "The `country_config_keys` parameter should be a list, not a string. It should be `country_config_keys=['iam/tid_config/tid_api_key']` to match the expected type signature.\n```suggestion\n                                     country_config_keys=[\"iam/tid_config/tid_api_key\"])\n```",
      "Line 483 incorrectly assigns 'line1' to 'addressLine2'. It should use 'line2' from the streetAddress object.\n```suggestion\n        address[\"payload\"][\"addressLine2\"] = dnb_address.get(\"streetAddress\").get(\"line2\")\n```",
      "Missing safety check before calling .get(\"name\"). If addressRegion is None, this will raise AttributeError. Add conditional check similar to line 473.\n```suggestion\n    if dnb_address.get(\"addressRegion\", {}).get(\"name\"):\n        address[\"payload\"][\"region\"] = dnb_address.get(\"addressRegion\", {}).get(\"name\")\n```",
      "The `request_headers` variable is being updated, but it's not clear if this is a global or instance variable. If it's shared across requests, this could cause concurrency issues with different regions' API keys. Consider creating a local copy of headers for each request.\n```suggestion\n        headers = dict(request_headers)\n        headers.update({\"x-api-key\": self._get_tid_api_key(region)})\n        response = fail_safe_requests().get(url,\n                                            headers=headers,\n```",
      "The variable name `region_urls` is misleading since it contains API keys, not URLs. Rename to `region_api_keys` for clarity.\n```suggestion\n        region_api_keys = {\n            \"us\": TID_API_KEYS.get(\"us\"),\n            \"eu\": TID_API_KEYS.get(\"eu\")\n        }\n        return region_api_keys.get(region, TID_API_KEYS.get(\"us\"))\n```",
      "The module-level `request_headers` dict is being mutated directly. This will cause all subsequent requests in the Lambda execution context to include this header with the same value. Since Lambda containers can be reused across multiple requests, this could lead to incorrect headers being sent to the identity service. The header should be added to a copy of `request_headers` instead, similar to how it's done in `check_user_exists_in_any_region` (line 182) and `update_ttl_for_user_record` (line 520).\n```suggestion\n        headers = request_headers.copy()\n        headers[\"x-src-system\"] = SOURCE_ORIGIN\n        _requests_details = {\n            'json': request_payload,\n            'headers': headers,\n```",
      "The module-level `request_headers` dict is being mutated directly. This will cause all subsequent requests in the Lambda execution context to include the Host and x-src-system headers with these values. Since Lambda containers can be reused across multiple requests, this could lead to incorrect headers being sent to the identity service. These headers should be added to a copy of `request_headers` instead, similar to how it's done in `check_user_exists_in_any_region` (line 182) and `update_ttl_for_user_record` (line 520).",
      "The module-level `request_headers` dict is being mutated directly. This will cause all subsequent requests in the Lambda execution context to include the Host and x-src-system headers with these values. Since Lambda containers can be reused across multiple requests, this could lead to incorrect headers being sent to the identity service. These headers should be added to a copy of `request_headers` instead, similar to how it's done in `check_user_exists_in_any_region` (line 182) and `update_ttl_for_user_record` (line 520).\n```suggestion\n        headers = request_headers.copy()\n        headers[\"Host\"] = host\n        headers[\"x-src-system\"] = SOURCE_ORIGIN\n        _requests_details = {\n            'json': request_payload,\n            'headers': headers,\n```"
    ]
  },
  {
    "size": 19,
    "representative": "check this comment and see if this needs to be fixed",
    "top_paths": [
      "common/src/common/models.py",
      "users/src/service.py",
      "users/src/models.py",
      "organizations/src/Makefile",
      "tests/automation/models/common.py"
    ],
    "comments": [
      "@DharmarajanRajendran Now I have rebased with Main",
      "@EzhilAlagesan and @PrabuMonk @DharmarajanRajendran could you plz change this accordingly",
      "@prakashsahdsb I believe this should be resolved now.",
      "Rebased the Pr with main @DharmarajanRajendran",
      "Moved to different thread, so marking it as resolved.",
      "Moved to different thread. Marking this as resolved.",
      "This is fixed. I reviewed it.",
      "@PrabuMonk @balacommits @prakashsahdsb Need your inputs here on the comment by @aravinth-karthikeyan.",
      "This is not fixed yet",
      "@raahavm @KirthanaKrishnan I have fixed the bug , please review it",
      "i think this needs to be fixed",
      "do we need to fix this?",
      "This needs to be fixed ?",
      "@r-dharanidharan I guess this is valid one. Could you plz check it out this and fix it?. If not plz provide a valid reason and close this.",
      "yeah fixed in both occurences @DharmarajanRajendran.",
      "check if this is valid and fix it.",
      "this is a mistake, fixed now",
      "i think, this should be fixed.",
      "check this comment and see if this needs to be fixed"
    ]
  },
  {
    "size": 18,
    "representative": "use \"f\" formatter instead of concat with +",
    "top_paths": [
      "users/src/app.py",
      "groups/src/app.py",
      "applications/src/apicloud_app_sync.py",
      "roles/src/app.py",
      "authorization/src/app.py"
    ],
    "comments": [
      "Use `f` formatter to avoid unexpected results of `+` operator",
      "F-String formatting not needed here",
      "Use f formatter,\r\nWhereever the string concat is done by + replace it with f formatter",
      "use \"f\" formatter instead of concat with +",
      "Replace all the c like string representations with \"F\" formatter where ever it applicable.",
      "In some places the f formatter is unnecessarily used without any additional strings. Even though those are not part of this PR just want to convey this to all so that slowly people should remove those unwanted \"f\" formetter.",
      "use python 'f' format string or %s",
      "Use \"f\" formatter for string concat to avoid unexpected outputs with \"+\"",
      "Use \"F\" formatter where ever string concats were done.",
      "can we use 'f' formatted string instead of %s ?",
      "There's no variables, you don't need to f-string.",
      "Use \"f\" formatter for string concats to avoid unexpected results if the string contains some other symbols",
      "Since you're reusing this in the f-string, you could refactor into variable assignment to be less brittle.",
      "Use \"f\" formatter for readability purposes.",
      "Use \"f\" formatter apply the same in other loggers too.",
      "Use \"f\" formatter to avoid unexpected outcomes.  apply the same where ever the \"+\" is used as concat.",
      "use \"f\" formatter and avoid \"+\" for concat operations.",
      "Use \"f\" formatter and avoid \"+\" for concat"
    ]
  },
  {
    "size": 18,
    "representative": "Already discussed in another PR comment https://github.com/Trimble-Cloud-Core-Platform/iam/pull/1856#discussion_r2250373541. Hence resolving this comment.",
    "top_paths": [
      "applications/src/service.py",
      "context/src/app.py",
      "accounts/cdh-migration/migrate_scripts/migrate/addresses/address_writer.py",
      "setup/infra/migration/data-synchronizer/template.yaml",
      "users/src/tid_sync_handler.py"
    ],
    "comments": [
      "branch that addresses this (IAM-1180) -> https://github.com/Trimble-Cloud-Core-Platform/iam/compare/dev/IAM-1142...dev/IAM-1180",
      "we need both source tables. refer this PR - https://github.com/Trimble-Cloud-Core-Platform/iam/pull/685",
      "This is handled in the consecutive [PR](https://github.com/Trimble-Cloud-Core-Platform/iam/pull/784/files#diff-2ac583da12e0dc08fdcd1b770c6cda888e0ef2966bb4a8cc24967c875b56857a)",
      "That change was reverted [here](https://github.com/Trimble-Cloud-Core-Platform/iam/pull/804) and merged.",
      "https://github.com/Trimble-Cloud-Core-Platform/iam/blob/main/tests/automation/utils/errors.py Please refer this and you can refactor using this in the next PR.",
      "It is covered in logging enhancement PR: https://github.com/Trimble-Cloud-Core-Platform/iam/pull/1273",
      "@DharmarajanRajendran Previously these error messages are kept in the error.py file. As per [Maria's comment i]( https://github.com/Trimble-Cloud-Core-Platform/iam/pull/1501#discussion_r2131616812)t is placed here as http_exceptions.",
      "Kinda thinking about https://github.com/Trimble-Cloud-Core-Platform/iam/blob/main/accounts/src/accounts/models/core/external_system/references.py#L173 as well.",
      "As per the [comment](https://github.com/Trimble-Cloud-Core-Platform/iam/pull/1540#discussion_r2144304102), the source system check has been removed for this use case",
      "Maria suggested renaming this from `add_resource_to_account` to `add_identity_to_account`.\r\nhttps://github.com/Trimble-Cloud-Core-Platform/iam/pull/1551#discussion_r2144511157",
      "https://github.com/Trimble-Cloud-Core-Platform/iam/pull/1674/files - I hope this is the PR",
      "@mariasekartrimble The PR for error code by @aravinth-karthikeyan is yet to merge, once it is merged will pull the latest and do the changes\r\n Reference: https://github.com/Trimble-Cloud-Core-Platform/iam/pull/1820",
      "Note: I did update the code in https://github.com/Trimble-Cloud-Core-Platform/iam/pull/1813\nBut merging this to main would break the current implementation",
      "This was already discussed in another PR comment https://github.com/Trimble-Cloud-Core-Platform/iam/pull/1856#discussion_r2250417230. Hence marking this as resolved.",
      "Already discussed in another PR comment https://github.com/Trimble-Cloud-Core-Platform/iam/pull/1856#discussion_r2250373541. Hence resolving this comment.",
      "@davemunson looks like duplicate PR for https://github.com/Trimble-Cloud-Core-Platform/iam/pull/2457/files",
      "https://github.com/Trimble-Cloud-Core-Platform/iam/pull/3079#discussion_r2832277212\r\nFollow discussion can be found here",
      "Will be handled as part of https://github.com/Trimble-Cloud-Core-Platform/iam/pull/3122"
    ]
  },
  {
    "size": 17,
    "representative": "https://jira.trimble.tools/browse/IAM-3017 - Logged a separate ticket for this check",
    "top_paths": [
      "config/staging-apis.yaml",
      "common/src/db/schema.py",
      "applications/src/app.py",
      "tests/automation/models/accounts.py",
      "tests/automation/utils/sourceSystems.py"
    ],
    "comments": [
      "https://jira.trimble.tools/browse/IAM-420 we will track this issue [bug using](https://jira.trimble.tools/browse/IAM-420) as this is a blocker for testing users",
      "Validations would be updated on the ticket https://jira.trimble.tools/browse/IAM-448\r\nKindly sync up with @r-dharanidharan on his changes as well. Thanks !",
      "As per the latest release in the spec, it is updated as PUT. @davemunson \r\n https://github.com/trimble-oss/iam/releases/tag/v0.0.3\r\ncc : @PrabuMonk",
      "has to be PATCH, looks like @manojtc-trimble has it fixed here.  I have an outstanding question to product before I approve [the PR](https://github.com/trimble-oss/iam/pull/99).",
      "I will address this issue in the upcoming ticket here: https://jira.trimble.tools/browse/IAM-1108",
      "This will be fixed here https://jira.trimble.tools/browse/IAM-1345",
      "https://jira.trimble.tools/browse/IAM-1908\r\nWe need this in the immediate release due to an urgent requirement from APICloud request.\r\nSo we will take it as an enhancement to the next release.",
      "This will be addressed in this ticket https://jira.trimble.tools/browse/IAM-1874",
      "@jfrench-trimble we have a separate ticket for qa2 https://jira.trimble.tools/browse/IAM-2543",
      "https://jira.trimble.tools/browse/IAM-2733 Logged this ticket for the same",
      "https://jira.trimble.tools/browse/IAM-3017 - Logged a separate ticket for this check",
      "Raised a PR for the same. https://github.com/trimble-oss/iam/pull/338",
      "Tracking that as separate ticket https://jira.trimble.tools/browse/IAM-3663",
      "Jira ticket ref - https://jira.trimble.tools/browse/IAM-4072",
      "This [Jira](https://jira.trimble.tools/browse/IAM-4474) ticket is created to track and address this.",
      "https://jira.trimble.tools/browse/IAM-6110 - Logged a ticket for tracking, Thanks Maria",
      "I created a ticket to address this issue. Please add any additional information to it. I don't have the context to fully understand what needs to be done. https://jira.trimble.tools/browse/IAM-4819"
    ]
  },
  {
    "size": 17,
    "representative": "Avoid magic number... I would use `HTTPStatus.OK` here.",
    "top_paths": [
      "roles/src/service.py",
      "organizations/src/service.py",
      "groups/src/app.py",
      "tests/automation/tests/conftest.py",
      "tests/automation/tests/sync/test_cdh_sync_account_and_address.py"
    ],
    "comments": [
      "HTTPStatus is preferred instead of hardcoded value 200",
      "I'd be fine with a follow-up PR or rebase, your choice, but these should be HTTPStatus.CREATED or what not.",
      "Would prefer to see HTTPStatus.CREATED, etc., but this whole file needs to be updated.  Probably out of scope for this PR.",
      "use HTTPStatus instead of 200",
      "use Httpstatus for defining status code",
      "Avoid magic number... I would use `HTTPStatus.OK` here.",
      "Use HTTPStatus instead of numbers to avoid magic numbers problem.  e.g. `HTTPStatus.CONFLICT` is more meaningful, and less cognitive load, than using `409`",
      "use HTTP Status for adding status code",
      "Kindly change all status code to respective HTTPStatus",
      "add HTTPStatus for status code",
      "You could replace these with HTTPStatus as well, but since there are several in this file, maybe consider doing in separate PR.",
      "use HTTPStatus instead of integer - all places",
      "Again, not a fan of `magic number`, even in tests.  Can we replace with HTTPStatus codes?\r\n\r\nI'm cool with follow-up PR since this has approvals already.",
      "can we use HTTPStatus ?",
      "use HTTPStatus.BAD_REQUEST instead of 400 and add this error message in error_messages file",
      "Don't use magic numbers.  Prefer a variable or constant instead.  Here, we should be using `HTTPStatus.NOT_FOUND`.  Please fix the other magic numbers below as well.",
      "Magic number, if this is HTTP code, can you use HTTPStatus.BAD_REQUEST or something appropriate instead?"
    ]
  },
  {
    "size": 17,
    "representative": "Potential None value access error. Similar to line 114, line 120 accesses `new_payload.get(\"account\")[\"id\"]` without verifying that \"account\" is not None. This could raise a TypeError if the \"account\" key is missing. Add appropriate None checks.",
    "top_paths": [
      "events/src/ingestions/consumers/central/user_handler.py",
      "roles/src/service.py",
      "tests/automation/tests/accounts/dealer_optin/test_dealer_optin.py",
      "roles/src/dal.py",
      "users/src/app.py"
    ],
    "comments": [
      "The `if \"permissionList\" in role_payload:` check is not necessary here & below is the reason.\r\nThe permissionList key is verified against the model named `Role`. This model mandates that permissionList should have at least one in it's list. So there should always exists one item in the list.\r\n![image](https://github.com/user-attachments/assets/81ea703b-afa1-442f-9608-c158b7013aa8)",
      "account id is not part of payload hence have added as separate params",
      "> I believe .get() method on dictionaries is specifically designed to not raise KeyError. KeyError may raise when direct dict access with square brackets like payload[\"type\"]. Here when the key is not found, .get() returns None which further evaluates to False.\r\n\r\nAhh, hrmm... this is correct.  My apologies.",
      "The check `if role_permission is not None` is redundant. The `RoleWithPermission(**role.get(\"payload\"))` constructor will either create an object or raise an exception; it will never return None. This condition should be removed.\n```suggestion\n                role_permission.trn = Trn(resource_type=\"role\", resource_id=role_permission.id).to_string()\n                role_permission.permissionList = self.dal.list_permission_by_role(role_permission.id)\n```",
      "Potential AttributeError if `user_app_relation` is `None`. The method `get_user_app_relation` can return `None` (as seen in the DAL), but the code attempts to access `.payload` without checking for `None` first. Add a null check before accessing the payload.\n```suggestion\n        user_app_relation: Optional[FullRelation] = self.dal.get_user_app_relation(user_id, application_id)\n        if user_app_relation is None:\n            raise_application_error(ErrorCode.NO_ROLES_BET_APP_USER, application_id, user_id)\n```",
      "Potential AttributeError if `user_app_relation` is `None`. Similar to the issue above, `.payload` is accessed without verifying `user_app_relation` is not `None`. This will cause a runtime error if the relation is not found.",
      "Line 198 has the same f-string quoting issue (`...response[\"id\"]...`) and will prevent the test module from parsing. Use single quotes around dict keys inside f-strings (e.g., `...response['id']...`).",
      "Potential None value access error. Similar to line 114, line 120 accesses `new_payload.get(\"account\")[\"id\"]` without verifying that \"account\" is not None. This could raise a TypeError if the \"account\" key is missing. Add appropriate None checks.",
      "Potential None value access error. On line 104, `new.payload.get(\"account\")` could return None if the \"account\" key is missing, which would cause a TypeError when attempting to access `[\"id\"]` on line 104. Add a None check or use a default empty dict to prevent this error.",
      "Potential None value access error. The code accesses `new_payload.get(\"account\")[\"id\"]` on line 114 without checking if \"account\" exists or is None. If the \"account\" key is missing from new_payload, this will raise a TypeError. Add proper None handling or use a default value.",
      "better assign the `new_payload.get(\"account\")[\"id\"]` either account or id into a variable and use it.\nAvoid repeated usage or access of same things,",
      "Potential KeyError when accessing account_detail[\"id\"]. If new_payload.get(\"account\") returns None, this will raise a KeyError. The function should validate that account_detail is not None before accessing its keys.",
      "Potential KeyError when accessing account_detail.get(\"status\", STATUS_COMMITTED) and account_detail[\"id\"]. If new_payload.get(\"account\") returns None, account_detail will be None and calling .get() or accessing [\"id\"] will raise an AttributeError or TypeError. Add validation to ensure account_detail is not None.",
      "Potential KeyError if new.payload.get(\"account\") returns None. The code calls new.payload.get(\"account\")[\"id\"] which will raise a KeyError if \"account\" is missing from new.payload. Consider adding a null check or using a default value.",
      "Accessing `user_app_relation.payload` before checking if `user_app_relation` is None will cause an AttributeError. The None check happens later at line 1287, but it's too late. Move the None check before accessing any attributes, or restructure the logic to handle the None case first.",
      "Same SyntaxError issue here: the f-string expression uses double quotes in `.get(\"id\", None)` inside another double-quoted f-string. Switch the dict key quotes to single quotes (or avoid nesting quotes) so this line parses.",
      "This assert message f-string also has unescaped double quotes inside the expression (`.get(\"id\", None)`), causing a SyntaxError. Use single quotes for the dict key (or build the message without nested quoting)."
    ]
  },
  {
    "size": 15,
    "representative": "I can change to constants.",
    "top_paths": [
      "devices/src/error_constants.py",
      "accounts/src/accounts/service/search.py",
      "iam-accounts/iam_api/accounts/authorization/config/consts.py",
      "common/src/common/models.py",
      "common/src/common/audit_logging/audit_logging_service.py"
    ],
    "comments": [
      "I can change to constants.",
      "Suggestion: Can we move this to constants file?",
      "Could you plz move this format to a constant so that it could be useful for others too.",
      "Could you plz move all those common errors to constant and use it where ever applicable.",
      "Can we use constant instead of hard coding ?",
      "Better to move this to a common constant",
      "use constants - all the places wherver applicable",
      "Plz move the fixed set of values to constant to avoid repeated initializations.",
      "Can we remove the error constants which are covered in error codes?",
      "I Checked, No Error constants are there in Error code so far.",
      "If there are no references to error constants then the file can be removed",
      "Can we use some sort of StrEnum? instead of hard coded string and can be useful for others too instead of relying the string literal. It could be changed at any time but constant variable wont. It will reduce the issues and maintainable.",
      "Move the fixed things to constants.",
      "I have added the variable to common constants and added description to it",
      "I think these values should be moved to some constants file"
    ]
  },
  {
    "size": 15,
    "representative": "iam_role_application_ids - What is application ID here? is this a iam_role_uuid?",
    "top_paths": [
      "applications/template.yaml",
      "tests/automation/configs/qatest_config.json",
      "setup/infra/sam/configuration/cdh-iam-users-roles-live-sync-env/cdh-iam-qa.json",
      "users/template.yaml",
      "setup/infra/sam/configuration/cdh-iam-accounts-live-sync-env/cdh-iam-qa.json"
    ],
    "comments": [
      "iam_role_application_ids - What is application ID here? is this a iam_role_uuid?",
      "this is also similar to above comment. Could you plz name it properly like \"iam_limited_role_id\" or based on the context.",
      "This owner id is for dev",
      "We need OWNER_ROLE_ID as well. Could you include?",
      "In QA, owner roleId is 02263b57-ed6b-442a-9bed-b33613e0aa3a",
      "In Dev, owner roleId is 6165dcb2-4325-4812-b6b3-d42f8df5c9e1",
      "do we need OWNER_ROLE_ID for this release?",
      "The `IamApplicationId` value is identical to `SkipInvitationAppIds` in this environment. Consider verifying if this is intentional or if these should have different values for proper functionality.",
      "The `IamApplicationId` value matches `SkipInvitationAppIds` in production. This pattern is consistent across environments, but should be verified that using the same ID for both purposes is the intended design.",
      "The `IamApplicationId` has a different value from `SkipInvitationAppIds` in this environment, unlike other environments where they match. Consider ensuring this difference is intentional and documented.\n```suggestion\n    \"IamApplicationId\": \"695d741b-2f4a-4cb8-8d97-e81fd4dbd765\"\n```",
      "The same IamApplicationId value '695d741b-2f4a-4cb8-8d97-e81fd4dbd765' is used across stage, qa, and dev environments. Consider using environment-specific application IDs to avoid potential conflicts or data mixing between environments.\n```suggestion\n    \"IamApplicationId\": \"b2a1c3e4-5d6f-4a7b-8c9d-0e1f2a3b4c5d\"\n```",
      "Production environment uses a different IamApplicationId value compared to other environments, which is good practice. However, verify that this production-specific ID is correctly configured in all production regions.",
      "The production configuration has an empty string for `iam_role_application_ids` while all other environments have UUID values. If this is intentional because production doesn't use role application IDs yet, consider documenting this or using a more explicit value like `null` or omitting the key entirely to make the intent clearer.\n```suggestion\n  \"iam/common_config/iam_role_application_ids\": null,\n```",
      "The column name `iamRoleId` uses camelCase, which is inconsistent with other column names in the same method like `account_id`, `user_id`, and `role_id` that use snake_case. Consider renaming to `iam_role_id` for consistency.",
      "The dev environment is using role IDs from the QA environment instead of dev-specific role IDs. Lines 16-19 contain the same UUIDs as the QA environment configuration (lines 18-19 in iam-cdh-qa.json and lines 16-17), whereas the other environments (stage, prod, qa) use environment-specific role IDs. This will cause incorrect role mappings in the dev environment.\n```suggestion\n    \"OwnerRoleId\": \"3c2b1a09-4d5e-4f70-9a8b-1c2d3e4f5a6b\",\n    \"EndUserRoleId\": \"7f8e9d0c-1b2a-4c3d-8e9f-0a1b2c3d4e5f\",\n    \"AdminRoleId\": \"a1b2c3d4-5e6f-4a7b-8c9d-0e1f2a3b4c5d\",\n    \"SecondaryOwnerRoleId\": \"d4c3b2a1-6f5e-4d3c-9b8a-7e6d5c4b3a2f\",\n```"
    ]
  },
  {
    "size": 15,
    "representative": "Missing word 'when' in the description. Should be 'This error occurs when user does not have permission to read devices'.\n```suggestion\n        \"This error occurs when user does not have permission to read devices\"\n```",
    "top_paths": [
      "devices/src/error_codes.py",
      "groups/src/error_codes.py"
    ],
    "comments": [
      "Grammatical error in the description. Should be 'This error occurs when trying to delete the group which is still in use'.\n```suggestion\n        \"This error occurs when trying to delete the group which is still in use\"\n```",
      "There's a grammatical error in the description. It should be 'an groupId' \u2192 'a groupId'.\n```suggestion\n         \"This error occurs when create device has a groupId that does not exist\"\n```",
      "Missing word 'when' in the description. Should be 'This error occurs when user does not have permission to create devices'.\n```suggestion\n        \"This error occurs when user does not have permission to create devices\"\n```",
      "Missing word 'when' in the description. Should be 'This error occurs when user does not have permission to update devices'.\n```suggestion\n        \"This error occurs when user does not have permission to update devices\"\n```",
      "Missing word 'when' in the description. Should be 'This error occurs when user does not have permission to read devices'.\n```suggestion\n        \"This error occurs when user does not have permission to read devices\"\n```",
      "Missing word 'when' in the description. Should be 'This error occurs when user does not have permission to delete devices'.\n```suggestion\n        \"This error occurs when user does not have permission to delete devices\"\n```",
      "Missing word 'when' in the description. Should be 'This error occurs when user does not have permission to transfer devices'.\n```suggestion\n        \"This error occurs when user does not have permission to transfer devices\"\n```",
      "Grammatical error in the description. Should be 'This error occurs when a transfer has already been initiated for device and is pending completion'.\n```suggestion\n        \"This error occurs when a transfer has already been initiated for device and is pending completion\"\n```",
      "Missing article in the description. Should be 'This error occurs during device transfer when user does not have permission to access the target account'.\n```suggestion\n        \"This error occurs during device transfer when user does not have permission to access the target account\"\n```",
      "Missing article 'the' before 'user'. Should be 'This error occurs when the user does not have permission to check device transfer status'.\n```suggestion\n        \"This error occurs when the user does not have permission to check device transfer status\"\n```",
      "Missing article 'the' before 'user'. Should be 'This error occurs when the user does not have permission to search devices'.\n```suggestion\n        \"This error occurs when the user does not have permission to search devices\"\n```",
      "Multiple issues: extra space and missing 'is'. Should be 'This error occurs when group is assigned to multiple accounts'.\n```suggestion\n        \"This error occurs when group is assigned to multiple accounts\"\n```",
      "Redundant 'during when' and missing article. Should be 'This error occurs when the user does not have permission to read account'.\n```suggestion\n        \"This error occurs when the user does not have permission to read account\"\n```",
      "Missing article 'the' before 'user'. Should be 'This error occurs when the user could not find account relation for the specified group'.\n```suggestion\n        \"This error occurs when the user could not find account relation for the specified group\"\n```",
      "The error description comment should be updated to reflect that both group and account TRNs are now valid, not just group TRNs.\n```suggestion\n        \"This error occurs when a TRN that is neither a group nor an account TRN is provided in query parameter\"\n```"
    ]
  },
  {
    "size": 15,
    "representative": "Remove commented-out code. If this test case is needed, it should be implemented properly; otherwise, it should be deleted to avoid code clutter.\n```suggestion\n\n```",
    "top_paths": [
      "tests/automation/tests/accounts/search_account/test_search_account_by_owner_email.py",
      "tests/automation/tests/accounts/dealer_optin/test_dealer_optin.py",
      "groups/src/service.py",
      ".github/workflows/configs_deployment.yaml",
      "tests/automation/tests/devices/test_device_sku_field.py"
    ],
    "comments": [
      "Remove commented-out code instead of leaving it in the codebase. This reduces code clutter and improves maintainability.\n```suggestion\n\n```",
      "Commented-out code should be removed rather than left in the workflow file. If this functionality is needed in the future, it can be restored from version control.\n```suggestion\n\n```",
      "Remove commented-out code block. Commented code should be removed rather than left in the codebase as it creates maintenance burden and confusion.\n```suggestion\n\n```",
      "Remove commented-out assertion. If this check is intentionally skipped, add a comment explaining why; otherwise, remove the line entirely.\n```suggestion\n\n```",
      "Remove commented-out code. If this test case is needed, it should be implemented properly; otherwise, it should be deleted to avoid code clutter.\n```suggestion\n\n```",
      "Remove commented-out code. If this code is not needed, it should be deleted rather than left as a comment.\n```suggestion\n\n```",
      "This commented-out code should be removed. If this logic may be needed in the future, consider documenting why it was removed in a commit message rather than leaving it as a comment in the code.\n```suggestion\n\n```",
      "Commented-out code should be removed rather than left in the codebase. If these environment variables are no longer needed, delete these lines instead of commenting them out.\n```suggestion\n\n```",
      "Commented-out code should be removed from tests. Since this test is marked as skipped, either remove the commented code or uncomment and update it when fixing the test.\n```suggestion\n\n```",
      "Commented-out code should be removed rather than left in the codebase. If this code is needed for future use, consider documenting why in a comment or tracking it in an issue.\n```suggestion\n\n```",
      "Commented-out code should be removed. If there's uncertainty about the expected behavior, document it in the test docstring or create a follow-up issue.\n```suggestion\n\n```",
      "Large blocks of commented-out code should be removed rather than left in the codebase. If this old implementation needs to be referenced, it can be found in version control history. Commented code clutters the codebase and creates confusion about which implementation is intended.\n```suggestion\n\n```",
      "Commented-out code should be removed. Version control preserves the history if this old implementation needs to be referenced later. Keeping commented code reduces code clarity.\n```suggestion\n\n```",
      "This code block is commented out with triple quotes. If this code is no longer needed, it should be removed entirely. If it's temporarily disabled for debugging, a TODO comment should explain why and when it should be re-enabled.\n```suggestion\n\n```",
      "Avoid leaving commented-out code in tests (the old organization name generation). It adds noise and makes it unclear which behavior is intended. Remove the commented line instead of keeping it alongside the active value.\n```suggestion\n\n```"
    ]
  },
  {
    "size": 15,
    "representative": "`users_data_builder.py` expects `SOURCE_BUCKET` to be a bucket name (no `s3://`) and `BUCKET_PATH`/`CENTRAL_TABLE`/`REGIONAL_TABLE` to be key prefixes. Passing `GlueScriptS3Path` (a full S3 URI) and values like `s3://accountaoss-data-...` will produce malformed paths such as `s3://s3://.../...` at runtime. Update these arguments (and/or introduce dedicated parameters) so the script receives the bucket name and plain prefixes it concatenates.\n```suggestion\n        \"--BUCKET_PATH\": \"\"\n        \"--REGION\": !Ref AWS::Region\n        \"--CENTRAL_TABLE\": iam/\n        \"--ENVIRONMENT\": !Ref environment\n        \"--REGIONAL_TABLE\": geo-code/\n        \"--SOURCE_BUCKET\": !Sub accountaoss-data-${environment}\n```",
    "top_paths": [
      "setup/infra/tools/glue-job/template.yaml",
      "setup/infra/tools/glue-job/README.md",
      ".github/workflows/release_common_sam_apply.yml",
      "scripts/opensearch/users-ingestion/user-data-builder/users_data_builder.py",
      "scripts/opensearch/users-ingestion/user-data-builder/readme.md"
    ],
    "comments": [
      "Missing comma after 'BUCKET_PATH' in the argument list. This will cause a syntax error when getResolvedOptions is called.\n```suggestion\n            \"BUCKET_PATH\",\n```",
      "The example path '/iam/test' starts with a slash, but S3 prefixes typically should not have leading slashes. Consider updating the example to 'iam/test' to follow S3 best practices.\n```suggestion\n- `BUCKET_PATH` - S3 path prefix for the output data. (eg: iam/test)\n```",
      "Missing required argument 'SOURCE_BUCKET' in getResolvedOptions. The script tries to access args[\"SOURCE_BUCKET\"] on line 14 and other source bucket/path arguments that were not declared in the getResolvedOptions call on line 11. This will cause a KeyError at runtime.\n```suggestion\nargs = getResolvedOptions(\n    sys.argv,\n    [\"JOB_NAME\", \"ENVIRONMENT\", \"SOURCE_BUCKET\", \"CENTRAL_DATA_PATH\"],\n)\n```",
      "Missing required arguments in getResolvedOptions. The script accesses args[\"SOURCE_BUCKET\"], args[\"REGIONAL_DATA_PATH\"], and args[\"CENTRAL_DATA_PATH\"] on lines 17-19 but these are not declared in the getResolvedOptions call on line 12. This will cause KeyError at runtime.\n```suggestion\nargs = getResolvedOptions(\n    sys.argv,\n    [\"JOB_NAME\", \"ENVIRONMENT\", \"SOURCE_BUCKET\", \"REGIONAL_DATA_PATH\", \"CENTRAL_DATA_PATH\"],\n)\n```",
      "The destination S3 location is effectively hard-coded to `s3://{SOURCE_BUCKET}/address-export/{ENVIRONMENT}/`. This doesn\u2019t align with the PR description of configuring S3 paths via job parameters and makes it easy to accidentally overwrite prior exports. Consider taking destination bucket and/or destination prefix/path as explicit Glue job parameters (and optionally include a dataset/version component in the path).",
      "The Glue job `DefaultArguments` don\u2019t line up with the Glue script that is uploaded in the workflow (`generate_searchable_accounts_dataset.py`): it resolves options named `ENVIRONMENT`, `GEOCODE_DATA_PATH`, and `CENTRAL_DATA_PATH`, but the template passes `--env` and uses `GEO_CODE_DATA_PATH`, and the central/geocode keys are missing the required `--` prefix. As written, `getResolvedOptions()` will fail at startup and the job won\u2019t run. Update the argument names/keys to match the script\u2019s expected option names (including `--` prefixes).\n```suggestion\n        \"--ENVIRONMENT\": !Ref environment\n        \"--JOB_NAME\": !Sub iam-${environment}-aoss-address-ingestor-glue-job\n        \"--SOURCE_BUCKET\": !Ref GlueScriptS3Path\n        \"--REGION\": !Ref AWS::Region\n        \"--REGIONAL_DATA_PATH\": !Sub s3://accountaoss-data-${environment}/iam/\n        \"--CENTRAL_DATA_PATH\": !Sub s3://accountaoss-data-central/iam/\n        \"--GEOCODE_DATA_PATH\": !Sub s3://accountaoss-data-central/geo-code/\n```",
      "`--SOURCE_BUCKET` is being set to `GlueScriptS3Path` (a full `s3://.../file.py` URL). The uploaded Glue script uses `SOURCE_BUCKET` as an S3 *bucket name* (e.g., for `dynamodb.s3.bucket` and building `s3://{SOURCE_BUCKET}/...`), so passing the script path will produce invalid S3 locations. Add a dedicated bucket-name parameter (or otherwise supply just the bucket) and pass that to `--SOURCE_BUCKET`.",
      "The README describes the job as ingesting directly into OpenSearch and lists runtime args `--env`/`--product`, but the deployment workflow uploads `generate_searchable_accounts_dataset.py`, which generates a searchable dataset to S3 and expects different Glue arguments (e.g., `--ENVIRONMENT`, `--CENTRAL_DATA_PATH`, `--GEOCODE_DATA_PATH`). Please align the README with the actual script/template behavior and use the correct environment naming (the repo uses `stage`, not `staging`).",
      "`--REGIONAL_DATA_PATH` / `CENTRAL_DATA_PATH` / `...GEOCODE...` are currently set to full `s3://bucket/prefix/` values. The Glue script that\u2019s uploaded by the workflow treats these as *prefix/path components* and concatenates them with `SOURCE_BUCKET` (and also uses them as DynamoDB export `dynamodb.s3.prefix`), so providing full S3 URIs will yield malformed paths (e.g., `s3://<bucket>/s3://...`). Pass prefixes (no `s3://` and no bucket) or adjust the script/template consistently so the value types match.\n```suggestion\n        \"--REGIONAL_DATA_PATH\": iam/\n        \"CENTRAL_DATA_PATH\": iam/\n        \"GEO_CODE_DATA_PATH\": geo-code/\n```",
      "The workflow uploads `scripts/opensearch/ingestions/generate_searchable_accounts_dataset.py` to `s3://iam-<stack>-ci-cd/aoss-address-ingestor-glue/` without renaming it, but the environment config files set `GlueScriptS3Path` to `.../glu_app.py`. That means the deployed Glue job will point at a key that doesn\u2019t exist. Either upload the script using the expected key name (e.g., copy to `.../glu_app.py`) or update the config/template to reference the actual uploaded object key.\n```suggestion\n          aws s3 cp scripts/opensearch/ingestions/generate_searchable_accounts_dataset.py s3://iam-${{ inputs.STACK }}-ci-cd/aoss-address-ingestor-glue/glu_app.py\n```",
      "The workflow uploads `generate_searchable_accounts_dataset.py` to the S3 prefix, but the environment configs set `GlueScriptS3Path` to `.../glu_app.py`. As written, the deployed Glue job will likely point at a key that was never uploaded. Either upload to the exact key referenced by `GlueScriptS3Path` (rename on copy) or update the configs to reference the uploaded filename.",
      "The README states the Glue job indexes documents into OpenSearch and includes retry logic for indexing, but the Glue script being deployed in this workflow (`generate_searchable_accounts_dataset.py`) generates a dataset and writes it to S3; OpenSearch ingestion appears to be handled by a separate script. Updating the README to reflect the actual Glue job responsibilities will avoid operational confusion.",
      "Parameter description has a typo: `script f(for example ...)` reads incorrectly and should be corrected for clarity.\n```suggestion\n    Description: S3 path to the User index data builder Glue ETL script (for example s3://bucket/path/glu_app.py)\n```",
      "`UserDataBuilderScriptS3Path` is a new required CloudFormation parameter with no default. The stage/prod SAM deploy uses `configuration/${STACK}/${STACK}.json`, which currently doesn't provide this parameter, so deployments for `glue-job` will fail until those configs are updated (or a safe default is provided).\n```suggestion\n    Description: S3 path to the User index data builder Glue ETL script (for example s3://bucket/path/glu_app.py)\n    Default: ''\n```",
      "`users_data_builder.py` expects `SOURCE_BUCKET` to be a bucket name (no `s3://`) and `BUCKET_PATH`/`CENTRAL_TABLE`/`REGIONAL_TABLE` to be key prefixes. Passing `GlueScriptS3Path` (a full S3 URI) and values like `s3://accountaoss-data-...` will produce malformed paths such as `s3://s3://.../...` at runtime. Update these arguments (and/or introduce dedicated parameters) so the script receives the bucket name and plain prefixes it concatenates.\n```suggestion\n        \"--BUCKET_PATH\": \"\"\n        \"--REGION\": !Ref AWS::Region\n        \"--CENTRAL_TABLE\": iam/\n        \"--ENVIRONMENT\": !Ref environment\n        \"--REGIONAL_TABLE\": geo-code/\n        \"--SOURCE_BUCKET\": !Sub accountaoss-data-${environment}\n```"
    ]
  },
  {
    "size": 14,
    "representative": "use the existing function for dal.list_relations",
    "top_paths": [
      "users/src/dal.py",
      "users/src/service.py",
      "groups/src/service.py",
      "roles/src/dal.py",
      "accounts/src/accounts/dynamodb/dal.py"
    ],
    "comments": [
      "@r-harivarthini  list_groups_by_user has handled NotFoundException in the dal layer. Can you kindly look?",
      "unfortunately its typed incorrectly in the DAL. list_relations takes an optional q parameter that is typed only as str, not as str | None",
      "if the relation isn't found, the dal throws an exception that we catch and return None in its place. so there isn't a case where this would be an empty list",
      "common dal 'list_resources' is throwing NotFoundException if there are no resources in the given context. This needs to be handled either in users dal or service layer",
      "lets try if we can use list_resources in groups dal",
      "Hmm, could have used wrapper of native dal's list_resources instead\r\nit'd return full relation and full entity",
      "we could combine all dal.find_relation into 1 single function. the only change is 'source_ref'. we could pass that as a parameter.",
      "same approach? can we have 1 common function for dal.list_relations ?",
      "use the existing list_resources function",
      "There is an existing function which does dal.list_resources. reuse that. rename the existing function if needed",
      "use the existing function for dal.list_resources",
      "use the existing function for dal.list_relations",
      "There are several list_relations in users dal. can you go thru all and remove duplicated functions?",
      "Is dal.list_relations() return empty [] if no relations exist?. If not plz add the condition to avoid none issue with the next line."
    ]
  },
  {
    "size": 14,
    "representative": "`InternalApiGateway` is set to `6supm9y030`, but the same env/region\u2019s other configs (e.g. `accounts-env.json`) use `6vypiyzbwb`. This mismatch will generate the wrong Lambda permission SourceArn and prevent API Gateway from invoking the function. Please align `InternalApiGateway` with the shared gateway id for qa2/eu-gb/eu-west-2.\n```suggestion\n    \"InternalApiGateway\": \"6vypiyzbwb\",\n    \"AnalyticsRoleArn\" : \"arn:aws:iam::387675059062:role/ProfilesDevIAM-IAMAnalyticsRole-si7sJNfhxPU\",\n```",
    "top_paths": [
      "setup/infra/sam/configuration/dev/us/us-west-2/accounts-env.json",
      "setup/infra/sam/configuration/qatest/us/us-west-2/auditLog-env.json",
      "setup/infra/sam/configuration/qa2/eu-gb/eu-west-2/auditLog-env.json",
      "setup/infra/sam/configuration/stage/us-west-2/applications-env.json",
      "tests/automation/configs/qatest_config.json"
    ],
    "comments": [
      "@mariasekartrimble either the RoleArn Account number or commonLayerArn, one of them could be wrong?\r\n\r\n\"RoleArn\": \"arn:aws:iam::751019381194\r\narn:aws:lambda:us-west-2:387675059062",
      "@nonoratoDEV, This is a default role added between a user and account relation in IAM. This role cannot be queried via API call. The value of this role will be set in the aws parameter store for each environment. \r\nWe can use this role in our feature testing. Hence added.",
      "Hard-coded role ID 'c72026c2-d92e-4444-9e46-f940d0f076fa' and account TRN 'trn:2:iam:us:account:3df90809-2849-5bff-a7d7-2f46895ab0ac' should be extracted to constants to improve maintainability and reduce duplication.",
      "The SecondaryOwnerRoleId value differs from the value used in accounts-env.json (5fac689e-ce34-4a8d-91c8-e0bdfcac6e92). These should likely be the same role ID across services for consistency.\n```suggestion\n    \"SecondaryOwnerRoleId\": \"5fac689e-ce34-4a8d-91c8-e0bdfcac6e92\",\n```",
      "The InternalApiGateway ID 'z8woxugi40' is duplicated in both dev/us/us-west-2 and dev4/us/us-west-2 environments. Each environment should have a unique API Gateway ID.\n```suggestion\n    \"InternalApiGateway\": \"a1b2c3d4e5\"  // <-- replaced with a unique ID\n```",
      "@bharath93-byte Need to replace with pi22nlvrw7 both accounts & users blu .\r\n\r\nInternalApiGateway  Dev4 stack is not used , still below change is needed to resolve copilot valid comment .\r\n\r\nsetup/infra/sam/configuration//dev4/us/us-west-2/users-env.json:    \"InternalApiGateway\": \"z8woxugi40\",\r\nsetup/infra/sam/configuration//dev4/us/us-west-2/applications-env.json:    \"InternalApiGateway\": \"pi22nlvrw7\",\r\nsetup/infra/sam/configuration//dev4/us/us-west-2/roles-env.json:    \"InternalApiGateway\": \"pi22nlvrw7\",\r\nsetup/infra/sam/configuration//dev4/us/us-west-2/groups-env.json:    \"InternalApiGateway\": \"pi22nlvrw7\",\r\nsetup/infra/sam/configuration//dev4/us/us-west-2/devices-env.json:    \"InternalApiGateway\": \"pi22nlvrw7\",",
      "`AnalyticsRoleArn` is pointing to an IAM API Lambda role ARN, not the environment\u2019s analytics role. In the same env/region, `events-env.json` uses `...:role/ProfilesDevIAM-IAMAnalyticsRole-...`. Using the wrong role here will likely break permissions for Firehose writes/audit logging. Update this to the correct Analytics role ARN.\n```suggestion\n    \"AnalyticsRoleArn\": \"arn:aws:iam::387675059062:role/ProfilesDevIAM-IAMAnalyticsRole-un8ia5vQ2tFV\"\n```",
      "`InternalApiGateway` value here doesn\u2019t match the same environment/region\u2019s other service configs (e.g. `accounts-env.json` uses `na1t5q3hw1`). This will produce an incorrect Lambda permission SourceArn and break API Gateway invocation. Update `InternalApiGateway` to the shared gateway id for qatest/us-west-2.",
      "`InternalApiGateway` is set to `4sra12y2r2`, but the same env/region\u2019s other configs (e.g. `accounts-env.json`) use `820shn85e3`. This mismatch will generate the wrong Lambda permission SourceArn and prevent API Gateway from invoking the function. Please align `InternalApiGateway` with the shared gateway id for dev/eu-gb/eu-west-2.\n```suggestion\n    \"InternalApiGateway\": \"820shn85e3\"\n```",
      "`InternalApiGateway` is set to `6supm9y030`, but the same env/region\u2019s other configs (e.g. `accounts-env.json`) use `6vypiyzbwb`. This mismatch will generate the wrong Lambda permission SourceArn and prevent API Gateway from invoking the function. Please align `InternalApiGateway` with the shared gateway id for qa2/eu-gb/eu-west-2.\n```suggestion\n    \"InternalApiGateway\": \"6vypiyzbwb\",\n    \"AnalyticsRoleArn\" : \"arn:aws:iam::387675059062:role/ProfilesDevIAM-IAMAnalyticsRole-si7sJNfhxPU\",\n```",
      "`InternalApiGateway` looks like a typo: `qatest/us/us-west-2/applications-env.json` uses `na1t5q3hw1` (includes an `h`), but this file uses `na1t5q3w1`. With the wrong ID, the API Gateway -> Lambda invoke permission won\u2019t match. Fix the gateway ID to the stack\u2019s actual Internal API Gateway.",
      "In UAT us-west-2, this `AnalyticsRoleArn` differs from `setup/infra/sam/configuration/uat/us/us-west-2/events-env.json`\u2019s `AnalyticsRoleArn`. Since both stacks appear to use the same `InternalApiGateway` and account, this is likely an incorrect ARN (and may not exist / may not have the right permissions). Please align this role ARN with the environment\u2019s actual analytics role output.",
      "`InternalApiGateway` doesn\u2019t match the QA2 EU-GB internal gateway used by the rest of the stack (see `qa2/eu-gb/eu-west-2/events-env.json`, which uses `6vypiyzbwb`). If this stays mismatched, API Gateway won\u2019t have invoke permission on the Lambda for `POST /auditLogs`. Align this ID with the stack\u2019s Internal API Gateway.",
      "`InternalApiGateway` was updated here, but the other `dev/eu-gb/eu-west-2/*-env.json` files still reference the previous gateway id. If the internal gateway REST API id changed, stacks for accounts/groups/roles/etc will have Lambda permissions pointing at the wrong API Gateway and internal GW calls to those services will fail. Update the rest of the env configs to the same `InternalApiGateway` (or document why users is intentionally different)."
    ]
  },
  {
    "size": 14,
    "representative": "I fix the assertion to test for pagination",
    "top_paths": [
      "tests/automation/tests/graphql/test_gql_pagination_accounts_addresses_users.py",
      "users/tests/graphql/test_users_appsync_handler.py",
      "accounts/src/accounts/dynamodb/account_repository/account_repository_impl.py",
      "users/src/service.py",
      "users/tests/test_users_app.py"
    ],
    "comments": [
      "cursors may not work properly if we do further filtering of the db results. for example, if page size 10 is requested, we will get 10 records from dal. but if you do additional application filter, you may get less than 10 records in response. Also next page may not return data (bcz of the application filter) though it shows some records are still available.",
      "The test does not verify the response or assert any behavior when pagination parameters are valid. Add assertions to verify that `handle_account_users` returns the expected result and that mocked functions were called with correct arguments.",
      "Because the results are mocked, only 1 result is every returned which is always less than or equal to the pagination value when the pagination value is valid. We would need to create a different test that returns more data to test if the pagination limits behave as expected.",
      "I fix the assertion of this function to actually test pagination",
      "Both functios were addjust to check on pagination",
      "I fix the assertion to test for pagination",
      "I change it so it may be a better test for pagination",
      "The test uses string values for cursor fields ('1', '2', '0'), but does not verify that the pagination links contain these actual cursor values in the query parameters. Consider asserting that the href values contain the expected cursor values (e.g., 'pageIndex=1', 'pageIndex=2') to ensure the cursor values are properly propagated to the links.",
      "Both `_handle_full_reference_search` and `_handle_search_by_ref_id` now ignore pagination input (`page_request` / `page_request_from_client`) and always return `Cursors()` with no `next` cursor. If this endpoint is consumed as a paginated API, this is a behavior change: callers providing `page_size`/`cursor` will no longer have them respected or validated. Consider applying `page_size` limits (and producing a deterministic cursor) or explicitly rejecting non-null cursors for these lookups to preserve/clarify API behavior.",
      "After the refactor, `_handle_full_reference_search` no longer calls DAL pagination APIs, so `InvalidCursorException` appears unreachable here. Either add explicit cursor validation (e.g., reject non-empty `page_request.cursor`) or remove this try/except block to avoid dead error handling that can hide real failures.",
      "Choosing the cursor parameter purely based on whether `page_index` was present in the *incoming query params* can generate incorrect links if `pageIndex` is provided but the response `cursors` are actually cursor tokens (or `pageIndex` was invalid and ignored downstream). Prefer deciding link style from the validated pagination request used by the handler/service (e.g., `page_request.page_index is not None`) or from the cursor payload type, rather than from the raw request query string.",
      "The block is replaced with a single assertion. If the group is not found in Account.groups pagination, the test now fails with the existing assertion message.",
      "Does the suite need \"Pagination\"? I see the other two tests, Address and User, and they don't have it, or maybe the others are the ones missing \"Pagination\"?",
      "The test are Ok, but I think there's already a file that is about testing pagination so I would recomend moving these there instead of making another file."
    ]
  },
  {
    "size": 13,
    "representative": "NIT:\n```\nstatus: str | None = None\n```",
    "top_paths": [
      "accounts/src/accounts/models/core/account/account_relations.py",
      "accounts/src/accounts/models/api/account.py",
      "tests/automation/base/generate_token.py",
      "accounts/src/accounts/models/core/account/address/address.py",
      "users/src/cdh_sync/app.py"
    ],
    "comments": [
      "Nit: remove extraneous blank lines in methods.",
      "Nit: don't really need to specify `str` here",
      "Nit: explicitly check for None value",
      "The union operator (ex: `str | None`) was introduced in python 3.10 in [PEP604](https://peps.python.org/pep-0604/) and is just the more modern way to define an optional. It doesn't require an import and is generally considered more readable.",
      "Use new union operator for  Optional from python3.10+ so\r\n\r\n```\r\nrelation: FullRelation | None = None,\r\nexternal_systems: dict[Any, Any] | None = None,\r\n```",
      "nit: method name can be better, this function doesn't filter out empty values alone. It also skips values with False and None",
      "`str | None` is preferable in python 3.10+",
      "NIT: too many empty lines",
      "NIT\n```\nstatus: str | None = None\n```",
      "NIT:\n```\nstatus: str | None = None\n```",
      "Use `None | str` instead of Optional",
      "Sorry looks like this would be `None | dict[str, dict]` but basically use the Union `|` operator instead of the Optional class",
      "str | None is the preferred way to represent an optional for newer versions of python"
    ]
  },
  {
    "size": 13,
    "representative": "do we still need this function?",
    "top_paths": [
      "users/src/service.py",
      "common/src/db/iam_database.py",
      "accounts/test/models/core/test_meta.py",
      "devices/src/device_certificate_dal.py",
      "common/src/db/dal_models.py"
    ],
    "comments": [
      "What is this? Is this needed?",
      "Do we really need this method?",
      "This should be passed from the respective calling function. do not keep it as global variable.",
      "this should be passed from calling function and should not be in global scope",
      "This function may not be needed ?",
      "can we remove this function ?",
      "i might just be missing it, but is this needed anymore?",
      "is this needed anymore too?",
      "do we still need this function?",
      "Are we still using this function ?",
      "it is declared outside of the function",
      "the variable is assigned with an value in the scope of this function",
      "The scope of this variable is bound to this method, so added here temporarily"
    ]
  },
  {
    "size": 13,
    "representative": "Similar to the previous issue, this assertion accesses `response.json()['relation']` at the top level, but it's inside a conditional checking a specific item. Should be `item['relation'].get('status', None)`.\n```suggestion\n                    assert_equals(item[\"relation\"].get(\"status\", None), \"PENDING\")\n```",
    "top_paths": [
      "groups/src/service.py",
      "tests/automation/tests/applications/application_consent_management/test_subscribe_application_to_account.py",
      "accounts/src/accounts/dynamodb/account_repository/relation_repository/relation_repository_impl.py",
      "tests/automation/tests/accounts/dealer_optin/test_dealer_optin.py",
      "users/src/invitation_webhook.py"
    ],
    "comments": [
      "[nitpick] The payload dictionary should be constructed on a separate line for better readability and maintainability, especially since it spans multiple lines with nested structure.\n```suggestion\n            account_payload = {\n                \"status\": status,\n                \"roleIds\": [account_guest_user_role]\n            }\n            user_dal.add_user_to_account(subject, source_id, account_id, user_account_relation_name, account_payload)\n```",
      "The roleName field is being populated with role_ids instead of role_names. This should use new.payload.get('role_names', []) to match the role_names extracted earlier in the function.\n```suggestion\n            \"roleName\": \",\".join(new.payload.get(\"role_names\", [])),\n```",
      "The repeated dictionary get operations with duplicate checks create unnecessary code duplication. Consider extracting the payload dictionary once and using the walrus operator or intermediate variables to simplify the logic. For example, store `account_account_relation.payload` in a variable, then check for 'relationData' once before accessing nested keys.\n```suggestion\n                payload = account_account_relation.payload or {}\n                relation_data = payload.get(\"relationData\") or None\n                role_key = f\"roleId:{ACCOUNT_MANAGER_ROLE_ID}\"\n                role_info = relation_data.get(role_key) if isinstance(relation_data, dict) else None\n                opt_in_status = role_info.get(\"status\") if isinstance(role_info, dict) else None\n                opt_in_roles = payload.get(\"roleIds\") or None\n```",
      "This code will raise an AttributeError if `relation_data` is None or if the first `.get()` returns None, since you cannot call `.get()` on None. The conditional check happens after the first `.get()` is already executed. Use safer chaining like `relation_data.get(f'roleId:{ACCOUNT_MANAGER_ROLE_ID}', {}).get('status')` or check `relation_data` first before accessing nested keys.\n```suggestion\n                relation_data = account_account_relation.payload.get(\"relationData\") or {}\n                role_data = relation_data.get(f\"roleId:{ACCOUNT_MANAGER_ROLE_ID}\") or {}\n                opt_in_status = role_data.get(\"status\")\n```",
      "The conditional check uses `item.get('accountId')` but the assertion accesses `response.json()['relation']` which is at the top level. The assertion should likely be `item['relation'].get('status', None)` to match the scope of the conditional.\n```suggestion\n                assert_equals(item[\"relation\"].get(\"status\", None), \"COMMITTED\")\n```",
      "Similar to the previous issue, this assertion accesses `response.json()['relation']` at the top level, but it's inside a conditional checking a specific item. Should be `item['relation'].get('status', None)`.\n```suggestion\n                    assert_equals(item[\"relation\"].get(\"status\", None), \"PENDING\")\n```",
      "`filter_roles_by_application(...)` can merge existing roles (e.g., shared/other-app roles) with the incoming list and you persist that merged list into `relation.payload[\"roleIds\"]`. But the response model sets `roleIds=role_ids` (the request input), which can diverge from what was actually stored. Return the updated/stored roleIds (e.g., from `relation.payload`) so the PUT response accurately reflects the group relation state.\n```suggestion\n            roleIds=relation.payload.get(\"roleIds\", []),\n```",
      "There is a mismatch between the test title (\"when there is no request\") and the expectation `len(response.json()['items']) == 1`. Given this test is meant to validate the empty state, either the expectation should be 0 or the title/fixture setup should be updated to reflect that a request already exists.\n```suggestion\n        assert_equals(actual=len(response.json()[\"items\"]), expected=0)\n```",
      "In `_build_groups`, `db_role_ids = item[1].payload.get('roleIds')` can be `None` (other places in this file use `.get('roleIds', [])`). Calling `list(db_role_ids)` will raise a `TypeError` when the relation has no `roleIds`. Use a default empty list (or guard for falsy) before converting/filtering.\n```suggestion\n                    db_role_ids = item[1].payload.get(\"roleIds\") or []\n```",
      "`filter_application_roles` assumes every dict returned by `get_multiple_entities` contains a `payload` dict (`role.get('payload').get('applicationId')`). `fetch_entities_in_bulk` only includes keys that exist, so `payload` may be missing; this would raise an `AttributeError`. Use `role.get('payload', {})` and also skip/guard `None` owner applicationIds so `list_shared_roles_from_app_to_app` isn\u2019t called with `None`.\n```suggestion\n                payload = role.get(\"payload\", {}) or {}\n                owner_app_id = payload.get(\"applicationId\")\n                if owner_app_id is not None:\n                    role_owner_app.add(owner_app_id)\n```",
      "This block asserts `response_json[\"type\"]` twice; the final assertion likely intended to validate a different field (commonly `detail`). As written, it doesn't validate the error message/body beyond type/title/status and may miss regressions.\n```suggestion\n            response_json[\"detail\"],\n            ApplicationErrors.UNAUTHORIZED_TO_ADD_APPLICATION_TO_ACCOUNT.detail,\n```",
      "Same issue here: `response_json[\"type\"]` is asserted twice, so the response body isn't fully validated (e.g., `detail`). Consider asserting the appropriate field instead of repeating the type check.\n```suggestion\n            response_json[\"detail\"],\n            ApplicationErrors.UNAUTHORIZED_TO_ADD_APPLICATION_TO_ACCOUNT.detail,\n```",
      "This assertion re-checks `response_json[\"type\"]` against `ALREADY_SUBSCRIBED.with_variables(...).type`. `with_variables()` only formats the `detail` (not the `type/title/status`), so this is redundant and also fails to assert the expected formatted `detail` message for the conflict case.\n```suggestion\n            response_json[\"detail\"],\n            ApplicationErrors.ALREADY_SUBSCRIBED.with_variables(\n                app_id=test_application[\"id\"], account_id=test_account_id\n            ).detail,\n```"
    ]
  },
  {
    "size": 13,
    "representative": "Test provides roleIds=['role-x'] but the test name and assertion expect no roles (empty list). The test setup should use roleIds=[] to match the test's intent of validating behavior when no roles are provided.\n```suggestion\n    req_payload = GroupJoinRequest(roleIds=[], emailTemplates=emailTemplate,\n```",
    "top_paths": [
      "tests/automation/tests/groups/test_groups_user_role_update_workflow.py",
      "groups/tests/test_service.py",
      "reverse_sync/iam_to_cdh_sync_user_roles/test/account_user_controller/test_update.py",
      "roles/tests/test_service.py",
      "tests/automation/tests/sync/test_iam_cdh_roles_sync.py"
    ],
    "comments": [
      "Test data inconsistency. The role is created with `permissionList=[permission]` here, but earlier in the diff the role was created with an empty permission list. This change may cause the test to pass for the wrong reasons if it's not actually testing the intended behavior of fetching permissions separately.\n```suggestion\n    role = RoleWithPermission(id=role_id, name=role_name, permissionList=[])\n```",
      "The else clause is incorrectly attached to the if statement inside the for loop. This will raise an AssertionError on the first user that doesn't match user_id, rather than after checking all users. Move the else clause outside the for loop or use a flag-based approach similar to the pattern used elsewhere in this test.\n```suggestion\n            user_found = False\n            for user in users_data[\"items\"]:\n                if user[\"id\"] == user_id:\n                    user_found = True\n                    assert (\n                        new_role_id in user[\"roleIds\"]\n                    ), f\"Expected IAM role id {new_role_id} in user roles {user['roleIds']}\"\n                    break\n            assert user_found, f\"User {user_id} not found in IAM account users {users_data['items']}\"\n```",
      "Function parameters lack type hints. Add type annotations (e.g., `dealer_account: str, customer_account: str, action: str, authorization_resource_type: str`) for consistency with other functions in this file like `check_role_permissions` and `get_subject_accounts`.\n```suggestion\ndef check_dealer_customer_permission_helper(\n    dealer_account: str,\n    customer_account: str,\n    action: str,\n    authorization_resource_type: str,\n) -> bool:\n```",
      "Test function name has a typo - \"test_verify_dealer_opt_invalid_opt_in_role\" is missing \"in\" after \"opt\". Should be \"test_verify_dealer_opt_in_invalid_opt_in_role\" to match the pattern of other test names.\n```suggestion\n    def test_verify_dealer_opt_in_invalid_opt_in_role(self, owner_token, create_dealer_account_and_add_owner,\n                                                      create_customer_account_and_add_owner):\n```",
      "Test assertion changed from checking that 'roleIds' is NOT in the payload to checking that it IS in the payload. This contradicts the test name 'test_join_group_no_roles_applies_pending_only' and the test setup which passes an empty roleIds list. The test expects that when no roles are provided, roleIds should not be in the created payload. This change breaks the test's original intent.",
      "Test provides roleIds=['role-x'] but the test name and assertion expect no roles (empty list). The test setup should use roleIds=[] to match the test's intent of validating behavior when no roles are provided.\n```suggestion\n    req_payload = GroupJoinRequest(roleIds=[], emailTemplates=emailTemplate,\n```",
      "Bug: Incorrect variable used in assertion. This code is verifying application roles, but it's comparing against `self.user_under_test[\"id\"]` instead of `self.app_details[\"id\"]`. This will cause the test to fail because it's looking for a user ID in the applications list instead of the application ID.\n```suggestion\n                if app[\"id\"] == self.app_details[\"id\"]:\n```",
      "Naming inconsistency: The path parameter uses 'invalid-user-id' but this is a test for invalid application ID. This should be 'invalid-app-id' or 'invalid-application-id' for clarity and consistency with the test's purpose.\n```suggestion\n                profileId=f\"{self.group_details[\"id\"]}/applications/invalid-app-id\",\n                payload=update_app_role_payload\n            )\n            assert_equals(update_app_role_response.status_code, HTTPStatus.NOT_FOUND)\n            assert_equals(update_app_role_response.json().get(\"detail\", None),\n                          \"Application not found for id: invalid-app-id\")\n```",
      "Naming inconsistency: Function name uses 'invalid_user_id' but the test is for invalid group ID in the context of application role update. The function should be named something like 'test_update_role_of_app_in_group_with_invalid_group_id' to accurately reflect what it's testing.\n```suggestion\n    def test_update_role_of_app_in_group_with_invalid_group_id(self, owner_token, get_roles):\n```",
      "Bug: Incorrect endpoint path for application role update test. The test is for updating an application's role with an invalid group ID, but the path uses '/users/' instead of '/applications/'. This should be `f\"invalid-group-id/applications/{self.app_details['id']}\"` to properly test the application role update endpoint.\n```suggestion\n                profileId=f\"invalid-group-id/applications/{self.app_details['id']}\",\n```",
      "The test should verify the return value is correct. Consider adding an assertion like `assert handler.create(input, Mock()) == []` before the existing assertions to verify the handler returns an empty list when no valid role IDs are found, consistent with the pattern in test_create_not_committed_status (line 121).",
      "The test should verify the return value is correct. Consider adding an assertion like `assert handler.update(input, Mock()) == []` before the existing assertions to verify the handler returns an empty list when no valid role changes are found, consistent with the pattern in test_update_not_committed_status (line 205).\n```suggestion\n    result = handler.update(input, Mock())\n    assert result == []\n```",
      "The test should verify that `role_mapper.get_cdh_roles` was called to ensure the handler properly attempts to map roles before determining there are no valid CDH roles. Consider adding an assertion like `role_mapper.get_cdh_roles.assert_called()` to verify this behavior.\n```suggestion\n    handler.update(input, Mock())\n    role_mapper.get_cdh_roles.assert_called()\n```"
    ]
  },
  {
    "size": 12,
    "representative": "Can we make the dynamoDB call as Batch get and put? I suspect a risk of throttling here",
    "top_paths": [
      "common/src/db/iam_database.py",
      "scripts/user_data_fix/users_data_fix.py",
      "accounts/src/accounts/dynamodb/account_writer.py",
      "accounts/src/accounts/dynamodb/account_user_writer.py",
      "setup/infra/migration/data-synchronizer/README.MD"
    ],
    "comments": [
      "aren't these two writes going to the same place? I see wanting to keep them separate logically but I think it is worthwhile to do in one network hop. and if they're going to make it two tables, we could still do [batch_write_item](https://boto3.amazonaws.com/v1/documentation/api/latest/reference/services/dynamodb/client/batch_write_item.html#)",
      "Is that something we want to do with these? I have no idea. It seems like a rabbit hole we could go down pretty quick for all database interactions. If we create the account but fail to create one of the relations. Then do we need to role back the account creation. The problem is that there is no way to do these operations atomically in dynamoDB so we will always have a chance of a partial transaction.",
      "Hmm, rethinking this\r\nfirst = an import approach\r\nincr = direct seed to dynamodb\r\n\r\nso may be i'll change it to `import` and `seed` instead of `first` and `incr`",
      "I can't think of any reason not too do this. And I don't think it will break compatibility but I haven't worked a lot with dynamoDB transactional writes.",
      "Sorry, why dont we go with dynamodb serialization methods for these instead of manual serialization or else you can reuse the serialization logic which is available in commons too.",
      "Can we make the dynamoDB call as Batch get and put? I suspect a risk of throttling here",
      "We will not be able to do update_item() as a batch in dynamo, So I have added a configurable rate limiting strategy to avoid throttling.",
      "[nitpick] The nested `.get()` calls with hardcoded 'S' key suggest this is expecting DynamoDB attribute value format. Consider adding a comment explaining the expected structure or extracting this logic into a helper function for better readability.",
      "Batch operations as well we can make it as transactionallly . Which is supported by Dynamo. plz check their docs . If such functions not avilalble in our db layer plz add it.",
      "Creating a copy of the batch with `batch[:]` is unnecessary since `write_batch_to_dynamodb_with_adaptive_retry` only reads from the batch. Remove the slice operation to avoid creating duplicate data in memory.\n```suggestion\n                                table, batch, current_ddb_delay\n```",
      "The batch_size of 20 contradicts the comment on line 330 which states 'up to 100 (DynamoDB transact limit)'. The comment should be updated to reflect the actual batch size of 20, or the batch size should be increased to a value closer to the DynamoDB limit if performance is a concern.\n```suggestion\n        # Process deletions in batches of up to 20 (see batch_size below)\n```",
      "The transaction limit check uses exact equality (== 100), which could allow exceeding DynamoDB's 100-item limit. Since 2 items are added per application, if transact_items has 99 items and 2 more are added, the list will have 101 items but won't trigger execution (101 != 100). This will cause a runtime error when transact_write_items is called. Change the condition to 'if len(transact_items) >= 100:' or better yet, check before adding items to ensure the batch never exceeds 100 items."
    ]
  },
  {
    "size": 12,
    "representative": "we may not need this validation. the existing should work",
    "top_paths": [
      "users/src/models.py",
      "setup/infra/migration/data-synchronizer/layers/transformers.py",
      "applications/src/main.py",
      "users/src/tid_sync_handler.py",
      "accounts/src/accounts/models/api/address.py"
    ],
    "comments": [
      "This will handled while adding the validations in other parts if any by @dharanitharan-selvaraj",
      "All these validation can be done in the model validator itself",
      "typically the core models are responsible for validation.",
      "Don't the core models already have an Email model with validation? Why cant we use that here?",
      "This validation could be part of the model",
      "i think the existing template validation should be ok. why we need to change this?",
      "we may not need this validation. the existing should work",
      "use the existing templates validator",
      "Existing validator mandates both the template, but this case requires validation only for existing template so modified the validator to handle both cases.",
      "@ragulkarthi Why we have two different validations. Both should be validated right?",
      "@ragulkarthi this validation is not required",
      "Could you explain why this validation is required?"
    ]
  },
  {
    "size": 12,
    "representative": "The resource path references 'stage' environment but this is the qa1 configuration file. The index resource should be 'index/iam-aoss-qa1-uw2/*' to match the qa1 environment.\n```suggestion\n          \"index/iam-aoss-qa1-uw2/*\"\n```",
    "top_paths": [
      "setup/infra/terraform_iac/applayer/configuration/templates/passthrough-swagger-yaml/iam-passthrough-api-uw2-prod-dr.yaml",
      "setup/infra/terraform_iac/data/configuration/stage/us/us-east-1/stage.tfvars",
      "setup/infra/terraform_iac/data/configuration/prod/us/us-east-1/prod.tfvars",
      "setup/infra/terraform_iac/data/configuration/qatest/us/us-west-2/qatest.tfvars",
      "setup/infra/terraform_iac/data/configuration/qa2/us/us-west-2/qa2.tfvars"
    ],
    "comments": [
      "@Sweatha1609 it suppose to be same base path like we have for prod , also we can use the same file for uw2 & ue1 region .",
      "The resource path references 'uw2' (us-west-2) but this is the us-east-1 configuration file. The index resource should likely be 'index/iam-aoss-stage-ue1/*' to match the us-east-1 region.\n```suggestion\n          \"index/iam-aoss-stage-ue1/*\"\n```",
      "The resource path references 'uw2' (us-west-2) but this is the us-east-1 configuration file. The index resource should likely be 'index/iam-aoss-prod-ue1/*' to match both the prod environment and us-east-1 region.\n```suggestion\n          \"index/iam-aoss-prod-ue1/*\"\n```",
      "The resource path references 'stage' environment but this is the qatest configuration file. The index resource should be 'index/iam-aoss-qatest-uw2/*' to match the qatest environment.\n```suggestion\n          \"index/iam-aoss-qatest-uw2/*\"\n```",
      "The resource path references 'stage' environment but this is the qa2 configuration file. The index resource should be 'index/iam-aoss-qa2-uw2/*' to match the qa2 environment.\n```suggestion\n          \"index/iam-aoss-qa2-uw2/*\"\n```",
      "The resource path references 'stage' environment but this is the qa1 configuration file. The index resource should be 'index/iam-aoss-qa1-uw2/*' to match the qa1 environment.\n```suggestion\n          \"index/iam-aoss-qa1-uw2/*\"\n```",
      "The resource path references 'stage' environment but this is the qa configuration file. The index resource should be 'index/iam-aoss-qa-uw2/*' to match the qa environment.\n```suggestion\n          \"index/iam-aoss-qa-uw2/*\"\n```",
      "The resource path references 'stage' environment but this is the pt configuration file. The index resource should be 'index/iam-aoss-pt-uw2/*' to match the pt environment.\n```suggestion\n          \"index/iam-aoss-pt-uw2/*\"\n```",
      "The resource path references 'stage' environment but this is the prod configuration file. The index resource should be 'index/iam-aoss-prod-uw2/*' to match the prod environment.\n```suggestion\n          \"index/iam-aoss-prod-uw2/*\"\n```",
      "The resource path references 'stage' environment but this is the prestage configuration file. The index resource should be 'index/iam-aoss-prestage-uw2/*' to match the prestage environment.\n```suggestion\n          \"index/iam-aoss-prestage-uw2/*\"\n```",
      "The resource path references 'stage' environment but this is the dev configuration file. The index resource should be 'index/iam-aoss-dev-uw2/*' to match the dev environment.\n```suggestion\n          \"index/iam-aoss-dev-uw2/*\"\n```",
      "The index resource references 'iam-aoss-dev-eu-gb' but the collection name on line 40 is 'iam-aoss-dev-eu'. The resource should be 'index/iam-aoss-dev-eu/*' to match the collection name.\n```suggestion\n          \"index/iam-aoss-dev-eu/*\"\n```"
    ]
  },
  {
    "size": 12,
    "representative": "This added `roleIds` value uses an f-string with nested double quotes (`f\"{get_roles[\"endUserRoleId\"]}\"`), which is a Python syntax error and will break test collection. Use single quotes for the key or pass `get_roles['endUserRoleId']` directly without an f-string.",
    "top_paths": [
      "tests/automation/tests/usecase/test_iam_5967.py",
      "groups/src/dal.py",
      "roles/src/service.py",
      "tests/automation/tests/users/NormalUsers/test_account_ownership_transfer_custom_template.py",
      "tests/automation/tests/users/NormalUsers/test_normalusers_update_via_invitation_endpoint.py"
    ],
    "comments": [
      "better to have like this as payload is a dictionary and not a class. roleIds is not a class property\r\nuser_account_relation.payload[\"roleIds\"] = roles",
      "same here. use payload[\"roleIds\"] = roles",
      "Better assign the role_id into a variable and use it",
      "My bad wrongly conveyed it,\n`f\"roleId:{role_id}\"` assign this string into a variable and use it.",
      "The f-string on line 135 is a Python syntax error due to unescaped nested double-quotes (`f\"{get_roles[\"ownerRoleId\"]}\"`). Use single quotes for dict keys (e.g., `get_roles['ownerRoleId']`) or avoid the f-string entirely if the value is already a string.",
      "This file won\u2019t parse: the f-string expression uses double quotes inside double quotes (`f\"{get_sharedRoles[\"endUserRoleId\"]}\"`), which causes a SyntaxError. Use single quotes for the dict key (or avoid the f-string entirely) and apply the same fix to the other repeated `roleIds` lines in this file.\n```suggestion\n                \"roleIds\": [str(get_sharedRoles[\"endUserRoleId\"])]\n```",
      "In this fixture, the `roleIds` f-string uses nested double quotes (`f\"{get_roles[\"adminRoleId\"]}\"`), which is a syntax error and will prevent the file from importing. Use single quotes for the dict key (or skip the f-string and use the value directly).",
      "This added payload uses `f\"{get_sharedRoles[\"endUserRoleId\"]}\"`, which is a syntax error due to nested double quotes inside the f-string and will break test collection. Use single quotes for the dict key (or pass `get_sharedRoles['endUserRoleId']` directly).",
      "This added `roleIds` list uses an f-string with nested double quotes (`f\"{get_roles[\"ownerRoleId\"]}\"`), which is invalid Python syntax and will prevent the test file from importing. Use single quotes for the dict key (or pass `get_roles['ownerRoleId']` directly).",
      "This newly-added f-string contains nested double quotes (`f\"{get_roles[\"adminRoleId\"]}\"`), which will cause a syntax error and prevent the test module from importing. Use single quotes for the dict key (e.g., `get_roles['adminRoleId']`) or avoid the f-string entirely and pass the value directly.",
      "This added `roleIds` value uses an f-string with nested double quotes (`f\"{get_roles[\"endUserRoleId\"]}\"`), which is a Python syntax error and will break test collection. Use single quotes for the key or pass `get_roles['endUserRoleId']` directly without an f-string.",
      "This f-string is syntactically invalid because it uses double quotes inside a double-quoted f-string expression (it will raise a SyntaxError and the test file won\u2019t import). Use single quotes for the dict key (or assign the value to a variable first) so the string can be parsed. Also, the same quoting issue appears earlier in this file (e.g., around lines 153/187/219 with get_sharedRoles[\"endUserRoleId\"]).\n```suggestion\n                      f\"Updated-{self.__class__.user_one_full_details.get('lastName', None)}\")\n```"
    ]
  },
  {
    "size": 12,
    "representative": "[nitpick] Inconsistent naming: groups uses 'error_messages.py' while devices uses 'error_constants.py'. Consider standardizing the filename pattern across modules for better maintainability.\n```suggestion\ndevices_constants_path = os.path.join(devices_src, 'error_messages.py')\n```",
    "top_paths": [
      "scripts/error_page_generator/readme_compiler.py",
      "groups/src/error.py",
      "users/src/http_exceptions.py",
      "groups/src/service.py",
      "accounts/src/accounts/service/list.py"
    ],
    "comments": [
      "Rename this file as \"http_messages\" and place all the common errors / messages. Because in couple of places the module name will conflict with \"except\" block exception object name as well and it will affect existing code.",
      "file error.py might be confusing whether to hold the error classes.\r\nCan we Rename this file to error_messages or an equivalent ?",
      "These are not exceptions. They are just error messages. can we rename this file to \"error_messages.py\"\r\nAlso there is another file error.py which also has error messages. can we combine all error messages into one single file ?",
      "Maria also suggested the same. We need to consolidate all error messages into one file. the file name can be error_messages.py or http_messages.py",
      "Add all error messages to error_messages.py",
      "The path is using `roles_src` instead of `groups_src` when loading groups error codes. This should be `groups_src` to correctly reference the groups directory.\n```suggestion\ngroups_error_codes_path = os.path.join(groups_src, 'error_codes.py')\n```",
      "The path is using `groups_src` instead of `devices_src` when loading devices constants. This should be `devices_src` to correctly reference the devices directory.\n```suggestion\ndevices_constants_path = os.path.join(devices_src, 'error_constants.py')\n```",
      "There's a typo in the directory path. 'deviecs' should be 'devices'.\n```suggestion\ndevs_src = os.path.abspath(os.path.join(os.path.dirname(__file__), '../../devices/src'))\n```",
      "There is a typo in the directory path. 'deviecs' should be 'devices'.\n```suggestion\ndevs_src = os.path.abspath(os.path.join(os.path.dirname(__file__), \"../../devices/src\"))\n```",
      "I think we want to be sure to also use the details string in the error_codes.py so that it is maintained in one place.",
      "[nitpick] Inconsistent naming: groups uses 'error_messages.py' while devices uses 'error_constants.py'. Consider standardizing the filename pattern across modules for better maintainability.\n```suggestion\ndevices_constants_path = os.path.join(devices_src, 'error_messages.py')\n```",
      "The filename should be 'error_constants.py' not 'error_constants' to be consistent with the naming pattern used in the other entries (organizations/error_messages.py, users/error_codes.py)."
    ]
  },
  {
    "size": 12,
    "representative": "Missing space after comma in the parameter list. Should be 'detail=detail, error_code=error_code.code'.\n```suggestion\n    raise ApplicationError(status=error_code.status, detail=detail, error_code=error_code.code)\n```",
    "top_paths": [
      "groups/src/error_codes.py",
      "devices/src/models.py",
      "common/src/common/models.py",
      "tests/automation/tests/users/NormalUsers/test_user_invitation_resend_workflow.py",
      "devices/tests/test_device_service.py"
    ],
    "comments": [
      "[nitpick] Missing space after comma before 'error_code' parameter. Should be 'detail, error_code=error_code.code' for consistent formatting.\n```suggestion\n    raise ApplicationError(status=error_code.status, detail=detail, error_code=error_code.code)\n```",
      "Missing space after comma in the ApplicationError constructor parameters. Should be `error_code=error_code.code` with a space after the comma.",
      "Missing space after comma in the parameter list. Should be 'detail=detail, error_code=error_code.code'.\n```suggestion\n    raise ApplicationError(status=error_code.status, detail=detail, error_code=error_code.code)\n```",
      "Missing space after comma in format string. Should be ', '.join(missing) to ensure proper spacing in the error message.",
      "Missing space after comma in format string. Should be ', '.join(empty) to ensure proper spacing in the error message.",
      "Extra space before comma. Should be 'ApplicationError,' without the space.",
      "Missing space after comma in function calls. Should be `CommonLayerError(400, \\\"country code is required.\\\")` and `CommonLayerError(400,` should have a space after the comma for consistency.\n```suggestion\n        raise CommonLayerError(400, \"country code is required.\")\n```",
      "Missing space after comma in function calls. Should be `CommonLayerError(400, \\\"country code is required.\\\")` and `CommonLayerError(400,` should have a space after the comma for consistency.",
      "Missing space after comma in parameter list. Should be `'code, title, key, value, expected_status, error_message'` for consistency with Python style conventions.\n```suggestion\n        \"code, title, key, value, expected_status, error_message\",\n```",
      "Missing space after comma in parameter list. Should be `'code, title, key, value, expected_status, error_message'` for consistency with Python style conventions.",
      "Missing space after comma in function call arguments. Should be `ErrorCode.USER_ALREADY_EXISTS_IN_GIVEN_GROUP, subject.sub` for consistency with Python style conventions.\n```suggestion\n                    raise_application_error(ErrorCode.USER_ALREADY_EXISTS_IN_GIVEN_GROUP, subject.sub)\n```",
      "Missing space after comma in function arguments. While this is valid Python, it's inconsistent with the rest of the codebase where spaces are used after commas in function calls (e.g., line 532, 575, 585, 591).\n```suggestion\n            params={\"on\": getTrn(\"account\", default_account_id)},\n```"
    ]
  },
  {
    "size": 12,
    "representative": "Extra space before the equals sign. Should be `status=\"PENDING\"` to follow PEP 8 style guidelines.\n```suggestion\n            status=\"PENDING\",\n```",
    "top_paths": [
      "roles/tests/test_service.py",
      "groups/tests/test_service.py",
      "tests/performance/k6/scripts/accounts.js",
      "accounts/src/accounts/service/get.py",
      "users/src/service.py"
    ],
    "comments": [
      "There's inconsistent spacing around the `=` operator on line 114. It should be `request_client=request_client` without extra spaces to maintain consistent code formatting.\n```suggestion\n                                                                                 request_client=request_client)\n```",
      "Similar to the previous case, replacing the explicit `status == STATUS_FAILED` check with a catch-all `else` reduces code clarity. Consider explicitly checking for both `STATUS_FAILED` and `STATUS_EXPIRED` statuses.",
      "[nitpick] The nested parentheses and inconsistent indentation reduce readability. Consider splitting into intermediate variables, e.g., build PageRequest separately and remove the outer wrapping parentheses.\n```suggestion\n    page_request = PageRequest(\n        page_size=first,\n        cursor=decode_optional_string(after)\n    )\n    account_page = get_account_service_handler().searcher.search_by_owner_user(\n        user_email=email,\n        account_type=None,\n        page_request=page_request,\n        client=client\n    )\n```",
      "Spacing around the equals sign is inconsistent with Python conventions. Should be `type=\\\"singleTierHierarchical\\\"` (no spaces around `=` in keyword arguments).\n```suggestion\n            type=\"singleTierHierarchical\"\n```",
      "Missing space after equals sign. Should be `resource1 = FullEntity(` to follow PEP 8 style guidelines.\n```suggestion\n    resource1 = FullEntity(\n```",
      "Missing space after equals sign. Should be `resource2 = FullEntity(` to follow PEP 8 style guidelines.\n```suggestion\n    resource2 = FullEntity(\n```",
      "The `status` variable on line 848 is declared but never used. Consider removing it or using it to track check results if needed for later logic.\n```suggestion\n        check(response, {\n```",
      "The `status` variable is declared but never used. Consider removing it or using it to track check results if needed for later logic.",
      "Inconsistent spacing around the equals sign in keyword argument. Line 61 has \"page_request = PageRequest(100)\" with spaces around the equals sign, which violates PEP 8 style guidelines for keyword arguments. It should be \"page_request=PageRequest(100)\" without spaces.\n```suggestion\n        page_request=PageRequest(100),\n```",
      "Extra space before the equals sign. Should be `status=\"COMMITTED\"` to follow PEP 8 style guidelines.\n```suggestion\n            status=\"COMMITTED\",\n```",
      "Extra space before the equals sign. Should be `status=\"PENDING\"` to follow PEP 8 style guidelines.\n```suggestion\n            status=\"PENDING\",\n```",
      "Extra space before the equals sign. Should be `status=\"PENDING\"` to follow PEP 8 style guidelines."
    ]
  },
  {
    "size": 11,
    "representative": "@samoconnell  Thanks for the feedback. To clarify, the source system check has already been removed for this specific use case, so there\u2019s no direct coupling to the accounts BLU\u2019s internal storage in this case.",
    "top_paths": [
      "applications/src/service.py",
      "applications/libs/python/requirements.txt",
      "common/pyproject.toml",
      "accounts/src/accounts/models/api/account.py",
      "live_sync/iam_to_cdh_sync/pytest.ini"
    ],
    "comments": [
      "may need a discussion for including common libraries to all BLU's dependency list.",
      "I have removed those libraries as it will be a part of other BLUs",
      "The BLU is under accounts folder only. Then again why we need to have another `accounts` folder within src? it will sometimes create confusion and import issues.",
      "Why do we need the accounts BLU?",
      "building on what @jfrench-trimble has mentioned, i dont think its a good idea to couple the applications BLU to some internal storage detail of the accounts BLU. i think its worth following up on this ticket with some way for the accounts blu to expose a public interface for getting a source system. just as an example, we have a bug in accounts where search by external system is taking too long and we're thinking of changing how the source system is stored. that would introduce a bug here because of the coupling",
      "@samoconnell  Thanks for the feedback. To clarify, the source system check has already been removed for this specific use case, so there\u2019s no direct coupling to the accounts BLU\u2019s internal storage in this case.",
      "@samoconnell if the storage attributes are going to be changed or any other new format is going to be introduced as part of performance improvements in accounts BLU, then plz let us know so that we can also be aware of this change and do a similar change if required. As mentioned, we are just checking the presence of the external system in the account. So later, if this logic alone can be modified based on the accounts BLU. I guess @ElammathiM moved the logic to service interface.",
      "it is necessary to include this dependency in the individual BLUs if it is included in common and the dependency is only used there?",
      "This is due to an inconsistency between user and account BLU",
      "accounts BLU has to use this as well, else there is no point in bring this here and on changes this'd be skewed from the reference within accounts",
      "It didnt need to but I figured it would make sense to have all of the blu data sources separate from the auth data source. I can change it back."
    ]
  },
  {
    "size": 11,
    "representative": "Please change the title as per the test.",
    "top_paths": [
      "tests/automation/tests/groups/test_group_owner_workflow_using_apptoken.py",
      "tests/automation/tests/accounts/update_address/update_address_param_test.py",
      "tests/automation/tests/graphql/test_gql_pagination_accounts_addresses_users.py",
      "tests/automation/tests/accounts/list_accounts/list_accounts_param_test.py",
      "tests/automation/tests/usecase/test_iam_1518.py"
    ],
    "comments": [
      "You may want to Include the parameters in the allure title to consider them as separate test cases.",
      "Dynamic title will not work when the test fails in fixture itself. Hence I would recommend using the allure.title itself.",
      "This also applies to other test titles as well.",
      "Please change the title as per the test.",
      "Please change the title, both title looks same.",
      "Need to change the title.",
      "Same. Need to change the title.",
      "There are two @allure.title decorators on the same test method. The second one will override the first, making the title on line 426 ineffective. Remove the first @allure.title decorator or merge them into a single, more generic title.",
      "Not related to the import, but considering the following change on the test function the allure title might need to change",
      "Can you add allure title for all the tests?",
      "Allure titles were add to all tests"
    ]
  },
  {
    "size": 11,
    "representative": "The code attempts to remove DEFAULT_ROLE from existing_roles without checking if it exists first. This could raise a ValueError if DEFAULT_ROLE is not in the list.",
    "top_paths": [
      "users/src/service.py",
      "roles/src/service.py",
      "groups/src/service.py",
      "setup/infra/migration/data-synchronizer/stream_handler/mutations.py",
      "tests/automation/tests/groups/test_cross_account_group_invitation_workflow.py"
    ],
    "comments": [
      "just `if role_ids` make sense to check both none and empty things",
      "role_ids variable type is always list ? do we need to check that using isinstance ?",
      "What if the role_ids empty list? and why we have to store the empty things in DB.\r\nIf that is not the case plz use `if role_ids:` alone.",
      "Observation: this can be just [role_id] as the if condition checks for a single role_id is present in the roles list or not. Could be take up in upcoming changes.",
      "The code attempts to remove DEFAULT_ROLE from existing_roles without checking if it exists first. This could raise a ValueError if DEFAULT_ROLE is not in the list.",
      "The conditional check for DEFAULT_ROLE should be added before attempting to remove it. The current code will raise a ValueError if DEFAULT_ROLE is not in the list.",
      "This condition check will always fail because `existing_roles` contains role IDs as strings but `DEFAULT_ROLE` might not be found after the role validation changes. The code should handle the case where `DEFAULT_ROLE` needs to be removed from a list that may not contain it.\n```suggestion\n        existing_roles = [role for role in existing_roles if role != DEFAULT_ROLE]\n```",
      "This code will raise a ValueError if DEFAULT_ROLE is not in existing_roles. The condition check is redundant since you're checking if DEFAULT_ROLE exists before trying to remove it, but the remove() method will still fail if the role doesn't exist after the check due to potential race conditions or logic errors.\n```suggestion\n        try:\n            existing_roles.remove(DEFAULT_ROLE)\n        except ValueError:\n            pass\n```",
      "[nitpick] The empty role IDs list validation appears in multiple places. Consider extracting this into a helper function or using a constant to improve maintainability and make the expected behavior clearer.",
      "The validation logic is incorrect. This validator raises an error when roleIds is an empty list or not a list, but the field is marked as Optional with a default of `[]`. This will reject valid empty lists. The validation should only check if a non-empty list contains invalid values, or remove the validation entirely since empty lists are allowed.\n```suggestion\n        if roleIds is not None and not isinstance(roleIds, list):\n            raise_application_error(ErrorCode.ROLE_ID_TYPE_INVALID)\n```",
      "This logic only appends the role_id if role_ids is non-empty. If role_ids is an empty list, the role_id will not be added. The condition should be `if role_id not in role_ids:` to handle both empty and non-empty lists correctly.\n```suggestion\n            if role_id not in role_ids:\n```"
    ]
  },
  {
    "size": 11,
    "representative": "The AWS account ID '751019381194' is hardcoded throughout the queries. Consider using a variable like '${var.aws_account_id}' to make the dashboard more maintainable and reusable across different environments.\n```suggestion\n                \"query\":\"sum:aws.aoss.ingestion_request_errors{aws_account:${var.aws_account_id},$region} by {collectionname}.as_count()\"\n```",
    "top_paths": [
      "setup/infra/terraform_iac/dd-monitors/sre_golden_metrics_dashboard.tf"
    ],
    "comments": [
      "The AWS account ID '751019381194' is hardcoded throughout the queries. Consider using a variable like '${var.aws_account_id}' to make the dashboard more maintainable and reusable across different environments.\n```suggestion\n                \"query\":\"sum:aws.aoss.search_ocu{aws_account:${var.aws_account_id},$region} by {aws_account,region}.as_count()\"\n```",
      "The AWS account ID '751019381194' is hardcoded throughout the queries. Consider using a variable like '${var.aws_account_id}' to make the dashboard more maintainable and reusable across different environments.\n```suggestion\n                \"query\":\"sum:aws.aoss.indexing_ocu{aws_account:${var.aws_account_id},$region} by {aws_account,region}.as_count()\"\n```",
      "The AWS account ID '751019381194' is hardcoded throughout the queries. Consider using a variable like '${var.aws_account_id}' to make the dashboard more maintainable and reusable across different environments.\n```suggestion\n                \"query\":\"sum:aws.aoss.ingestion_request_errors{aws_account:${var.aws_account_id},$region} by {collectionname}.as_count()\"\n```",
      "The AWS account ID '751019381194' is hardcoded throughout the queries. Consider using a variable like '${var.aws_account_id}' to make the dashboard more maintainable and reusable across different environments.\n```suggestion\n                \"query\":\"sum:aws.aoss.search_request_errors{aws_account:${var.aws_account_id},$region} by {collectionname}.as_count()\"\n```",
      "The AWS account ID '751019381194' is hardcoded throughout the queries. Consider using a variable like '${var.aws_account_id}' to make the dashboard more maintainable and reusable across different environments.\n```suggestion\n                \"query\":\"sum:aws.aoss.ingestion_data_rate{aws_account:${var.aws_account_id}, $region} by {collectionname}\"\n```",
      "The AWS account ID '751019381194' is hardcoded throughout the queries. Consider using a variable like '${var.aws_account_id}' to make the dashboard more maintainable and reusable across different environments.",
      "The AWS account ID '751019381194' is hardcoded throughout the queries. Consider using a variable like '${var.aws_account_id}' to make the dashboard more maintainable and reusable across different environments.\n```suggestion\n                \"query\":\"sum:aws.aoss.ingestion_request_rate{aws_account:${var.aws_account_id}, $region} by {collectionname}.as_count()\"\n```",
      "The AWS account ID '751019381194' is hardcoded throughout the queries. Consider using a variable like '${var.aws_account_id}' to make the dashboard more maintainable and reusable across different environments.\n```suggestion\n                \"query\":\"sum:aws.sqs.approximate_number_of_messages_visible{(aws_account:${var.aws_account_id} and $region) and (queuename:${var.project}-regional-publish-events-dlq-v2-${var.stack}-uw2 or queuename:${var.project}-central-publish-events-dlq-v2-${var.stack}-uw2 or queuename:${var.project}-account-source-system-dlq-v2-${var.stack}-uw2 or queuename:${var.project}-email-to-user-mapping-retry-queue-v2-${var.stack}-uw2 or queuename:${var.project}-user-accounts-membership-dlq-v2-${var.stack}-uw2 or queuename:${var.project}-organization-regional-publish-events-dlq-v2-${var.stack}-uw2 )} by {queuename}.as_count()\"\n```",
      "The AWS account ID '751019381194' is hardcoded throughout the queries. Consider using a variable like '${var.aws_account_id}' to make the dashboard more maintainable and reusable across different environments.\n```suggestion\n                \"query\":\"sum:aws.lambda.errors{(aws_account:${var.aws_account_id} and $region) and (functionname:${var.project}-regional-events-handler-v2-${var.stack}-uw2 or functionname:${var.project}-central-events-handler-v2-${var.stack}-uw2 or functionname:${var.project}-analytics-email-user-mapping-v2-${var.stack}-uw2 or functionname:${var.project}-tid-sync-handler-v2-${var.stack}-uw2 or functionname:${var.project}-account-source-system-handler-v2-${var.stack}-uw2 or functionname:${var.project}-user-accounts-membership-handler-v2-${var.stack}-uw2 or functionname:${var.project}-organization-regional-events-handler-v2-${var.stack}-uw2 )} by {functionname}.as_count()\"\n```",
      "The AWS account ID '751019381194' is hardcoded throughout the queries. Consider using a variable like '${var.aws_account_id}' to make the dashboard more maintainable and reusable across different environments.\n```suggestion\n                \"query\":\"sum:aws.lambda.invocations{aws_account:${var.aws_account_id} ,$region ,functionname:${var.project}-aoss-*} by {functionname}.as_count()\"\n```",
      "The AWS account ID '751019381194' is hardcoded throughout the queries. Consider using a variable like '${var.aws_account_id}' to make the dashboard more maintainable and reusable across different environments.\n```suggestion\n                \"query\":\"sum:aws.lambda.errors{aws_account:${var.aws_account_id} ,$region, functionname:${var.project}-aoss-*} by {functionname}.as_count()\"\n```"
    ]
  },
  {
    "size": 11,
    "representative": "The OpenSearch host endpoint region (us-west-2) does not match the file path region (us-east-1). According to the PR description, collections should be updated in us-east-1 region.\n```suggestion\n    \"OpensearchHost\": \"9thwebilu05p9ptq145k.us-east-1.aoss.amazonaws.com\",\n```",
    "top_paths": [
      "setup/infra/sam/configuration/qa/eu/eu-west-1/events-env.json",
      "setup/infra/sam/configuration/integ/us/us-west-2/organizations-env.json",
      "setup/infra/sam/configuration/qa/us/us-east-1/organizations-env.json",
      "setup/infra/sam/configuration/qa/us/us-east-1/events-env.json",
      "setup/infra/sam/configuration/qa/us/us-east-1/accounts-env.json"
    ],
    "comments": [
      "The OpenSearch host endpoint appears to be missing a leading character. The value \"815zm40nf044zex9un5.us-west-2.aoss.amazonaws.com\" starts with a digit followed immediately by alphanumeric characters, which is inconsistent with other endpoints in this PR that follow patterns like \"2qduvu1m...\" or \"sjj9f9vc...\". This may be a typo.\n```suggestion\n    \"OpensearchHost\": \"a815zm40nf044zex9un5.us-west-2.aoss.amazonaws.com\",\n```",
      "The OpenSearch host endpoint region (us-west-2) does not match the file path region (us-east-1). According to the PR description, collections should be updated in us-east-1 region.\n```suggestion\n    \"OpensearchHost\": \"REPLACE_WITH_USEAST1_OPENSEARCH_ENDPOINT.aoss.amazonaws.com\",\n```",
      "The OpenSearch host endpoint region (us-west-2) does not match the file path region (us-east-1). According to the PR description, collections should be updated in us-east-1 region.\n```suggestion\n  \"OpensearchHost\":  \"9thwebilu05p9ptq145k.us-east-1.aoss.amazonaws.com\",\n```",
      "The OpenSearch host endpoint region (us-west-2) does not match the file path region (us-east-1). According to the PR description, collections should be updated in us-east-1 region.\n```suggestion\n    \"OpensearchHost\": \"9thwebilu05p9ptq145k.us-east-1.aoss.amazonaws.com\",\n```",
      "The OpenSearch host endpoint region (us-west-2) does not match the file path region (us-east-1). According to the PR description, collections should be updated in us-east-1 region.\n```suggestion\n    \"OpensearchHost\":  \"vqi7lzpgcwrmc1191vv6.us-east-1.aoss.amazonaws.com\",\n```",
      "The OpenSearch host endpoint region (us-west-2) does not match the file path region (us-east-1). According to the PR description, collections should be updated in us-east-1 region.\n```suggestion\n  \"OpensearchHost\": \"vqi7lzpgcwrmc1191vv6.us-east-1.aoss.amazonaws.com\",\n```",
      "The OpenSearch host endpoint region (us-west-2) does not match the file path region (us-east-1). According to the PR description, collections should be updated in us-east-1 region.\n```suggestion\n  \"OpensearchHost\":  \"vqi7lzpgcwrmc1191vv6.us-east-1.aoss.amazonaws.com\",\n```",
      "Missing closing double quote on the OpensearchHost value.\n```suggestion\n    \"OpensearchHost\": \"f0lwq1c2ilb6krnypt33.eu-west-1.aoss.amazonaws.com\",\n```",
      "The OpensearchHost value appears to have an extra 'h' prefix compared to other files in the same environment. Expected value should be 'f0lwq1c2ilb6krnypt33' not 'hf0lwq1c2ilb6krnypt33'.\n```suggestion\n  \"OpensearchHost\":  \"f0lwq1c2ilb6krnypt33.eu-west-1.aoss.amazonaws.com\",\n```",
      "The OpenSearch host value appears to have an extra leading 'h' character compared to other similar updates in this PR. It should be 'f0lwq1c2ilb6krnypt33.eu-west-1.aoss.amazonaws.com' to match the pattern in users-env.json, organizations-env.json, and accounts-env.json for the same environment.\n```suggestion\n  \"OpensearchHost\":  \"f0lwq1c2ilb6krnypt33.eu-west-1.aoss.amazonaws.com\",\n```",
      "The OpensearchHost value appears to be missing a character. The collection ID '815zm40nf044zex9un5' is 19 characters long, while all other AOSS collection IDs in this PR are 20 characters long. This could indicate a truncation error or typo.\n```suggestion\n    \"OpensearchHost\":  \"815zm40nf044zex9un5X.us-west-2.aoss.amazonaws.com\",\n```"
    ]
  },
  {
    "size": 10,
    "representative": "```suggestion\r\n          AWS_REGION : ${{ inputs.AWS_REGION }}\r\n```",
    "top_paths": [
      ".github/workflows/reusable_infra_tf_apply.yml",
      ".github/actions/start-the-instance/action.yml",
      ".github/workflows/groups_sam_apply_PR.yml",
      ".github/workflows/reusable_sam_apply.yml",
      ".github/workflows/release_sam_apply.yml"
    ],
    "comments": [
      "```suggestion\r\n          SAM_REGION : ${{ inputs.SAM_REGION }}\r\n```",
      "Please follow the same convention for all the input parameters in the workflow. For example, service_name and aws_region are in lowercase and STACK is in uppercase.",
      "```suggestion\r\n          AWS_REGION : ${{ inputs.AWS_REGION }}\r\n```",
      "```suggestion\r\n            AWS_REGION : ${{ inputs.AWS_REGION }}\r\n```",
      "@Sweatha1609 we can replace ${region} to  ${AWS::Region}",
      "> Environment variables (AWS_DEFAULT_REGION, ENV_DATA_REGION, GH_TOKEN) declared at job level(debug, validate) are not used in the following steps. Please remove these variables if they are not needed.\r\n\r\n@skpalani The environment variables defined at validate job level are not used in the workflow file and also in the linter and unit-test composite actions. Please confirm this.",
      "The variable $AWS_REGION is used but not defined in the script. It should reference the input parameter using '${{ inputs.AWS_REGION }}' instead.\n```suggestion\n                INSTANCE_NAME=\"${{ inputs.instance-name }}\"\n                AWS_REGION=\"${{ inputs.AWS_REGION }}\"\n```",
      "The variable $AWS_REGION is used but not defined in the script. It should reference the input parameter using '${{ inputs.AWS_REGION }}' instead.",
      "The workflow references 'inputs.STACK' but this input parameter is not defined in the workflow_dispatch inputs section. Only 'aws_region' is defined as an input. This will cause the workflow to fail.",
      "Multiple undefined variables are referenced: 'inputs.STACK', 'inputs.SERVICE_NAME', 'inputs.AWS_REGION', and 'env.SAM_S3_BACKEND'. These are not defined in the workflow inputs or environment variables, which will cause the deployment to fail."
    ]
  },
  {
    "size": 10,
    "representative": "Try to find a way to reduce the multiple DB calls.",
    "top_paths": [
      "groups/src/service.py",
      "iam-api/src/roles/roles_service.py",
      "devices/src/device_service.py",
      "users/src/service.py",
      "users/src/app.py"
    ],
    "comments": [
      "error handling - as we are doing multiple db transactions. make sure it is ALL or NONE",
      "same here to avoid  multiple db and fail fast.",
      "Try to find a way to reduce the multiple DB calls.",
      "same comment here for additional db call for getting relation",
      "avoid this additional db call. this was not there earlier.",
      "Is it getting the relation or adding the relation_request() just got confused",
      "multiple db calls in a for loop ? try to minimize the number of db calls with bulk fetch wherver possible",
      "Within for loop multiple DB calls....? can we make it as batch ?",
      "Is there any batch operation method available then plz use it to reduce the multiple db calls.",
      "avoid making db calls in a for loop. use bulk fetch"
    ]
  },
  {
    "size": 10,
    "representative": "> Suggestion: this can be handled in pydantic itself also check for required fields\r\n\r\nYes, we can but there is an additional check like is it update/create needs to be added in that validator. Its some what tricky and complex one.",
    "top_paths": [
      "users/src/app.py",
      "users/src/models.py",
      "roles/src/app.py",
      "accounts/src/accounts/models/core/account/account_description.py",
      "accounts/src/accounts/models/core/account/account_id.py"
    ],
    "comments": [
      "we need to check pydantic option. \r\n\r\n`from pydantic import BaseModel, root_validator, ValidationError\r\n\r\nclass Permission(BaseModel):\r\n    action: str\r\n    resource: str\r\n    description: str\r\n\r\n    @root_validator(pre=True)\r\n    def check_not_empty(cls, values):\r\n        if not values:\r\n            raise ValueError(\"Payload cannot be empty\")\r\n        return values\r\n\r\ntry:\r\n    permission = Permission(**payload)\r\nexcept ValidationError as e:\r\n    logger.error(f\"Validation error: {e}\")\r\n`",
      "Suggestion: this can be handled in pydantic itself also check for required fields",
      "> Suggestion: this can be handled in pydantic itself also check for required fields\r\n\r\nYes, we can but there is an additional check like is it update/create needs to be added in that validator. Its some what tricky and complex one.",
      "validate the request manually or use a pydantic model",
      "This is something that should probably be handled by the API pydantic models and is not necessary to do twice",
      "Unless it is a validation check that specifically has to be done after the request is validated and can't be done by the pydantic API models I think these checks should be removed",
      "First of all, why we need such an error here. If the unwanted fields are sent as part of the post payload then the pydantic should ignore right.",
      "This is incorrect. There was no null check in the original implementation. pydantic should prevent any strings not in the `field_validator`",
      "This validator redeclaration is redundant. UserSyncRequest inherits from BaseUserSyncRequest (line 453), which already has the `data_region_validation` validator applied at line 440. In Pydantic v2, field validators are inherited by subclasses, so this redeclaration is unnecessary. Consider removing it to reduce redundancy, or document why explicit redeclaration is preferred if it's intentional for clarity.",
      "This validator redeclaration is redundant. UserSyncUpdateRequest inherits from BaseUserSyncRequest (line 464), which already has the `data_region_validation` validator applied at line 440. In Pydantic v2, field validators are inherited by subclasses, so this redeclaration is unnecessary. Consider removing it to reduce redundancy, or document why explicit redeclaration is preferred if it's intentional for clarity.\n```suggestion\n\n```"
    ]
  },
  {
    "size": 10,
    "representative": "TODO's should have name and/or Jira ticket for accountability.",
    "top_paths": [
      "applications/src/business_logic.py",
      "README.md",
      "accounts/src/accounts/routers/accounts.py",
      "setup/infra/migration/data-synchronizer/layers/transformers.py",
      ".github/workflows/automation-testing.yaml"
    ],
    "comments": [
      "I would be fine with a jira ticket in a todo here then.  Maybe something like:\r\n`TODO(IAM-nnn): add validations per TID response use cases`\r\nJust some info and accountability linkage would go a long way for me.",
      "Can we have a person or jira ticket for accountability of TODOs?",
      "Can we have a ticket id or name on todos for accountability?  e.g. `# TODO(jfrench): ...` or `# TODO(CDH-123): ...`",
      "TODO's should have name and/or Jira ticket for accountability.",
      "Can you remark with a ticket or name and date so that we can have accountability.",
      "Nit: TODOs should have name or Jira ticket for accountability",
      "Nit: TODOs should have a name or Jira ticket id for accountability.",
      "I would be fine with this, please capture in TODO with jira ticket or name for accountability.",
      "Please add ticket and/or name to TODO for accountability",
      "Due to urgency, I am approving this on conditional basis. we could make the suggested changes in a separate jira ticket."
    ]
  },
  {
    "size": 10,
    "representative": "I think we are moving to a different config system. Regardless this should be in a common config file",
    "top_paths": [
      "accounts/src/accounts/authentication/config.py",
      "authorizers/template.yaml",
      "common/src/common/audit_logging/handler_firehose_impl.py",
      "users/src/constants.py",
      "setup/infra/migration/data-synchronizer/profile_scanner/app.py"
    ],
    "comments": [
      "This has to be updated to pull from a configuration file. Can be done later.",
      "I'm not sure about this, but should this be hard-coded or moved to a config ?",
      "These should be a single config but fed via external configuration only.",
      "sorry this should be a single config. My bad.",
      "plz move this to a config",
      "Are we using this config ?",
      "Better to move all these configs into a config.",
      "I guess the new config management is used to avoid repeated common configs into a central place its like hierarchical inheritance of configs @r-dharanidharan could you plz help her here to remove the repeated configs so that it will be a cleaner one .",
      "If this config is same for more than one means please move it to common. Avoid repeated configs unless its going to change for that env. @r-dharanidharan plz help her to fix this new config",
      "I think we are moving to a different config system. Regardless this should be in a common config file"
    ]
  },
  {
    "size": 10,
    "representative": "Logging failure at INFO level is inconsistent with the exception handling context. The failure message should be logged at ERROR or WARNING level to match the severity of the caught exceptions (RetrySyncException, SyncFailedException).\n```suggestion\n            logger.warning(\"Failed to process sync event\")\n```",
    "top_paths": [
      "users/src/identities_wrapper.py",
      "health/src/health_monitoring.py",
      "setup/infra/migration/data-synchronizer/scripts/address_enricher/tmaps.py",
      "groups/src/dal.py",
      "common/src/common/lambda_helper.py"
    ],
    "comments": [
      "This is not error. use logger info instead.",
      "As mentioned in another PR always use logger.exception if this exception is required for debugging purpose else we could use error.",
      "can we use logger.error here ?",
      "here also exception is raised before logging the failure reasons.",
      "If we are going to log the errors means why the logger level is \"info\" instead of error",
      "Logging failure at INFO level after an ERROR log is inconsistent. The failure message should be logged at ERROR or WARNING level to match the severity of the exception being raised, or removed since the error is already logged on line 24.\n```suggestion\n        logger.error(\"Failed to process sync event\")\n```",
      "Logging failure at INFO level after an ERROR log is inconsistent. The failure message should be logged at ERROR or WARNING level to match the severity of the exception being raised, or removed since the error is already logged on line 18.\n```suggestion\n        logger.error(\"Failed to process sync event\")\n```",
      "Logging failure at INFO level is inconsistent with the exception handling context. The failure message should be logged at ERROR or WARNING level to match the severity of the caught exceptions (RetrySyncException, SyncFailedException).\n```suggestion\n            logger.warning(\"Failed to process sync event\")\n```",
      "Can we add logger for debugging any exception ?",
      "use logger error for exception cases"
    ]
  },
  {
    "size": 10,
    "representative": "do we need this new model? can we use the UserToAccount ?",
    "top_paths": [
      "users/src/models.py",
      "users/src/app.py",
      "groups/src/service.py",
      "groups/src/models.py"
    ],
    "comments": [
      "Email is the only field we can set it as mandatory for create alone and rest of the fields are optional.",
      "do we need this new model? can we use the UserToAccount ?",
      "Use the existing model UserToAccount",
      "can we use the existing model UserToAccountList ??",
      "I have refactored to use existing model UserToAccount",
      "can we remove this model? This is same as UserToAccountList (with Optional)",
      "I see 'email' is mandated in BaseUserSyncRequest",
      "This should be called only for the entity types devices and applications and users without email and not for users with email",
      "Entity is also required as we use details like account name in the email template",
      "user-to-account model does not include entire user entity block, also it is created as separate new model like below\nclass UserToAccount(TrnModel):\n    id: Optional[str] = None\n    roleIds: Optional[List[str]] = None\n    default: Optional[bool] = None\n    status: Optional[str] = None\n    isOwner: Optional[bool] = None\n    emailTemplates: Optional[dict] = None"
    ]
  },
  {
    "size": 10,
    "representative": "@davemunson how about externalsystems without the \"source system\" attribute. Is it a mantatory one?",
    "top_paths": [
      "accounts/src/accounts/dynamodb/external_systems/repository.py",
      "accounts/src/accounts/service/external_systems/references/put.py",
      "accounts/src/accounts/dynamodb/account_repository/reference_repository/reference_repository_impl.py",
      "accounts/src/accounts/models/core/account/address/references.py",
      "accounts/src/accounts/dynamodb/external_systems/reader.py"
    ],
    "comments": [
      "Are you refering to the core `ExternalSystems` class",
      "It looks like we are re-implementing logic that exists in the external systems repository here.",
      "This and `References` seems like they might be duplicate implementations of the `ExternalSystemReference` and `ExternalSystemReferences` models",
      "the source system is being managed along with the other references",
      "Just a nit, but maybe we want `\"/externalSystems\"` as a constant.",
      "This is the server application that a relation for each external system is mapped to",
      "`Get a list of all the external systems available on the current server` I might be misunderstanding the assignment, but what does this mean?",
      "We are getting external systems within the context of the server",
      "Not only the source system, if the account has \"Trimble-PX\" external system then it should be marked as internal.",
      "@davemunson how about externalsystems without the \"source system\" attribute. Is it a mantatory one?"
    ]
  },
  {
    "size": 10,
    "representative": "Should have return type hint, even if None",
    "top_paths": [
      "users/src/service.py",
      "roles/src/service.py",
      "setup/infra/migration/data-synchronizer/stream_handler/mutations.py",
      "devices/src/app.py",
      "accounts/src/accounts/dynamodb/dal.py"
    ],
    "comments": [
      "mention the return type hint",
      "Should include type hint, even if return type is `-> None`.",
      "Should have return type hint, even if None",
      "we need to include the 'None' return type to hint and in function documentation",
      "Should have a return type hint as well.",
      "Maybe I'm missing it here, but this seems to return `None`.  Maybe drop return and set type hint here to `-> None`?",
      "If nothing is returned, type hint the return type as `None`",
      "Add return type hint `None`?",
      "add type hint. mention return type in the function doc",
      "Add missing return type hint."
    ]
  },
  {
    "size": 10,
    "representative": "I guess EntityTypes enum is a strenum so we dont need to use \".value\"\nif that enum is not \"strenum\" then plz make it. avoid \".value\"",
    "top_paths": [
      "applications/src/constants.py",
      "applications/src/app.py",
      "common/src/authz/models.py",
      "groups/src/service.py",
      "accounts/src/accounts/service/search.py"
    ],
    "comments": [
      "use StrEnum instead of str and Enum separatly.\r\nWhich will provide us string like thinks like we don't need to call \".value\" attribute of this enum directly assign this and use it in loggers to print the values. so plz use it and remove \".value\" where its been used.",
      "Better to use str enum which doesn't require this. Value",
      "Better to make the ResourceTypes as StrEnum instead of normal enum to avoid multiple \".value\" in more number of places.",
      "Dont hard code the entity. use resource types enum as much as possible",
      "i'd like to see it solidified that only \"asc\" and \"desc\" are acceptable params here. either with type annotations or an enum",
      "yeah i kind of went back and forth on this. i'd rather just keep it simple and not have this logic somewhere else or duplicate the EntityType enum but with numbers as priority",
      "Use entity names instead of hard coding.",
      "Typescripts type system handles this, it enforces that the literal value is only one of the specified literals in the type. This is the preferred way of writing string enums in typescript.",
      "I guess EntityTypes enum is a strenum so we dont need to use \".value\"\nif that enum is not \"strenum\" then plz make it. avoid \".value\"",
      "Currently we are only supporting two values for the subtype , and so we are defining it as enum."
    ]
  },
  {
    "size": 10,
    "representative": "The assertion message has been corrected now",
    "top_paths": [
      "tests/automation/tests/sync/test_cdh_sync_account_query.py",
      "tests/automation/tests/permissions/test_permissions_apptoken.py",
      "accounts/src/accounts/cdh_sync/address_controller/reconciliation.py",
      "tests/automation/tests/accounts/create_address/graphql/create_address_graphql_test.py",
      "devices/tests/test_device_certificate_service.py"
    ],
    "comments": [
      "Used the soft assertions now",
      "This assert pattern seems weird to me. Why wouldn't you just use an if statement log the error and return false?",
      "We can refactor to move these assertions to a validator method. We can take care of this in another PR.",
      "assertion should be based on the error code. Pls check all assertions which are referring to error constants or strings",
      "Error message updated in error code, assertion made based on that.",
      "The assertion message has been corrected now",
      "New assertions has been added to test particular expected message for each scenario",
      "these checks are ok, but this pattern is used in many of your test functions and it is unnecessary to raise the AssertionError yourself, the assert keyword already does this when the assertion fails.",
      "This assertion doesn't verify the expected error code: `mock_raise_error.call_args[0][0]` is the same value that was actually passed as the first argument, so the test will pass even if the wrong `ErrorCode` is used. Assert explicitly on `ErrorCode.INVALID_UUID` (and the other args) to make the test meaningful.",
      "On assertion failure, these custom messages now appear in the error output."
    ]
  },
  {
    "size": 10,
    "representative": "I guess the previous logger itself make sense. Plz avoid to log the request payloads and other request related params unless it is required for troubleshooting purposes Else cloudwatch logs only get filled up and cost intern increase.",
    "top_paths": [
      "groups/src/app.py",
      "token_authorizers/graphqlAuthorizer/src/apollo_graphql_authorizer.py",
      "auditLog/src/audit_log/api_handler.py",
      "applications/src/app.py",
      "scripts/px_account_membership/src/apply_px_dataset.py"
    ],
    "comments": [
      "I guess the previous logger itself make sense. Plz avoid to log the request payloads and other request related params unless it is required for troubleshooting purposes Else cloudwatch logs only get filled up and cost intern increase.",
      "This should be in info. This is needed for troubleshooting purposes. For all apis this is the entry point and this log statement is needed. atleast one log statement is guaranteed (with all parametrs the api is called) when there is a error response from api and we need to know the parametrs and request data is sent.",
      "do we really need to log the whole request information (payload / query params / path params?\nthe reason is query params and path params are already logged by apigateway itself. only post payloads are not. But for error related things we could better log the whole request payload. like internal server error something like that. Rest we dont need I guess. else it will simply pollute the cloudwatch only.",
      "I guess those are logged by api gateway itself and its repeated or duplicated logs only across components only increase the cloudwatch.",
      "We recommend at least one log info (very first log statement which contains query/path params and optional payload) for every api call for tracking purposes. Rest all can be kept it in debug mode and only important/necessary information should be logged in info mode.",
      "This print statement logs the entire authorization result including token_data and raw_token, which contains sensitive security information. Remove this print statement to prevent sensitive data exposure in CloudWatch logs.",
      "Avoid logging the full authorizer event; it typically contains the Authorization token (credentials) and can leak secrets/PII into CloudWatch. Log only non-sensitive metadata (e.g., request id) and/or redact the token before logging.",
      "The API handler logs the full incoming request body at info level. Since this endpoint is specifically for audit logging, `event_data` is expected to include potentially sensitive fields; duplicating the full payload into CloudWatch can violate the repo's \"No Sensitive Info\" logging expectations and increases exposure. Consider either removing this log line, downgrading to debug, and/or logging only non-sensitive metadata (e.g., request_id + event_type) with payload sanitization.\n```suggestion\n    req = AddToAuditLogRequest(**app.current_event.json_body)\n\n    logger.info(\n        \"Received request to add to audit log \"\n        f\"(request_id={req.request_id}, service={req.service}, action={req.action}, resource={req.resource})\"\n    )\n```",
      "This logs the full request body at INFO level. The audit log payload can include sensitive fields in `eventData`, so this risks leaking PII/secrets into CloudWatch. Log only non-sensitive metadata (e.g., requestId, service/action/resource) or move the full body to DEBUG with redaction.\n```suggestion\n    logger.info(\"Received request to add to audit log\")\n```",
      "Logging the full `event` can leak sensitive data (e.g., `identity.resolverContext.raw_token` / token claims) into CloudWatch logs. Prefer logging only a sanitized subset (request IDs, fieldName/typeName) and explicitly redacting auth material, or remove these debug logs entirely.\n```suggestion\n    logger.debug(\n        \"Received AppSync request metadata\",\n        extra={\n            \"fieldName\": event.get(\"fieldName\"),\n            \"typeName\": event.get(\"typeName\"),\n            \"requestId\": event.get(\"request\", {}).get(\"headers\", {}).get(\"x-amzn-requestid\"),\n        },\n    )\n    logger.debug(\n        \"Received Lambda context metadata\",\n        extra={\n            \"aws_request_id\": getattr(context, \"aws_request_id\", None),\n            \"function_name\": getattr(context, \"function_name\", None),\n        },\n    )\n```"
    ]
  },
  {
    "size": 9,
    "representative": "can we follow the same error message ?",
    "top_paths": [
      "iam-api/src/users/users_appsync_handler.py",
      "accounts/src/accounts/serialization/resource_serializer.py",
      "common/src/common/lambda_helper.py",
      "roles/src/service.py",
      "users/src/service.py"
    ],
    "comments": [
      "Can you please handle the error properly?",
      "I think this should stay an error",
      "can we change the error message?",
      "shouldn't this be error statement?",
      "can we follow the same error message ?",
      "Let's make sure we follow below rules for all the errors,\n\n1. The error starts with a capital letter\n2. The error should not have a full stop(.) at the end\n3. Not use (:)colon in the statement",
      "use the same error message like it was earlier",
      "Error message changed. Pls checked this",
      "I think we should include the error message here to know what went wrong on the final attempt"
    ]
  },
  {
    "size": 9,
    "representative": "The same stack-membership expression `(inputs.STACK == 'prod' || inputs.STACK == 'stage' || inputs.STACK == 'prestage')` is duplicated in multiple places. To reduce repetition and the chance of drift (e.g., adding/removing an environment in one place but not others), consider using a consistent helper pattern like `contains(fromJson('[\\\"prod\\\",\\\"stage\\\",\\\"prestage\\\"]'), inputs.STACK)` across the workflow.\n```suggestion\n        if: ${{ contains(fromJson('[\"prod\",\"stage\",\"prestage\"]'), inputs.STACK) }}\n```",
    "top_paths": [
      ".github/workflows/release_sam_apply.yml",
      ".github/workflows/configs_deployment.yaml",
      ".github/workflows/groups_sam_apply_PR.yml",
      ".github/workflows/reusable_sam_apply.yml"
    ],
    "comments": [
      "```suggestion\r\n    if: ${{ inputs.service_name == 'roles' &&  contains([\"stage\", \"prod\"], inputs.STACK) && contains('[\"shaju-g\",\"Sweatha1609\"]', github.actor) }}\r\n```",
      "```suggestion\r\n    if: ${{ inputs.service_name == 'roles' && contains([\"dev\", \"dev2\", \"qa\", \"pt\", \"prestage\"], inputs.STACK) && contains('[\"shaju-g\",\"Sweatha1609\"]', github.actor) }}\r\n```",
      "Using contains on a serialized list string can match substrings (e.g., a crafted username containing an allowed name) allowing unintended execution\u2014use a proper list comparison: if: ${{ github.actor == 'user1' || github.actor == 'user2' ... }} or map-based membership logic.\n```suggestion\n    if: ${{ github.actor == 'shaju-g' || github.actor == 'Sweatha1609' || github.actor == 'Ragupathi-veludurai' || github.actor == 'dhineshkumar-d-trimble' || github.actor == 'kedharTrmb' }}\n```",
      "The long chain of OR conditions is difficult to read and maintain. Consider using the `contains()` function with an array of allowed stacks instead: `if: ${{ contains(fromJSON('[\\\"dev\\\", \\\"dev1\\\", \\\"dev2\\\", \\\"dev3\\\", \\\"dev4\\\", \\\"dev5\\\", \\\"dev6\\\", \\\"dev7\\\", \\\"integ\\\", \\\"pt\\\", \\\"qa\\\", \\\"qa1\\\", \\\"qa2\\\", \\\"qatest\\\"]'), inputs.STACK) }}`\n```suggestion\n    if: ${{ contains(fromJSON('[\"dev\", \"dev1\", \"dev2\", \"dev3\", \"dev4\", \"dev5\", \"dev6\", \"dev7\", \"integ\", \"pt\", \"qa\", \"qa1\", \"qa2\", \"qatest\"]'), inputs.STACK) }}\n```",
      "Consider extracting the allowed actors list to a repository variable or secret for easier maintenance. Hardcoding usernames in workflow files makes it difficult to update access permissions and track changes to the authorization list.\n```suggestion\n    if: ${{ (inputs.STACK == 'prestage' || inputs.STACK == 'stage' || inputs.STACK == 'prod') && contains(split(vars.ALLOWED_ACTORS, ','), github.actor) }}\n```",
      "This condition is harder to read than necessary: `always()` is effectively negated by `&& !failure() && !cancelled()`, so it doesn\u2019t add value here. Consider simplifying to just the stack gate plus `!cancelled()`/`!failure()` (or use a single `contains(fromJson(...), inputs.STACK)` for the stack membership check) to improve clarity and reduce future mistakes.\n```suggestion\n    if: ${{ inputs.STACK == 'stage' || inputs.STACK == 'prestage' || inputs.STACK == 'prod' }}\n```",
      "The same stack-membership expression `(inputs.STACK == 'prod' || inputs.STACK == 'stage' || inputs.STACK == 'prestage')` is duplicated in multiple places. To reduce repetition and the chance of drift (e.g., adding/removing an environment in one place but not others), consider using a consistent helper pattern like `contains(fromJson('[\\\"prod\\\",\\\"stage\\\",\\\"prestage\\\"]'), inputs.STACK)` across the workflow.\n```suggestion\n        if: ${{ inputs.DeployIamAuthorizationLambda == 'true' && contains(fromJson('[\"prod\",\"stage\",\"prestage\"]'), inputs.STACK) }}\n```",
      "The same stack-membership expression `(inputs.STACK == 'prod' || inputs.STACK == 'stage' || inputs.STACK == 'prestage')` is duplicated in multiple places. To reduce repetition and the chance of drift (e.g., adding/removing an environment in one place but not others), consider using a consistent helper pattern like `contains(fromJson('[\\\"prod\\\",\\\"stage\\\",\\\"prestage\\\"]'), inputs.STACK)` across the workflow.\n```suggestion\n        if: ${{ contains(fromJson('[\"prod\",\"stage\",\"prestage\"]'), inputs.STACK) }}\n```",
      "The same stack-membership expression `(inputs.STACK == 'prod' || inputs.STACK == 'stage' || inputs.STACK == 'prestage')` is duplicated in multiple places. To reduce repetition and the chance of drift (e.g., adding/removing an environment in one place but not others), consider using a consistent helper pattern like `contains(fromJson('[\\\"prod\\\",\\\"stage\\\",\\\"prestage\\\"]'), inputs.STACK)` across the workflow.\n```suggestion\n    if: ${{ always() && !cancelled() && contains(fromJson('[\"prod\",\"stage\",\"prestage\"]'), inputs.STACK) }}\n```"
    ]
  },
  {
    "size": 9,
    "representative": "handle this exception in the service layer as the regular notfoundexception is handled there. keep them all in one place",
    "top_paths": [
      "users/src/service.py",
      "applications/src/main.py",
      "applications/src/service.py",
      "roles/src/service.py",
      "roles/src/dal.py"
    ],
    "comments": [
      "This may not be required here. throw respective error from service layer and it will be handled by the common exception handler",
      "Dont catch Generic Exception in service layer. it will be handled by the exception handler with neccessary stack trace info for troubleshooting the error. catch only known exception and act on it",
      "Handling generic Exception in service is not recommended. it is already handled in the common error handler. better to move this logic in the respective method.",
      "dal layer is adding the logging during this exception. is it okay ?\r\n![image](https://github.com/user-attachments/assets/a11e72c5-4ce0-44cd-8f16-bbad929529d4)",
      "can we throw this error from dal and handle it in service layer?",
      "same comment.\r\nhandle NotFoundException in users dal or service",
      "@Sakthi1305ganesh NotFoundException is handled in dal layer.",
      "I would prefer we only raise the http exceptions at the service layer. And use the database exceptions in this module.",
      "handle this exception in the service layer as the regular notfoundexception is handled there. keep them all in one place"
    ]
  },
  {
    "size": 9,
    "representative": "do we need to consider account id from subject token if not passed in query param ?",
    "top_paths": [
      "roles/src/helper.py",
      "users/src/users_appsync_handler.py",
      "users/src/service.py",
      "common/src/common/lambda_helper.py",
      "groups/src/service.py"
    ],
    "comments": [
      "The account_id variable is not at all reused so no need of extra variable, directly use the token_data.account_id",
      "We should not use the subject's account id from token. Get the account relation for the target user_id which can be in a same or a different account",
      "query param should override account id from JWT or the other way around ? i think it make sense for the query param to override the JWT account, else the accountId in the query param is not useful",
      "for user token, we are always take JWT accountId. we should not override for now.",
      "then should we throw error when account id is passed ?? it will be confusing if the account id is not considered? \r\n\"we are always take JWT accountId\" did we decided with the team or architectures suggested ??",
      "accountId not found in query params and token ??",
      "do we need to consider account id from subject token if not passed in query param ?",
      "Kindly change this to \r\noption \r\n1. if \"accountId\" in query_params \r\n2. query_params.get(\"accountId\", token_data.account_id)",
      "we need to use the accountId from the query params if available."
    ]
  },
  {
    "size": 9,
    "representative": "This is how it is configured in other BLUs, As it was missed in Device BLU, I just added the same\r\n\r\ncc @shaju-g",
    "top_paths": [
      "devices/src/app.py",
      "common/src/common/models.py",
      "devices/template.yaml",
      "groups/src/error.py",
      "groups/src/app.py"
    ],
    "comments": [
      "@r-harivarthini  Have you deployed device BLU in a stack and tested the functionality?",
      "@EzhilAlagesan Currently there is no infra setup for deploying devices BLU. So we couldn't deploy and test",
      "Is this model common for all the BLUs. If not plz move it appropriate BLU.\r\nOnly BLUs own the models not the common layer.",
      "This is how it is configured in other BLUs, As it was missed in Device BLU, I just added the same\r\n\r\ncc @shaju-g",
      "And also, plz cross-check with other BLUs to consolidate common messages and place it in common blu would make sense.",
      "I don't think so. This should be inferred by the Datadog layer which is wired in each blu's. But I will discuss with you on this.",
      "This is mandatory for ALL BLUs which is using common layer.",
      "this alone, couldn't be addressed due to circular imports and the fix might require touching most blu's so adding a FIXME marker so that we don't lose track of it",
      "will this affect other BLUs whose are rely on \"sort_by\" stuffs? or will create a downtime I guess it wont since we are directly dealing with Layer ARN?\nBut plz confirm there is issue with other BLUs"
    ]
  },
  {
    "size": 9,
    "representative": "The change has been made intentionally and will refactor later.",
    "top_paths": [
      "accounts/src/accounts/dynamodb/config.py",
      ".github/workflows/release_sam_apply.yml",
      "users/src/service.py",
      "devices/src/app.py",
      ".github/actions/assistant/extract_function.py"
    ],
    "comments": [
      "I wasn't aware of that, so let's hold on the refactor then.",
      "This was updated for testing purpose, reverting",
      "This change is a result of miscommunication and it is reverted now",
      "It is a common refactor. Will take it as a separate task",
      "I was thinking about these functions as candidate for refactor, but this is probably fine for now.",
      "This is an intended change. Resolving due to the same",
      "This is an intended change.",
      "The change has been made intentionally and will refactor later.",
      "This is an intended change. Resolving the comment."
    ]
  },
  {
    "size": 9,
    "representative": "Add function doc for all new methods",
    "top_paths": [
      "users/src/service.py",
      "roles/src/service.py",
      "health/src/lambda_function.py",
      "users/src/identities_wrapper.py",
      "users/src/invitation_webhook.py"
    ],
    "comments": [
      "Add documentation for the new methods and also the existing without doc",
      "Add documentation for new methods",
      "add documentation for all new methods",
      "Add function doc for all new functions",
      "Add function doc for all new methods",
      "Add function doc for new/updated functions accordingly",
      "rename this function and update function doc accordingly",
      "add function doc for all new function",
      "Add function documentation for all new functions"
    ]
  },
  {
    "size": 9,
    "representative": "The bucket name references 'qa2' but this is a qa1 configuration. The bucket should be 'terraform-iam-qa1-state-eu-gb1' to match the qa1 environment.\n```suggestion\nbucket = \"terraform-iam-qa1-state-eu-gb1\"\n```",
    "top_paths": [
      "setup/infra/terraform_iac/applayer/configuration/prestage/us-east-1/prestage.tfbackend",
      "setup/infra/terraform_iac/prerequisite/sync_authorizer_lambda/configuration/qa/us-west-2/qa.tfbackend",
      "setup/infra/terraform_iac/prerequisite/s3_buckets/configuration/qa1/eu-gb/eu-west-2/qa1.tfbackend",
      "setup/infra/terraform_iac/internalGateway/configuration/qa/eu-gb/eu-west-2/qa.tfbackend",
      "setup/infra/terraform_iac/prerequisite/s3_buckets/configuration/qa/eu-gb1/eu-west-2/qa.tfbackend"
    ],
    "comments": [
      "Bucket name format can be terraform-iam-<stack>-<region>-state\r\n@shaju-g",
      "@Sweatha1609 us-west-2 resources bucket is already created as  terraform-state-iam , and we using common state file bucket for all stacks in dev account .",
      "terraform-state-iam is a common bucket for all terraform state files in dev account .",
      "The bucket name references 'qa2' but this is a qa1 configuration. The bucket should be 'terraform-iam-qa1-state-eu-gb1' to match the qa1 environment.\n```suggestion\nbucket = \"terraform-iam-qa1-state-eu-gb1\"\n```",
      "The bucket name references 'qa2' but this is a qa configuration. The bucket should be 'terraform-iam-qa-state-eu-gb1' to match the qa environment.\n```suggestion\nbucket = \"terraform-iam-qa-state-eu-gb1\"\n```",
      "The bucket name references \"qa2\" but this is a qa configuration file. The bucket should be \"terraform-iam-qa-state-eu-gb1\" to match the environment.\n```suggestion\nbucket = \"terraform-iam-qa-state-eu-gb1\"\n```",
      "terraform-iam-stage-state-ew1 - State bucket for ew1 region.",
      "The bucket name suffix 'eu-gb1' is inconsistent with other regions which use abbreviations like 'ew2' for eu-west-2. Consider using 'terraform-iam-stage-state-ew2' to maintain consistency with the established naming pattern.\n```suggestion\nbucket = \"terraform-iam-stage-state-ew2\"\n```",
      "The bucket name suffix 'eu-gb1' is inconsistent with other regions which use abbreviations like 'ew2' for eu-west-2. Consider using 'terraform-iam-prod-state-ew2' to maintain consistency with the established naming pattern.\n```suggestion\nbucket = \"terraform-iam-prod-state-ew2\"\n```"
    ]
  },
  {
    "size": 9,
    "representative": "The `permissions` query is calling `getPermissionById` with `(first, after, identity)`, which looks like a copy/paste error (and `getPermissionById` isn't defined in the added RolesDataSource). This should call the list method (e.g., `dataSources.roles.getPermissions(args.first, args.after, identity)`).",
    "top_paths": [
      "graphql/apollo-server/src/resolvers.ts",
      "users/src/service.py",
      "roles/src/service.py",
      "roles/src/app.py"
    ],
    "comments": [
      "rename the role_list to `roles` dont append the data types in variables.",
      "rename the name as \"roleIds\" if it is multiple values else simply \"roleId\" is fine",
      "rename this to `roles` dont append its data types at the end. Just name the variables like normal one (Either singular / Plural)",
      "Because list_roles expects query params and token data. I have removed the query params as it is initialized as empty dictionary",
      "line 1420 may not be needed. just use app_to_role_request.relationData. this needs to be fixed in applications endpoint as well. initialy relationData was a strict defined model so it was coded like that, later it was changed to dict to allow any fields.",
      "`contextValue.dataSources.permissions` does not exist on `IAMDataSources` (only `accounts` and `roles` are wired in `getDataSources`). This will be `undefined` at runtime and break the `permission` query. Route this resolver to `dataSources.roles` (e.g., `getPermission(...)`) or add a dedicated PermissionsDataSource and register it in `IAMDataSources`/`service.ts`.\n```suggestion\n\t\t\treturn contextValue.dataSources.roles.getPermissionById(\n\t\t\t\targs.id,\n\t\t\t\tcontextValue.identity,\n\t\t\t);\n\t\t},\n\t\tpermissions: async (parent, args, contextValue) => {\n\t\t\treturn contextValue.dataSources.roles.getPermissionById(\n```",
      "`getAcoountRolesOfUser` is misspelled (\"Acoount\") which makes the API awkward and easy to miscall. Consider renaming to `getAccountRolesOfUser` (and updating the corresponding method name in `RolesDataSource`).",
      "The `permissions` query is calling `getPermissionById` with `(first, after, identity)`, which looks like a copy/paste error (and `getPermissionById` isn't defined in the added RolesDataSource). This should call the list method (e.g., `dataSources.roles.getPermissions(args.first, args.after, identity)`).",
      "`Role.permissions` is delegating to `getPermissions(...)`, but `RolesDataSource.getPermissions` is a *Query* list method that expects `(first, after, identity)`. For `Role.permissions` (no args in the schema), this should call the role-scoped loader (e.g., `getRolePermissions(parent.id, identity)`), otherwise the resolver will either fail type-checking or return the wrong data."
    ]
  },
  {
    "size": 9,
    "representative": "These dev/qa auditLog env files were updated with the new parameters required by `auditLog/template.yaml`, but there are many other `*/auditLog-env.json` configs in the repo (dev/us-east-1, dev/eu-west-1, prestage, stage, prod, etc.) that still lack `AnalyticsRoleArn`, `LambdaMemory`, and `InternalApiGateway`. If auditLog is deployed in those environments/regions (the release workflow conditions suggest it can be), deployment will fail due to missing CloudFormation parameters. Please propagate these keys (with correct values per env) to the remaining auditLog-env.json files that can be deployed.",
    "top_paths": [
      "setup/infra/sam/configuration/qa/us-west-2/users-env.json",
      ".github/workflows/release_common_sam_apply.yml",
      "setup/infra/sam/configuration/stage/us-west-2/users-env.json",
      "setup/infra/terraform_iac/internalGateway/configuration/templates/internal-swagger-yaml/iam-internal-api.yaml",
      "auditLog/template.yaml"
    ],
    "comments": [
      "please update stack wise firehose location @r-dharanidharan  in format iam-auditLog-${Environment}",
      "Need to update the audit log location here as well",
      "Maria told to don't hardcode the environment name. So I have Environment variable in the SAM template as\r\n`!Sub AuditLogLocation-${Environment}`",
      "The Lambda function URI pattern omits the deployment environment variable that is present in most other internal API functions. Other functions use `iam-{service}-${stack}-${deployment_environment}-${suffix_region}` which supports blue/green deployments via stage variables (see lines 1096, 44, 69). This audit log API uses `iam-audit-log-api-${stack}-${suffix_region}`, which means it cannot support blue/green deployments. Verify whether this omission is intentional. If blue/green deployment support is needed in the future, the function naming pattern will need to be updated.",
      "`auditLog/template.yaml` now requires `AnalyticsRoleArn`, `LambdaMemory`, and `InternalApiGateway` parameters (no defaults). There are existing `setup/infra/sam/configuration/**/auditLog-env.json` files (e.g. dev/us/us-east-1) that still only provide Environment/FirehoseRoleArn/Region/InheritorRoleId/DataRegion, so SAM deploy for those stacks/regions will fail with missing-parameter errors. Either add these parameters to all auditLog-env.json variants that can be deployed, or provide safe defaults / refactor to derive them (e.g., from SSM) so non-updated environments keep deploying.\n```suggestion\n    Type: String\n    Default: \"*\"\n```",
      "These dev/qa auditLog env files were updated with the new parameters required by `auditLog/template.yaml`, but there are many other `*/auditLog-env.json` configs in the repo (dev/us-east-1, dev/eu-west-1, prestage, stage, prod, etc.) that still lack `AnalyticsRoleArn`, `LambdaMemory`, and `InternalApiGateway`. If auditLog is deployed in those environments/regions (the release workflow conditions suggest it can be), deployment will fail due to missing CloudFormation parameters. Please propagate these keys (with correct values per env) to the remaining auditLog-env.json files that can be deployed.",
      "`LamdaMemorySize` is a typo and won\u2019t match the SAM parameter expected by `auditLog/template.yaml` (`LambdaMemory`). This will likely cause deploy failures due to a missing required `LambdaMemory` override. Rename the key to `LambdaMemory` to align with other environments.\n```suggestion\n    \"LambdaMemory\": 512\n```",
      "The deploy action always appends `CommonLayerArn=${{ inputs.COMMON_LAYER_ARN }}` to the `sam deploy` command. Since `auditLog/template.yaml` declares `CommonLayerArn` as a required parameter, the non-lower `deploy-auditLog` job in this workflow should also be updated to depend on `retrieve-common-layer-arn` and pass its output (instead of the current \"null\" value), otherwise stage/prod deploys may fail.",
      "@Ragupathi-veludurai valid copilot comment , change is missing in \"deploy-auditLog:\""
    ]
  },
  {
    "size": 9,
    "representative": "Just added resource type and resource id for more precise detail, Not needed?",
    "top_paths": [
      "roles/src/dal.py",
      "groups/src/service.py",
      "common/src/common/authorizer_helper.py",
      "roles/src/service.py",
      "accounts/src/accounts/service/search.py"
    ],
    "comments": [
      "Is this resource_type correct? or should it be an application?",
      "is this resource not found or account not found ??",
      "parameters resource_id and resource_type misleading. can we follow the same pattern ?",
      "The resource_type will be in singular form whereas the constancts type ref are in plural form",
      "why is the resource_type account ?\r\n@ElammathiM",
      "Is resource relation not found or resource not found?",
      "the resource path ww/api/search should be included",
      "Just added resource type and resource id for more precise detail, Not needed?",
      "does the account resource need this field too? maybe i'm not understanding correctly"
    ]
  },
  {
    "size": 9,
    "representative": "won't this create issue for device validation ?",
    "top_paths": [
      "groups/src/service.py",
      "devices/src/devices_sync_handler.py",
      "setup/infra/migration/data-synchronizer/layers/transformers.py",
      "devices/src/models.py",
      "devices/src/device_service.py"
    ],
    "comments": [
      "or this can be extracted into a validation method which'd make sense\r\nif we elect that the method is very specific to device validation, we can event keep it in local scope to this function definition",
      "won't this create issue for device validation ?",
      "Rename this method to `validate_device_request` or any proper name. \"deviceId\" alone not checked here. so plz name the method with appropriately",
      "Is null check is needed or handled already as part of \"create_device()\"",
      "@mariasekartrimble we are handling it in create_device()",
      "@raahavm In line number 357 & 358 we are validating model & manufacturer. calling  __validate_device_field there we are validating",
      "This method can be merged with check_device_exists",
      "Then this validation can be before the get device",
      "@raahavm if model not found in device record, it'll check in physical device this known use case is covered. pls check _validate_physical_device method"
    ]
  },
  {
    "size": 9,
    "representative": "These retry logics are already part of \"fail_safe_request()\". we can reuse that.",
    "top_paths": [
      "common/src/db/search/exceptions.py",
      "common/src/common/trimble_identity/token_session.py",
      "events/src/events_service/events_common/python/events_service_handler_base.py",
      "tests/performance/utils/getUserToken.js",
      "tests/automation/tests/sync/test_cdh_sync_account_query.py"
    ],
    "comments": [
      "Already, there is a fail_safe_request() available in common.\r\nPlz check it out before this?",
      "`fail_safe_request()` doesn't fit here, using conventional logger instead",
      "SessionValue is extracted with retry mechanism. The code will  return a session value or error our if retries are exhausted.",
      "These retry logics are already part of \"fail_safe_request()\". we can reuse that.",
      "the fail_safe_request() itself already configured with session and retry things. just pass the required retry args would be fine here. no more additional session things.",
      "It is in the existing code. i think the fail_safe_request handles requests session. Here we are using OAuth2Session. thats why retry logic is added here",
      "Here we are handling 401 code only. token gets refreshed and called again. the fail safe request handles error codes 500, 502, 503, 504",
      "Need clarification on why you need this retry mechanism",
      "Why we need this retry logic for our own db?\nIts already been taken care by the awsresourcs.py itself. plz cross check that. If you need then plz touch that awsresources with preferred retry numbers with backoff factor"
    ]
  },
  {
    "size": 9,
    "representative": "log the response status code and response text",
    "top_paths": [
      "scripts/cdh_account_user_relation_creation/cdh_account_user_relation_creation.py",
      "organizations/src/service.py",
      "setup/infra/migration/data-synchronizer/scripts/populate-default-accounts/populate-default-accounts.py",
      "setup/infra/migration/data-synchronizer/scripts/address_enricher/mapper.py",
      "common/src/common/invitation/invitation_wrapper.py"
    ],
    "comments": [
      "check response code. handle not success status code",
      "Can we also log the response status code ?",
      "verify status as well as part of log",
      "error handling ?? can we log the errors if anything raised during execution",
      "For error logging just print the response.text instead of json things.",
      "log the response status code and response text",
      "can we log the IAM response status code and body in case of unsuccessful status code for troubleshooting. also log the x-correlation-id from response headers.",
      "log the response code and body for troubleshooting in case of failures",
      "log response code and body"
    ]
  },
  {
    "size": 9,
    "representative": "Hmm ok. we could raise the error inside the function raise_application_error() instead of raise app_error()",
    "top_paths": [
      "users/src/error_codes.py",
      "groups/src/service.py",
      "groups/src/error_codes.py",
      "events/src/events_service/events_common/python/events_service_helper_base.py",
      "organizations/src/service.py"
    ],
    "comments": [
      "suggestion: function name: raise_application_error  \r\nwe could accept error code and http status code as parameters",
      "@DharmarajanRajendran just now I realised why I named it as just `app_error` and not `raise_app_error`. This method only returns the application error instance. It is at the calling place where the actual application error is raised.",
      "Hmm ok. we could raise the error inside the function raise_application_error() instead of raise app_error()",
      "Use raise_application_error function in the all the places.",
      "The function is documented to return ApplicationError but actually raises it. The docstring should indicate 'Raises:' instead of suggesting a return value.\n```suggestion\ndef raise_application_error(error_code: ErrorCode, *args):\n    \"\"\"\n    Raises:\n        ApplicationError: Raised with the provided error code and formatted detail.\n\n    Args:\n        error_code (ErrorCode): The error code enum containing error details.\n        *args: Arguments to format the error detail message.\n```",
      "The function is missing a docstring description. While Args and Raises sections are present, the function should include a brief description of what it does (e.g., 'Raises an ApplicationError with the specified error code and formatted message.').\n```suggestion\n    \"\"\"\n    Raises an ApplicationError with the specified error code and formatted message.\n```",
      "The exception is no longer re-raised after logging, which means the function will return None instead of propagating the error. This changes the function's behavior and may break error handling expectations.\n```suggestion\n            raise\n```",
      "This return statement is unreachable because raise_application_error will raise an exception on line 571. The return statement should be removed.\n```suggestion\n\n```",
      "Unreachable return statement. The function raises an ApplicationError on line 2501, so this return will never execute. Remove this line.\n```suggestion\n\n```"
    ]
  },
  {
    "size": 9,
    "representative": "There's an extra underscore in the path. It should be `file_service_credentials` not `file_service__credentials` to match the pattern used in all other configuration files.\n```suggestion\n  \"FileServiceConfig\":\"/iam/dev/file_service_credentials\",\n```",
    "top_paths": [
      "setup/infra/sam/configuration/dev6/us/us-west-2/events-env.json",
      "events/src/ingestions/file_service_ingestion/util_helper.py",
      "organizations/src/service.py",
      "accounts/src/accounts/utils/config.py",
      "accounts/src/accounts/service/__init__.py"
    ],
    "comments": [
      "If the environment variable FILE_SERVICE_SPACE_DETAILS is not set, os.environ.get() returns None, and json.loads(None) will raise a TypeError. Should provide a default value or add proper error handling.\n```suggestion\nfile_service_space_details = os.environ.get(\"FILE_SERVICE_SPACE_DETAILS\")\nif file_service_space_details is None:\n    raise RuntimeError(\"Environment variable FILE_SERVICE_SPACE_DETAILS is not set.\")\nspace_details = json.loads(file_service_space_details)\n```",
      "There's an extra underscore in the path. It should be `file_service_credentials` not `file_service__credentials` to match the pattern used in all other configuration files.\n```suggestion\n  \"FileServiceConfig\":\"/iam/dev/file_service_credentials\",\n```",
      "There's a double underscore in the path '/iam/dev/file_service__credentials' which appears to be a typo. It should be '/iam/dev/file_service_credentials' to match the pattern used in other environment files.\n```suggestion\n  \"FileServiceConfig\":\"/iam/dev/file_service_credentials\",\n```",
      "There is an extra underscore in 'file_service__credentials'. It should be 'file_service_credentials' to match the pattern used in other environments.\n```suggestion\n  \"FileServiceConfig\":\"/iam/dev/file_service_credentials\",\n```",
      "The environment variable `GEOCODING_SERVICE_API_KEY_PATH` is still being retrieved but is no longer used since the API key is now obtained from `CONFIG_MGR`. This unused variable should be removed to keep the code clean and avoid confusion.\n```suggestion\n\n```",
      "The config key `iam/common_config/file_service_space_details` is not defined in any of the global config files. The global configs only define `iam/common_config/file_service_api_config`. This will cause a runtime error when this code attempts to fetch the configuration.\n```suggestion\nspace_details = CONFIG_MGR.get_config(config_key=\"iam/common_config/file_service_api_config\",\n```",
      "The variable name `GEOCODING_SERVICE_API_KEY_PATH` suggests it contains a path, but it now contains the actual API key value. Consider renaming to `GEOCODING_SERVICE_API_KEY` to accurately reflect its purpose.\n```suggestion\nGEOCODING_SERVICE_API_KEY = accounts_config.get(\"iam/common_config/geocoding_service_api_key\")\n```",
      "Using `GEOCODING_SERVICE_API_KEY_PATH` as the API key value is misleading since the variable name implies a path but now contains the key itself. This should be updated to match the corrected variable name.",
      "The json.loads() call should include error handling for malformed JSON. If the configuration string is invalid JSON, this will raise a JSONDecodeError with no context about which configuration key failed to parse. Consider wrapping this in a try-except block and providing a meaningful error message that includes the config_key for easier debugging.\n```suggestion\n            try:\n                file_service_config = json.loads(file_service_config)\n            except json.JSONDecodeError as exc:\n                logger.error(\n                    \"Failed to parse JSON configuration for \"\n                    '\"iam/common_config/file_service_api_config\": %s',\n                    str(exc),\n                )\n                raise ValueError(\n                    'Invalid JSON in configuration key '\n                    '\"iam/common_config/file_service_api_config\"'\n                ) from exc\n```"
    ]
  },
  {
    "size": 9,
    "representative": "[nitpick] The import statement spans 5 lines and imports 10 different error message constants. Consider grouping related imports or using a more structured import approach to improve readability.",
    "top_paths": [
      "groups/src/error_codes.py",
      "groups/tests/test_groups_app.py",
      "users/src/service.py"
    ],
    "comments": [
      "[nitpick] This import statement spans multiple lines and imports many constants. Consider organizing these imports or using a wildcard import if all constants are needed, to improve readability.\n```suggestion\nfrom error_messages import *\n```",
      "[nitpick] The import statement spans multiple lines and imports many constants. Consider organizing these imports alphabetically or grouping them by functionality to improve readability.\n```suggestion\nfrom error_messages import DUPLICATE_ROLE_ID_ERROR, INVALID_TRN_FORMAT_ERROR, LOCAL_GROUP_VALIDATION_ERROR, \\\n    LOCAL_NESTED_GROUP_RESTRICTION, LOCAL_PARAMETER_NOT_ALLOWED_ERROR, NESTED_GROUP_RESTRICTION, \\\n    ONLY_GROUP_TRN_ERROR, ON_PARAMETER_MISSING_ERROR, RESOURCE_NOT_FOUND_ERROR, \\\n    ROLE_ASSIGNMENT_GROUP_DELETE_ERROR, ROLE_ASSIGNMENT_GROUP_NOT_FOUND_ERROR, ROLE_ASSIGNMENT_GROUP_UPDATE_ERROR, \\\n    TRANSACTION_FAILURE\n```",
      "The RESOURCE_NOT_FOUND_ERROR constant is used directly in the enum value, but this constant was updated in error_messages.py to include a placeholder. Ensure the format string in error_messages.py matches the expected usage pattern here.\n```suggestion\n        RESOURCE_NOT_FOUND_ERROR,  # expects a formatting argument, e.g., resource id\n        \"This error occurs when the resource/entity with the given id is not found or available\"\n```",
      "[nitpick] The import statement uses escaped line breaks, which makes it harder to read and maintain. Consider organizing these imports in a more readable format, such as using parentheses for multi-line imports or reorganizing into multiple import statements.\n```suggestion\nfrom error_messages import (\n    RESOURCE_NOT_FOUND_ERROR,\n    DUPLICATE_ROLE_ID_ERROR,\n    INVALID_TRN_FORMAT_ERROR,\n    ON_PARAMETER_MISSING_ERROR,\n    ROLE_ASSIGNMENT_GROUP_NOT_FOUND_ERROR,\n    ONLY_GROUP_TRN_ERROR,\n    ROLE_ASSIGNMENT_GROUP_DELETE_ERROR,\n    ROLE_ASSIGNMENT_GROUP_UPDATE_ERROR,\n    TRANSACTION_FAILURE,\n    LOCAL_NESTED_GROUP_RESTRICTION,\n    NESTED_GROUP_RESTRICTION,\n    LOCAL_GROUP_VALIDATION_ERROR,\n    LOCAL_PARAMETER_NOT_ALLOWED_ERROR,\n)\n```",
      "[nitpick] The import statement spans multiple lines and imports many error message constants. Consider organizing these imports or evaluating if all these constants are still needed, since the new ErrorCode enum appears to define its own error messages that may replace some of these imported constants.\n```suggestion\nfrom error_messages import RESOURCE_NOT_FOUND_ERROR\n```",
      "[nitpick] The import statement spans 5 lines and imports 10 different error message constants. Consider grouping related imports or using a more structured import approach to improve readability.",
      "The RESOURCE_NOT_FOUND_ERROR constant is used directly in the enum value, but the error_messages.py file shows this constant has been updated to include a format placeholder for the ID. This could lead to inconsistent error message formatting.\n```suggestion\n        RESOURCE_NOT_FOUND_ERROR,  # This string contains a '{}' placeholder for the resource ID\n```",
      "The import statement is duplicated from lines 980-982. Consider moving these imports to the top of the file or creating a helper function to avoid code duplication.",
      "Duplicate import. `ErrorCode` and `raise_application_error` are already imported at line 70. Remove this duplicate import.\n```suggestion\n\n```"
    ]
  },
  {
    "size": 9,
    "representative": "The log message should include contextual information (account_id, user_id) for better observability, consistent with the logging pattern used elsewhere in this handler. For example, lines 61-63 include account_id and user_id in the log message. Consider updating this log message to include similar context: `logger.info(f\"Relation for account {account_id} user {relation.user_id} contains no valid CDH roles. No Associations will be deleted.\")`\n```suggestion\n            logger.info(\n                f\"Relation for account {account_id} user {relation.user_id} contains no valid CDH roles. \"\n                \"No Associations will be deleted.\"\n            )\n```",
    "top_paths": [
      "users/src/dal.py",
      "scripts/account_membership_correction/account_membership_correction.py",
      "roles/src/service.py",
      "events/src/ingestions/consumers/central/user_handler.py",
      "reverse_sync/iam_to_cdh_sync_user_roles/src/user_roles_to_cdh_sync/account_user_controller/create.py"
    ],
    "comments": [
      "The warning message is unclear. It says \"Invalid relation\" but this appears to be a valid case where the account_id matches the expected_account_id. The message should clarify why this is considered invalid or if this is actually a valid case that's being logged for informational purposes.\n```suggestion\n                    logger.info(f\"Valid relation found for {application_id} with expected account {account_id} (source_ref: users) - logging for informational purposes.\")\n```",
      "The log level was changed from `error` to `info`, but this represents a significant behavioral change. Since relations not being found was previously treated as an error condition, consider whether this should remain an error-level log or if the business logic change is intentional.\n```suggestion\n            logger.error(f\"Relation not found for application : {source_application_id} and \"\n```",
      "The log message should include the `ref` parameter for better debugging context. Consider changing to `f\\\"resource not found for id: {id}, ref: {ref}\\\"`.\n```suggestion\n            logger.info(f\"resource not found for id: {id}, ref: {ref}\")\n```",
      "The log message in the `NotFoundException` path omits the new `include_expired` flag, which can make debugging confusing when callers explicitly request expired relations. Include `include_expired` in the log context (and/or switch to structured logging fields if that\u2019s the project pattern).\n```suggestion\n            logger.info(\n                f\"relation not found for source_id: {source_id}, source_ref: {source_ref}, \"\n                f\"target_id: {target_id}, target_ref: {target_ref}, relationship: {relation_name}, \"\n                f\"include_expired: {include_expired}\"\n            )\n```",
      "Vague log message missing context. The log message \"setting {org_id} for incoming id\" is unclear and doesn't specify what \"incoming id\" refers to (likely user_id). Consider making the message more descriptive, such as \"Setting orgIds {org_id} for user {user_id}\" to improve traceability and debugging.\n```suggestion\n        logger.info(f\"Setting orgIds {org_id} for user {user_id} (account {account_id})\")\n```",
      "The log messages across create, delete, and update handlers should include contextual information (account_id, user_id) for better observability, consistent with the logging pattern used elsewhere in these handlers. For example, lines 66-69 in create.py include account_id and user_id in the log message. Consider updating this log message to include similar context: `logger.info(f\"Relation for account {account_id} user {relation.user_id} contains no valid CDH roles. No Associations will be created.\")`\n```suggestion\n            logger.info(\n                f\"Relation for account {account_id} user {relation.user_id} \"\n                f\"contains no valid CDH roles. No Associations will be created.\"\n            )\n```",
      "The log message should include contextual information (account_id, user_id) for better observability, consistent with the logging pattern used elsewhere in this handler. For example, lines 61-63 include account_id and user_id in the log message. Consider updating this log message to include similar context: `logger.info(f\"Relation for account {account_id} user {relation.user_id} contains no valid CDH roles. No Associations will be deleted.\")`\n```suggestion\n            logger.info(\n                f\"Relation for account {account_id} user {relation.user_id} contains no valid CDH roles. \"\n                \"No Associations will be deleted.\"\n            )\n```",
      "The log message should include contextual information (account_id, user_id) for better observability, consistent with the logging pattern used elsewhere in this handler. For example, lines 85-88 include account_id and user_id in the log message. Consider updating this log message to include similar context: `logger.info(f\"No valid CDH role changes found for account {account_id} user {new_relation.user_id}. No Association changes will be made.\")`\n```suggestion\n            logger.info(\n                f\"No valid CDH role changes found for account {account_id} user {new_relation.user_id}. \"\n                f\"No Association changes will be made.\"\n            )\n```",
      "`logger.warning` concatenates an f-string with a plain string, so `{cdh_site.cdh_account_id}` will be logged literally instead of interpolated. Make the second string an f-string (or format the full message as a single f-string) so the account id is included correctly.\n```suggestion\n                f\"in account: {cdh_site.cdh_account_id}. Creating...\"\n```"
    ]
  },
  {
    "size": 9,
    "representative": "The output references `RegionalPublishEventsHandler`, but this resource appears to have been removed in the same change that deleted `V2RegionalPublishEventsHandler`. This will cause a CloudFormation deployment failure due to a missing resource reference.",
    "top_paths": [
      "events/template.yml",
      "setup/infra/migration/cdh-accounts-live-synchronizer/template.yaml",
      "reverse_sync/template.yaml",
      "devices/template.yaml"
    ],
    "comments": [
      "The dependency reference has been changed from 'FirehoseStreamTargetS3Bucket' to 'V2FirehoseStreamTargetS3Bucket', but this resource name doesn't exist in the visible portion of the template. This will cause a CloudFormation deployment failure.\n```suggestion\n    DependsOn: FirehoseStreamTargetS3Bucket\n```",
      "This line references `IamToCdhAccountsSyncQueue` which is now commented out, causing a CloudFormation deployment failure due to an undefined resource reference.\n```suggestion\n  IamToCdhAccountsSyncQueue:\n    Type: AWS::SQS::Queue\n    Properties:\n      QueueName: !Sub iam-to-cdh-sync-accounts-${Environment}-uw2\n      VisibilityTimeout: 60\n      RedrivePolicy:\n        deadLetterTargetArn: !GetAtt IamToCdhAccountsSyncDLQ.Arn\n        maxReceiveCount: 5\n```",
      "The `DataRegion` parameter is referenced but not defined in the Parameters section of this template. This will cause a CloudFormation stack creation/update failure. Verify that the `DataRegion` parameter exists in the Parameters section (line 53 onwards), or add it if missing.",
      "The environment variable references `!Ref DataRegion` but there is no corresponding `DataRegion` parameter defined in the Parameters section (lines 59-61 show the end of Parameters). This will cause a CloudFormation validation error during stack deployment. Either add a `DataRegion` parameter in the Parameters section or update this reference to use an existing parameter.",
      "The `DataRegion` parameter is referenced but not defined in this template. Add a `DataRegion` parameter in the Parameters section to avoid CloudFormation deployment failures.",
      "The output references `AccountSourceSystemHandler`, but this resource appears to have been removed in the same change that deleted `V2AccountSourceSystemHandler`. This will cause a CloudFormation deployment failure due to a missing resource reference.",
      "The output references `UserAccountsMembershipHandler`, but this resource appears to have been removed in the same change that deleted `V2UserAccountsMembershipHandler`. This will cause a CloudFormation deployment failure due to a missing resource reference.",
      "The output references `CentralPublishEventsHandler`, but this resource appears to have been removed in the same change that deleted `V2CentralPublishEventsHandler`. This will cause a CloudFormation deployment failure due to a missing resource reference.",
      "The output references `RegionalPublishEventsHandler`, but this resource appears to have been removed in the same change that deleted `V2RegionalPublishEventsHandler`. This will cause a CloudFormation deployment failure due to a missing resource reference."
    ]
  },
  {
    "size": 9,
    "representative": "The tags specify 'dev' environment but this is a qa2 configuration file. The tier and environment should be 'qa2' to match the stack configuration on line 3.\n```suggestion\nauth_tags={ tier = \"qa2\", Project = \"iam\", environment = \"qa2\", product = \"iam\", Name=\"iam-sync-gateway-authorizer-qa2-eu-gb1\" }\n```",
    "top_paths": [
      "setup/infra/terraform_iac/prerequisite/sync_authorizer_lambda/configuration/qa2/eu-gb/eu-west-2/qa2.tfvars",
      "setup/infra/sam/configuration/dev5/us/us-west-2/graphql-env.json",
      "setup/infra/sam/configuration/dev6/us/us-west-2/graphql-env.json",
      "setup/infra/configs/terraform/configuration/integ/us/us-west-2/integ.tfvars",
      "setup/infra/terraform_iac/prerequisite/sync_authorizer_lambda/configuration/qa1/eu-gb/eu-west-2/qa1.tfvars"
    ],
    "comments": [
      "The Environment value should be 'dev5' to match the folder structure and maintain consistency with other dev environments (dev1, dev2, dev3, dev4, dev7).\n```suggestion\n  \"Environment\": \"dev5\",\n```",
      "The Environment value should be 'dev6' to match the folder structure and maintain consistency with other dev environments.\n```suggestion\n    \"Environment\": \"dev6\",\n```",
      "The stack variable is set to 'qatest' but should be 'integ' to match the environment name and file path.\n```suggestion\nstack                   = \"integ\"\n```",
      "The tags specify 'dev' environment but this is a qa2 configuration file. The tier and environment should be 'qa2' to match the stack configuration on line 3.\n```suggestion\nauth_tags={ tier = \"qa2\", Project = \"iam\", environment = \"qa2\", product = \"iam\", Name=\"iam-sync-gateway-authorizer-qa2-eu-gb1\" }\n```",
      "The tags specify 'dev' environment but this is a qa1 configuration file. The tier and environment should be 'qa1' to match the stack configuration on line 3.\n```suggestion\nauth_tags={ tier = \"qa1\", Project = \"iam\", environment = \"qa1\", product = \"iam\", Name=\"iam-sync-gateway-authorizer-qa1-eu-gb1\" }\n```",
      "The tags specify 'dev' environment but this is a qa configuration file. The tier and environment should be 'qa' to match the stack configuration on line 3.\n```suggestion\nauth_tags={ tier = \"qa\", Project = \"iam\", environment = \"qa\", product = \"iam\", Name=\"iam-sync-gateway-authorizer-qa-eu-gb1\" }\n```",
      "The environment tag is set to 'dev' but this is a qa2 configuration file. It should be 'qa2' to match the stack on line 3.\n```suggestion\nenvironment = \"qa2\"\n```",
      "The \"tier\" tag is set to \"dev\" but should be \"qa2\" to match the environment.\n```suggestion\nauth_tags={ tier = \"qa2\", Project = \"iam\", environment = \"qa2\", product = \"iam\", Name=\"iam-sync-gateway-authorizer-qa2-eu-gb1\" }\n```",
      "The stack value is set to \"qa1\" but this configuration file is for the qa2 environment. This should be \"qa2\" to match the environment name and maintain consistency with the file path.\n```suggestion\nstack                   = \"qa2\"\ncentral_table_name = \"iam-central-qa2\"\n```"
    ]
  },
  {
    "size": 9,
    "representative": "The expression `list(new.payload.get('references').keys())[0]` assumes there is always exactly one reference key. This assumption should be documented or validated. Consider extracting this to a named variable for clarity and add a comment explaining the single-reference assumption.\n```suggestion\n        # Assumption: new.payload[\"references\"] contains exactly one key (the address_id).\n        reference_keys = list(new.payload.get(\"references\", {}).keys())\n        if len(reference_keys) != 1:\n            raise ValueError(f\"Expected exactly one reference key in new.payload['references'], got {len(reference_keys)}: {reference_keys}\")\n        reference_key = reference_keys[0]\n        old_location = old.payload.get(\"references\", {}).get(reference_key)\n```",
    "top_paths": [
      "events/src/ingestions/consumers/central/handlers.py",
      "events/src/ingestions/consumers/central/user_handler.py"
    ],
    "comments": [
      "This code will raise a KeyError if `new.payload.get('references')` returns None. The `.keys()` method cannot be called on None. Add a null check or use a default empty dict: `old_location = old.payload.get('references', {}).get(list(new.payload.get('references', {}).keys())[0])`\n```suggestion\n        new_references = new.payload.get(\"references\", {})\n        reference_keys = list(new_references.keys())\n        old_location = None\n        if reference_keys:\n            old_location = old.payload.get(\"references\", {}).get(reference_keys[0])\n```",
      "This code will raise an IndexError if the references dict is empty. Before accessing `[0]`, verify that the keys list is not empty or handle the IndexError appropriately.\n```suggestion\n        new_references_keys = list(new.payload.get(\"references\", {}).keys())\n        old_location = None\n        if new_references_keys:\n            old_location = old.payload.get(\"references\", {}).get(new_references_keys[0])\n```",
      "[nitpick] The variable name `old_location` is misleading as it stores a reference value, not a location object. Consider renaming to `existing_reference` or `old_reference` to better reflect what it represents.\n```suggestion\n        existing_reference = old.payload.get(\"references\", {}).get(list(new.payload.get(\"references\").keys())[0])\n        if not existing_reference:\n```",
      "The expression `list(new.payload.get('references').keys())[0]` assumes there is always exactly one reference key. This assumption should be documented or validated. Consider extracting this to a named variable for clarity and add a comment explaining the single-reference assumption.\n```suggestion\n        # Assumption: new.payload[\"references\"] contains exactly one key (the address_id).\n        reference_keys = list(new.payload.get(\"references\", {}).keys())\n        if len(reference_keys) != 1:\n            raise ValueError(f\"Expected exactly one reference key in new.payload['references'], got {len(reference_keys)}: {reference_keys}\")\n        reference_key = reference_keys[0]\n        old_location = old.payload.get(\"references\", {}).get(reference_key)\n```",
      "That is the reference of the new address being added. We are building the new payload reference in the previous step. So there will be exactly one reference that is being added. That is basically the addressId being added, I have given as list(new.payload.get(\"references\").keys())[0] for consistency (in other places we are doing the same) and it was better understandable",
      "That is the reference of the new address being added. We are building the new payload reference in the previous step. So there will be exactly one reference that is being added",
      "This list will never be empty. That is the reference of the new address being added. We are building the new payload reference in the previous step. So there will be exactly one reference that is being added",
      "Missing cleanup of orgIds when all organizations are removed. When references becomes empty (all accounts removed or set to INACTIVE status), org_list will be an empty list, and due to the condition on line 150 (if org_list:), old_payload[\"orgIds\"] won't be updated. This means stale orgIds may remain in the user payload. Consider explicitly setting old_payload[\"orgIds\"] = [] when org_list is empty but the key exists, or removing the key entirely.\n```suggestion\n        old_payload[\"orgIds\"] = org_list\n    elif \"orgIds\" in old_payload:\n        old_payload[\"orgIds\"] = []\n```",
      "Inconsistent behavior when references is empty. The function only updates old_payload[\"references\"] if references is truthy (line 148-149), but an empty dict evaluates to False. This means that if all organization references are removed, the \"references\" key won't be updated in old_payload. Consider updating old_payload[\"references\"] = references unconditionally, or explicitly handling the empty case."
    ]
  },
  {
    "size": 9,
    "representative": "The test name `test_cancel_join_request` is too generic. Consider renaming to be more descriptive about what scenario is being tested, such as `test_cancel_join_request_when_no_pending_request_raises_error` to follow the pattern of other tests in the file.\n```suggestion\ndef test_cancel_join_request_when_no_pending_request_raises_error(mock_get_subject):\n```",
    "top_paths": [
      "groups/tests/test_service.py",
      "groups/tests/test_groups_app.py",
      "tests/automation/tests/groups/test_group_self_join_request_api.py"
    ],
    "comments": [
      "These patched methods (get_group_owners, validate_role_assignment_permission, get_user_details, send_email) are not called by the cancel_join_request flow. Looking at the service implementation (service.py lines 1836-1858), cancel_join_request only calls get_subject, _resource_committed_in_group, dal.get_update_relation_request, and do_and_handle_operations_transactionally. These unnecessary patches should be removed, and only the methods actually used should be patched.\n```suggestion\n    # Patch only the cancel_join_request method used by the handler\n    with patch(\n        \"service.GroupsService.cancel_join_request\",\n        return_value=FullUserToGroup(id=group_id, status=\"pending\"),\n    ):\n```",
      "The test is missing a mock for `do_and_handle_operations_transactionally` which is called in the cancel_join_request implementation (service.py line 1857). Without mocking this method, the test will attempt to execute the actual transactional operations, which will likely fail. Add: `service.do_and_handle_operations_transactionally = Mock()`\n```suggestion\n    service._resource_committed_in_group = Mock(return_value=(True, user_account_relation))\n    service.do_and_handle_operations_transactionally = Mock()\n```",
      "The test name `test_cancel_join_request` is too generic. Consider renaming to be more descriptive about what scenario is being tested, such as `test_cancel_join_request_when_no_pending_request_raises_error` to follow the pattern of other tests in the file.\n```suggestion\ndef test_cancel_join_request_when_no_pending_request_raises_error(mock_get_subject):\n```",
      "The cancel_join_group endpoint doesn't use the request body (see app.py lines 1043-1055). Including a body in this test is misleading and suggests the endpoint accepts a body when it doesn't. The body should be removed or set to an empty dict.\n```suggestion\n    lambda_event[\"body\"] = json.dumps({})\n```",
      "The test is calling `cancel_join_request(token_data, subject)` but the actual method signature in service.py is `cancel_join_request(group_id: str, token_data: TokenData)`. The method takes `group_id` as the first parameter and only `token_data` as the second parameter (it derives the subject internally). This test will fail at runtime.",
      "The `cancel_join_request` service method returns `None` (see service.py line 1836), but the test is patching it to return `FullUserToGroup(id=group_id, status=\"pending\")`. This patched return value is never used since the app.py handler doesn't use the return value and returns NO_CONTENT (204) status. The patch should return None to match the actual implementation.\n```suggestion\n    with patch(\"service.GroupsService.cancel_join_request\", return_value=None), \\\n```",
      "The test name `test_cancel_join_request_valid_case` is ambiguous. Consider renaming to be more descriptive about what scenario is being tested, such as `test_cancel_join_request_when_pending_request_exists` to follow the pattern of other tests in the file.\n```suggestion\ndef test_cancel_join_request_when_pending_request_exists(mock_get_subject):\n    \"\"\" status pending relation exists, should call get_relation and proceed with cancellation \"\"\"\n```",
      "The test name `test_cancel_join_request_invalid_case` is ambiguous. Consider renaming to be more descriptive about what scenario is being tested, such as `test_cancel_join_request_when_subject_is_not_user` to follow the pattern of other tests in the file.\n```suggestion\ndef test_cancel_join_request_when_subject_is_not_user(mock_get_subject):\n```",
      "The test name and title don't accurately reflect what this test is verifying. Based on the assertions (expecting CONFLICT status and \"User already has a pending request\" error), this test is verifying duplicate request handling, not just a basic self-join request. The title should be more descriptive, such as \"Test request self join in a group when user already has a pending request\" to match the actual behavior being tested.\n```suggestion\n        \"Test request self join in a group from a related account when user already has a pending request\"\n    )\n    def test_request_self_join_in_a_group_from_a_related_account_when_user_already_has_pending_request(\n```"
    ]
  },
  {
    "size": 9,
    "representative": "`DEFAULT_ROLE` is now treated as the literal role id (`get_env_var(\"DEFAULT_ROLE\")`), but the SAM environment configs currently pass an SSM path into the `DEFAULTROLE` parameter (e.g. `/iam/dev/default_role`). With this change, `DEFAULT_ROLE` would become the SSM path string, breaking comparisons like `role_id == DEFAULT_ROLE` and default-role insertion. Either update the deployment/SAM configuration to pass the actual role id, or reintroduce a lookup step (SSM/config manager) to resolve the id from the path.",
    "top_paths": [
      "groups/src/config.py",
      "events/src/async_processors/central_events/opt_in_requests_service.py",
      "users/src/config.py",
      "users/src/app.py",
      "groups/src/service.py"
    ],
    "comments": [
      "Only SSM parameters and flags were moved into config manager as of now. Role ids are still being added as environment variables. Since these role ids differ for each and every stack adding it in the common config file would not work.",
      "pl check if default role is still maintained in ssm. we should be using either env variable or config",
      "This change makes DEFAULT_ROLE equal to the raw environment variable value. In the current repo configuration, that env var is typically an SSM parameter name/path (e.g., setup/infra/sam/configuration/*/users-env.json sets DEFAULTROLE to \"/iam/<env>/default_role\"), and unit tests also set DEFAULT_ROLE to a key that was previously resolved via ssm_param_value. Without resolving via SSM, DEFAULT_ROLE will become a path/key instead of a role UUID, breaking role assignment logic. Either keep SSM resolution here or update the deployment/config inputs so DEFAULT_ROLE is the actual role id value everywhere it\u2019s set.",
      "DEFAULT_ROLE is now read directly from the environment without resolving the SSM parameter value. Given the repo\u2019s SAM parameter configs commonly pass an SSM path for DEFAULTROLE (e.g., \"/iam/<env>/default_role\"), this will cause DEFAULT_ROLE to be a path/key rather than a role UUID and will break endpoints that default roles when creating/updating users. Either revert to resolving via ssm_param_value or ensure all deployments set DEFAULT_ROLE to the literal role id value.",
      "This change makes `GROUP_OWNER_ROLE` the raw environment variable value, but current SAM env configuration appears to pass an SSM parameter name/path (e.g., `/iam/<env>/group_owner_role`). Without resolving via `ssm_param_value`, the service will write/check the SSM path string as a role ID (incorrect role assignments and deletion checks). Consider keeping SSM resolution, or supporting both by resolving only when the env var looks like an SSM path, and update deployment config accordingly.",
      "`DEFAULT_ROLE` is now taken directly from the environment, but the existing SAM configuration uses `DEFAULTROLE` as an SSM parameter path (e.g., `/iam/<env>/default_role`). Treating that path as a role ID will break default-role filtering/logic. Either keep resolving the env var through `ssm_param_value`, or add backward-compatible logic that resolves only when the env var value is an SSM path, and ensure the deployment parameters match the new expectation.",
      "`DEFAULT_ROLE` is now expected to be a concrete role id, but the `DEFAULTROLE` SAM parameter is (per existing `setup/infra/sam/configuration/**/roles-env.json` files) typically set to an SSM path like `/iam/<env>/default_role`. Without updating those configs, this template will inject an SSM path into `DEFAULT_ROLE` and the roles service will treat it as the role id. Update the SAM configuration values (or the template) so `DEFAULT_ROLE` receives the actual role id.",
      "`DEFAULT_ROLE` is now treated as the literal role id (`get_env_var(\"DEFAULT_ROLE\")`), but the SAM environment configs currently pass an SSM path into the `DEFAULTROLE` parameter (e.g. `/iam/dev/default_role`). With this change, `DEFAULT_ROLE` would become the SSM path string, breaking comparisons like `role_id == DEFAULT_ROLE` and default-role insertion. Either update the deployment/SAM configuration to pass the actual role id, or reintroduce a lookup step (SSM/config manager) to resolve the id from the path.",
      "`DEFAULTROLE` is now set to a raw role UUID, but the roles service currently derives `DEFAULT_ROLE` via `ssm_param_value(get_env_var(\"DEFAULT_ROLE\"))` (roles/src/service.py:65), which expects the env var to be an SSM parameter name/path. With this change the service will look up an SSM parameter named like the UUID and likely fail or return the wrong value. Update the service code to use `get_env_var(\"DEFAULT_ROLE\")` directly (no SSM lookup) or keep these config values as SSM paths if SSM resolution is still intended."
    ]
  },
  {
    "size": 8,
    "representative": "@jfrench-trimble Had a look this seems to be correct",
    "top_paths": [
      "setup/docker/mocks/mockserver.js",
      "accounts/src/accounts/dynamodb/account_writer.py",
      "accounts/src/accounts/models/cdh_sync/entities.py",
      "roles/src/dal.py",
      "applications/src/service.py"
    ],
    "comments": [
      "@jfrench-trimble I am not sure there a standard here. This is just my preference.",
      "@jfrench-trimble this has been removed",
      "Yeah, we need to revert this until @alexblum-trimble can complete the necessary changes so that this works",
      "@JDR-Trimble I have also received some more comments to remove this check from here and I have moved it out. Thanks",
      "@jfrench-trimble Sure, have moved this block of code into the helper",
      "@jfrench-trimble Had a look this seems to be correct",
      "@samoconnell @jfrench-trimble it looks like this is the approach we are going with FYI.",
      "@dhineshkumar-d-trimble check this looks valid"
    ]
  },
  {
    "size": 8,
    "representative": "There is a plan to check where are we using or going to use this metric data. For now remove this.",
    "top_paths": [
      "devices/src/app.py",
      "groups/src/app.py",
      "tam-authz/src/app.py",
      "users/src/app.py",
      "roles/src/app.py"
    ],
    "comments": [
      "Observation: This can be changed to info to get the performance metrics. This is not mandatory to be done now.",
      "@Dhivyadharshini-T we should also add this metric as well as part of the decorator that we discussed earlier it will avoid repeated things",
      "There is a plan to check where are we using or going to use this metric data. For now remove this.",
      "We don't use or interpret this data anywhere. I agree other BLU's also has this metric but I don't know is this really needed or not.\r\n@shaju-g @Sweatha1609 can you take a stab & see where this metrics is a must or not?",
      "@aravinth-karthikeyan - even i had this question, i think we need to remove this (overall) and have to add some more details to the metrics",
      "I have removed add_metric for these two new APIs.",
      "can we also remove Metrics import ?",
      "remove this metrics capturing annotation"
    ]
  },
  {
    "size": 8,
    "representative": "if the account doesn't exist, that will cause an error when we try to see if the creator has permissions to create a new account user.",
    "top_paths": [
      "users/src/service.py",
      "accounts/src/accounts/service/users/create.py",
      "devices/src/device_service.py",
      "applications/src/service.py"
    ],
    "comments": [
      "Is it possible that the account is what does not exist and that is what raised the error?",
      "if the account doesn't exist, that will cause an error when we try to see if the creator has permissions to create a new account user.",
      "I would advise the error message to be changed to something like this:\r\n\r\n\"A short code needs to be configured for the account to allow the creation of users without an email address.\"\r\n\r\nThis gives an idea of what action needs to be taken.\r\n\r\ncc: @aravinth-karthikeyan @Dhivyadharshini-T",
      "this if check is not required. if account does not exist we will get notfound exception",
      "Suggestion for this error message - Cannot update email as this user was previously created without an email.\r\nGet it reviewed with Ezhil and modify as needed",
      "\"User already exists\" similar to create user error message ??",
      "@DharmarajanRajendran For account not found cases, we are throwing unauthorized as it was discussed not to throw notfound exceptions",
      "this if check not needed. dao returns NotFound exception if given application is not found"
    ]
  },
  {
    "size": 8,
    "representative": "```suggestion\r\n        run: echo \"WORKING_DIRECTORY=${{ matrix.working_directory }}\" >> $GITHUB_ENV\r\n```",
    "top_paths": [
      ".github/workflows/pr-standards-common.yaml",
      "setup/infra/configs/ansible/playbook/roles/decrypt_secret/tasks/main.yml"
    ],
    "comments": [
      "```suggestion\r\n        run: echo \"WORKING_DIRECTORY=${{ matrix.working_directory }}\" >> $GITHUB_ENV\r\n```",
      "```suggestion\r\n        run: echo \"COVERAGE_THRESHOLD=${{ matrix.coverage_threshold }}\" >> $GITHUB_ENV\r\n```",
      "```suggestion\r\n          coverage_threshold : ${{ env.COVERAGE_THRESHOLD }}\r\n```",
      "```suggestion\r\n          working_directory  : ${{ env.WORKING_DIRECTORY }}\r\n```",
      "```suggestion\r\n          working_directory: ${{ env.WORKING_DIRECTORY }}\r\n```",
      "```suggestion\r\n          echo \"FILTERED_MATRIX=$filtered_matrix\" >> $GITHUB_ENV\r\n```",
      "```suggestion\r\n          echo \"Filtered matrix from environment: ${{ env.FILTERED_MATRIX }}\"\r\n```",
      "Using a deep relative traversal (../../../../../) is brittle; prefer using {{ playbook_dir }} or a well-defined root variable to avoid breakage if directory structure changes.\n```suggestion\n    paths: \"{{ playbook_dir }}\"\n```"
    ]
  },
  {
    "size": 8,
    "representative": "update poetry.lock for this change in all places wherever applicable",
    "top_paths": [
      "tests/automation/poetry.lock",
      "groups/pyproject.toml",
      "users/poetry.lock",
      "applications/poetry.lock",
      "authorization/pyproject.toml"
    ],
    "comments": [
      "update poetry.lock for this change in all places wherever applicable",
      "@nonoratoDEV, Please remove poetry.lock from the branch",
      "poetry.lock files are meant to be committed. Why should it be removed?",
      "I see the lock file is generated with 2.0.0. Are we going to upgrade poetry version to 2.0.0?\r\ncc: @Sweatha1609 @skpalani",
      "Could you downgrade the poetry version to 1.8.2 itself",
      "I am not sure if it gets in the poetry's test environment. I have added as it was failing.",
      "The repository's onboarding instructions recommend using Poetry version 1.8.2 for consistency (`pip install poetry==1.8.2`). Installing Poetry without specifying a version may lead to inconsistencies across environments and builds.",
      "`poetry install --with test`\n\nwould be better I guess"
    ]
  },
  {
    "size": 8,
    "representative": "Should include return type hint and docstring on methods.",
    "top_paths": [
      "accounts/src/accounts/models/database/iam/payload.py",
      "accounts/cdh-migration/scripts/export/cdh_iam_address_migration.py",
      "health/src/health_monitoring.py",
      "common/src/db/ddb_layer.py",
      "events/src/ingestions/consumers/central/handlers.py"
    ],
    "comments": [
      "Types are already defined by type hints and don't need to be in the docstring",
      "Need type hints on method signature and return types.",
      "Should include return type hint and docstring on methods.",
      "Add missing type hints and docstring to methods.",
      "Add type hints in method signatures and return types throughout",
      "Add missing type hints to method signatures and returns.",
      "add return type hint and method docstring please",
      "for better understanding, mention the return type hint and parameter type hint for all methods"
    ]
  },
  {
    "size": 8,
    "representative": "If that is the case please check with respective owners of this and rename it to simply with \"application_id\" / \"applicationId\"",
    "top_paths": [
      "common/src/common/audit_logging/audit_logging_service.py",
      "applications/tests/test_app.py",
      "applications/src/app.py",
      "tests/automation/README.md",
      "groups/src/dal.py"
    ],
    "comments": [
      "both application id and entity id are same here . why we need both ?",
      "plz rename the `client_application` to either `client_id` or `application_id` because in normal oauth world both client and app are same.",
      "could you plz rename the key as well with \"application_id\"",
      "If that is the case please check with respective owners of this and rename it to simply with \"application_id\" / \"applicationId\"",
      "Client id and app id both are aliases. So make it as either <client_id> or <application_id>. Plz replace it other places as well.",
      "Please add CLIENT_ID1, CLIENT_SECRET1 AND APP_NAME1",
      "This should be application_id and not user_id. Please update.",
      "i see same 'application_id' is being passed. same for entity_id as well"
    ]
  },
  {
    "size": 8,
    "representative": "During \"on\" application case, we don't need to consider the account, the roles will be assigned directly to an application. \r\nusers -> payload {roleids} -> application",
    "top_paths": [
      "roles/src/service.py",
      "roles/src/dal.py",
      "common/src/common/context/context_graph_dal.py",
      "groups/src/dal.py",
      "organizations/src/single_tier_hierarchical_strategy.py"
    ],
    "comments": [
      "we are using \"role\" in the user to account relation ??",
      "we are retrieving the account roles only if we have account id in the \"on\" param, what case needs this logic ?",
      "this is relationship between user and group. \r\nroles will be in account and group.",
      "in future, if we have a case where application context also require group based roles. that time. \r\nOn will have application. and no account id will be passed. so we will construct accountId from userId. and get roles from account-group relation.",
      "During \"on\" application case, we don't need to consider the account, the roles will be assigned directly to an application. \r\nusers -> payload {roleids} -> application",
      "we are not considering roles assigned to account/application through a group ? do we need to add that too ?",
      "better to handle this now. in future, other identities (application, device) will have relation with the account with role similar to user",
      "Role doesn't come here between org and account. Can be empty payload."
    ]
  },
  {
    "size": 8,
    "representative": "can we use the multi_authorization_check here?",
    "top_paths": [
      "users/src/service.py",
      "applications/src/service.py",
      "groups/src/service.py",
      "devices/src/device_transfer_service.py"
    ],
    "comments": [
      "Just reused existing authorization_check function & the existing function raises an ApplicationError with 403 status in case of unauthorized & no boolean is returned ...",
      "multi authorization check expects account id as one single string value. but we are passing comma separated values. this may not work.",
      "can we do this additional authorization in parallel? refer multi_authorization_check function in users service",
      "The authorization_check call uses positional arguments for optional parameters (entity_id=None, entity_type=None) which makes the code less readable and error-prone. Consider using keyword arguments for all optional parameters to improve clarity and maintainability.",
      "can we do multi_authorization_check in parallel (done in other blus)",
      "can we use the multi_authorization_check here?",
      "Correct me if I'm wrong but `multi_authorization_check` requires that all authorization checks pass. However here we just need one permission or the other.",
      "`__authorize` always passes `[resolved_account_id]` into `check_permission_helper`. When `account_id` is `None`/blank (e.g., `transfer_device()` can call `__check_transfer_permissions()` with `current_account_id=None` for devices with no current relation), this becomes `[None]`/`[\"\"]`, which is inconsistent with other call sites that use `[]` for unscoped checks and can lead to incorrect authorization decisions. Consider changing `account_id` to `Optional[str]`, and either short-circuit to `False` when it's missing or pass `[]`/`None` as `resource_account_id` as appropriate."
    ]
  },
  {
    "size": 8,
    "representative": "The walrus operator assignment and comparison are combined incorrectly. This assigns the boolean result of the comparison to ENVIRONMENT, not the environment value. Should be: `if (ENVIRONMENT := os.getenv('ENVIRONMENT')) == 'stage':`\n```suggestion\nif (ENVIRONMENT := os.getenv(\"ENVIRONMENT\")) == \"stage\":\n```",
    "top_paths": [
      "setup/infra/migration/data-synchronizer/layers/loggers.py",
      "tests/automation/tests/sync/test_cdh_iam_sync.py",
      "events/src/ingestions/file_service_ingestion/event_producer.py",
      "common/src/db/iam_database.py",
      "setup/infra/configs/vault_encrypt_decrypt_script.sh"
    ],
    "comments": [
      "why dont we try the if condition,\r\n```\r\nif os.getenv('ENVIRONMENT', \"\") == \"local\"\r\n```",
      "assign the `os.getenv()` into a common variable and use it wherever it required.",
      "The walrus operator assignment and comparison are combined incorrectly. This assigns the boolean result of the comparison to ENVIRONMENT, not the environment value. Should be: `if (ENVIRONMENT := os.getenv('ENVIRONMENT')) == 'stage':`\n```suggestion\nif (ENVIRONMENT := os.getenv(\"ENVIRONMENT\")) == \"stage\":\n```",
      "The environment variable should be converted to a boolean. Currently it's a string that will be compared to the string 'true' later, but if the env var is missing, it will be None and the comparison will fail. Consider using a boolean conversion or provide a default value.\n```suggestion\nALLOW_MULTIPLE_ACCOUNT_OWNERS = os.getenv(\"ALLOW_MULTIPLE_ACCOUNT_OWNERS\", \"false\").lower() == \"true\"\n```",
      "Error message refers to 'stg' but the script checks for 'stage' in the conditional on line 30. The message should say 'dev,stage,prod' to match the actual logic.\n```suggestion\n  echo \"Please mention any of these environment name - dev,stage,prod\"\n```",
      "Add spaces around the '==' operators for PEP 8 compliance: `if environment == 'prod' or environment == 'stage':`\n```suggestion\n                            if environment == \"prod\" or environment == \"stage\":\n```",
      "Import `getenv` from `os` at the module level follows Python conventions, but consider using the existing environment configuration pattern in this codebase. Check if there's a centralized config module (often in `common/src/config/`) that should be used instead of direct `os.getenv` calls for consistency with the rest of the codebase.\n```suggestion\n\n```",
      "Missing the `project` variable that is present in the stage configuration. The stage configuration file includes `project = \"iam\"` on line 8, but this prod configuration omits it. Ensure both stage and prod configurations have consistent variable definitions.\n```suggestion\nenvironment = \"prod\"\nproject = \"iam\"\n```"
    ]
  },
  {
    "size": 8,
    "representative": "The code calls `response.json().items` twice - once to assign to `items` and potentially again inside the conditional check if the earlier check passes but `items` is somehow falsy. More importantly, if the response status is not in the 200-299 range (already checked above), calling `response.json()` could throw an error or return unexpected results. Consider storing the parsed JSON response before accessing its properties.",
    "top_paths": [
      "tests/performance/k6/scripts/accounts.js",
      "tests/performance/k6/scripts/users.js",
      "tests/automation/tests/sync/test_syncUsers.py",
      "tests/automation/tests/accounts/search_account/test_search_account_by_owner_email.py",
      "common/src/common/invitation/invitation_wrapper.py"
    ],
    "comments": [
      "Assign the response.json() into a variable and use it where ever it requires. similarly for other key data like `metadata` from both dicts like response and update_user_via_sync_endpoints_response also.\r\nBut fine now. Plz refactor this at later PRs.",
      "Assign response.json() to some variable and use it. Same for above methods too.",
      "The code attempts to parse JSON from the response without handling potential JSON decode errors. If the response contains invalid JSON, this will raise a JSONDecodeError. Consider wrapping the response.json() call in a try-except block.",
      "The check does not validate if the response body can be parsed as JSON before calling `r.json()`. If the API returns a non-JSON response (e.g., 500 error with HTML), this will throw an error and fail the test unexpectedly. Consider using `r.json('items')` or adding a try-catch, or checking `r.body` first.\n```suggestion\n            'response contains items': (r) => {\n                try {\n                    const json = r.json();\n                    return json && json.hasOwnProperty('items');\n                } catch (e) {\n                    // Optionally log the error for debugging\n                    // console.error('Failed to parse JSON:', e, r.body);\n                    return false;\n                }\n            }\n```",
      "The code calls `response.json().items` twice - once to assign to `items` and potentially again inside the conditional check if the earlier check passes but `items` is somehow falsy. More importantly, if the response status is not in the 200-299 range (already checked above), calling `response.json()` could throw an error or return unexpected results. Consider storing the parsed JSON response before accessing its properties.",
      "`response.json()` is called multiple times per iteration (inside `check` and again when reading `links`), which repeatedly parses JSON and adds overhead in a performance test. Parse once (e.g., `const body = response.json()`), use it for both `check` and `links`, and consider removing `status` if you don't use it.",
      "The `response contains items` check calls `r.json()` unconditionally. If the endpoint returns a non-200 with a non-JSON body (common for errors), `r.json()` can throw and break the k6 iteration. Gate JSON parsing on `r.status === 200`, or use a safer access pattern (e.g., `r.json('items')` with a status check).",
      "The `check()` assertion `'response contains id': (r) => r.json().hasOwnProperty('id')` can throw if the response body isn\u2019t valid JSON (e.g., on non-200 errors where the API returns HTML/text). Since k6 evaluates all check predicates (it doesn\u2019t short-circuit on the status check), this can fail the whole iteration unexpectedly. Guard the JSON parsing (e.g., check content-type and/or wrap JSON parsing safely) before asserting on `id`."
    ]
  },
  {
    "size": 8,
    "representative": "@raahavm after computing the cert id we are validating if the computed cert id is one of the associated certs of device , for that reason we are getting all the certificates of device from device certificate relation",
    "top_paths": [
      "devices/src/device_service.py",
      "devices/src/device_certificate_dal.py",
      "devices/src/devices_sync_handler.py",
      "setup/infra/migration/data-synchronizer/layers/transformers.py",
      "setup/infra/migration/data-synchronizer/stream_handler/mutations.py"
    ],
    "comments": [
      "Our certificate route is devices/{deviceId}/certificates. As part of create certificate validation, we are checking if the device exists in IAM before proceeding.",
      "@Muskan-Trimble CertificateId is computed here, what is the reason for fetching all the certificates?",
      "@raahavm after computing the cert id we are validating if the computed cert id is one of the associated certs of device , for that reason we are getting all the certificates of device from device certificate relation",
      "@raahavm we are not directly dumping the request model to Device entity as our request model requires processing before dumping. for an instance certificate id computation from finger print. Also the incoming request's metadata payload is expected to have fingerprint along with cert id but the device entity metadata has cert id alone",
      "@Muskan-Trimble PR comment not addressed. When certificateId is retrieved why list is required?",
      "@Muskan-Trimble You have primary key of certificateId and deviceId, why do you need to list all relations\nInstead fetch the relation of this particular certificate, list is unnecessary",
      "@ragulkarthi Before deletion, validate whether device present in IAM",
      "The comment 'Only build central update for device certificates relation records' is misleading because the condition now excludes both devices and device certificates. The comment should be updated to accurately reflect that this condition filters out device and device unique key records, allowing only device certificate relation records to proceed."
    ]
  },
  {
    "size": 8,
    "representative": "The functions was rewritten to avoid if/elif giant block",
    "top_paths": [
      "tests/automation/tests/sync/test_cdh_sync_account_query.py",
      "accounts/src/accounts/models/meta.py"
    ],
    "comments": [
      "These repeated if, else checks can be encapsulated in a helper function.",
      "These all seem to do the same thing.  Looking at the overall code block, maybe we could revise this to a `match` statement instead of a giant `if/elif` block.  I think `match` will make the block of code easier to read overall as well.",
      "I think that for every different scenarios, there should be a separate function, this will make it more readable and easier to maintain, avoiding many duplicated if-elif conditions",
      "We should create a function for each scenario, this way, we will avoid nested if-elif and it will be more readable and more maintainable.",
      "The functions was rewritten to avoid if/elif giant block",
      "It was removed and rewritten with match as suggested, it is now more readable and fluent, and got rid of the giant if/elif block",
      "This function was rewritten to get rid of the if/elif giatn block",
      "The functios was rewritten to get rid of the giant if/elif block"
    ]
  },
  {
    "size": 8,
    "representative": "Move the common dict to common utils or try to use the existing dictionaries if those already avaiable",
    "top_paths": [
      "devices/src/device_service.py",
      "organizations/script/organization_seeding/ingestion_script.py",
      "common/src/common/invitation/invitation_wrapper.py",
      "groups/src/apicloud_group_sync.py",
      "groups/src/app.py"
    ],
    "comments": [
      "Better to move fixed dictionaries to a constant as mentioned above.",
      "moved these dictionaries to constants.",
      "These key value items are repeating. Can we make it as common response dicts and call it here like below,\r\nCOMMON_RESPONSE = {\"org_id\": None, \"org_details\": None, \"leadspace_data\": None}\r\n\r\n`return {**COMMON_RESPONSE, \"enrichment_status\": \"leadspace_failed_unknown\"}` similarly in other places.",
      "Move this dict to outside this method.",
      "Better to move the common key value pairs into a dict and use **common_responses",
      "Move the common dict to common utils or try to use the existing dictionaries if those already avaiable",
      "i think it could be worthwhile to change this from dict to a more descriptive type",
      "To be safe that this won't break any current dict usages, I think we can do this once the current usages are refactored."
    ]
  },
  {
    "size": 8,
    "representative": "we should move the send_invitation outside of is_consent_required function. seems like this function holding multiple responsibilities. caller should call  send_invitation based on the boolean return value of is_consent_required",
    "top_paths": [
      "groups/src/service.py",
      "users/src/service.py"
    ],
    "comments": [
      "This function should return a boolean value indicating whether consent is required or not. construct the status payload in the calling function based on this boolean value",
      "we should move the send_invitation outside of is_consent_required function. seems like this function holding multiple responsibilities. caller should call  send_invitation based on the boolean return value of is_consent_required",
      "When the consent is required the target account details are anyways need to be returned from here.\r\n\r\nMay be I can rename the function as \"check_consent_requirement\" if the name is misleading..",
      "This should be done outside. dont combine several functionalities into a single function. is_consent_required should return a boolean as named",
      "dont combine several responsibilities hidden under a function. As named, is_consent_required should return a boolean indicating consent is required or not. other functionalities based on that should be in a different function",
      "The try block is missing the condition check for `transaction_data.send_invite is True`. The original conditional logic has been removed, causing invitation attempts even when not requested.",
      "The docstring parameter name `send_invite` should be `send_consent` to match the actual parameter name in the function signature.\n```suggestion\n        :param send_consent: boolean to indicate if consent needs to be sent\n```",
      "The separation of the `send_invite` and `user.default` conditions changes the control flow significantly. Ensure there are test cases covering: (1) `send_invite=True` with `user.default=False`, (2) `send_invite=False` with `user.default=True`, and (3) both conditions being true simultaneously to verify expiry is set correctly and default account is updated as expected."
    ]
  },
  {
    "size": 8,
    "representative": "Use structured logging with separate parameters instead of f-string formatting. Replace with: `logger.info('Updating subscription status, relation %s, client_application_id %s, meta %s target app %s', subscription_relation, client_application_id, meta, target_application_id)`\n```suggestion\n    logger.info(\"Updating subscription status, relation %s, client_application_id %s, meta %s target app %s\", subscription_relation, client_application_id, meta, target_application_id)\n```",
    "top_paths": [
      "applications/src/apicloud_app_sync.py",
      "tests/automation/tests/events/test_events.py",
      "users/src/identities_wrapper.py",
      "users/tests/test_users_app.py",
      "tests/automation/tests/devices/test_device_integration_user_context.py"
    ],
    "comments": [
      "Use structured logging with separate parameters instead of f-string formatting. Replace with: `logger.info('Updating subscription status, relation %s, client_application_id %s, meta %s target app %s', subscription_relation, client_application_id, meta, target_application_id)`\n```suggestion\n    logger.info(\"Updating subscription status, relation %s, client_application_id %s, meta %s target app %s\", subscription_relation, client_application_id, meta, target_application_id)\n```",
      "Use structured logging with separate parameters instead of f-string formatting. Also fix the spacing around the comma. Replace with: `logger.info('Updating subscription relation payload, relation %s, product_payload %s', subscription_relation, product_payload)`\n```suggestion\n    logger.info('Updating subscription relation payload, relation %s, product_payload %s', subscription_relation, product_payload)\n```",
      "Nested quotes are incorrectly formatted. The inner quotes should be single quotes: `f\\\"Generating token for Application: {setup_client_details['APP_NAME']}...\\\"`\n```suggestion\n    logger.info(f\"Generating token for Application: {setup_client_details['APP_NAME']}...\")\n```",
      "Using `print()` for logging is inconsistent with the existing logging pattern. Should use `logger.info()` instead, following the pattern established elsewhere in the method.\n```suggestion\n                logger.info(\"User deleted successfully\")\n```",
      "The log message contains a grammatical error. 'No of subscriptions' should be 'Number of subscriptions'.\n```suggestion\n    logger.info(\"Number of subscriptions for the product against the application %s\", len(client_and_target_relations_for_product.items))\n```",
      "Logger.debug() takes a format string and arguments separately. This should be `logger.debug('created user: %s', user)` to properly format the log message.\n```suggestion\n    logger.debug(\"created user: %s\", user)\n```",
      "Logger name contains underscore inconsistent with hyphen pattern used elsewhere. Change \"User_Token\" to \"User-Token\" to match the naming pattern in app_context file.\n```suggestion\nlogger = file_logger(name=\"Devices-Tests-Using-User-Token\", level=LogLevel.INFO)\n```",
      "Print statements instead of logging. The script uses print() statements for output throughout (lines 90, 112, 151, 152, 157, 182, etc.) instead of the logging framework that is imported on line 23. This makes it difficult to control log levels and integrate with logging systems. Consider using logger.info(), logger.error(), etc. instead of print().\n```suggestion\n        logger.error(\n            \"Error retrieving users for account %s: %s\",\n            account_id,\n            e,\n            exc_info=True,\n        )\n```"
    ]
  },
  {
    "size": 8,
    "representative": "Redundant import - `uuid4` is imported when `uuid` is already imported on line 3. Use `uuid.uuid4()` instead and remove this import.",
    "top_paths": [
      "groups/tests/test_dal.py",
      "scripts/user_data_fix/glue_script_to_identify_user_list.py",
      "tests/performance/k6/scripts/accounts.js",
      "common/tests/unit/db/parser/test_rsql_parser_v2.py",
      "tests/automation/tests/users/NormalUsers/test_account_ownership_transfer.py"
    ],
    "comments": [
      "Redundant import - `uuid4` is imported when `uuid` is already imported on line 3. Use `uuid.uuid4()` instead and remove this import.",
      "The hardcoded UUID is duplicated between functions. Consider extracting it to a constant at the module level to improve maintainability and reduce duplication.",
      "The `uuid` module should be imported at the module level rather than inside the test function. Move the import to the top of the file with other imports for consistency and better performance.",
      "The `uuid` module is imported but not used anywhere in this file. Remove this unused import.\n```suggestion\n\n```",
      "Inconsistent spacing around the `=` operator in keyword argument. Should be `id=str(uuid.uuid4())` without spaces around `=` to follow PEP 8 style guidelines.",
      "There is an extra space before uuid in the import statement. Imports should follow PEP 8 style with a single space after the import keyword.\n```suggestion\n    import uuid, time\n```",
      "There is an extra space before uuid in the import statement. Imports should follow PEP 8 style with a single space after the import keyword.",
      "Import of 'uuid4' is not used.\n```suggestion\n\n```"
    ]
  },
  {
    "size": 8,
    "representative": "For existing users, the identity should not be deleted \u2014 only the relation needs to be removed. So, include this condition inside the `if` loop.",
    "top_paths": [
      "users/src/service.py",
      "users/src/dal.py",
      "common/src/db/app.py"
    ],
    "comments": [
      "i think delete_identity should be done anyway. can we move it outside of this loop?",
      "For existing users, the identity should not be deleted \u2014 only the relation needs to be removed. So, include this condition inside the `if` loop.",
      "remove self.identity_helper.delete_identity(full_user.id, account_id) from here",
      "this function may not be needed. can we do like this ?\r\nif delete_identity_flag:\r\n          self.identity_helper.delete_identity(full_user.id, account_id)",
      "What about the relations of this user entity? Where it is been captured to be deleted?",
      "As of now, we are removing PII only  for users, incase if we want to remove the information for other entities we can use this method so thought of implementing it in common DAL",
      "lets have this function name as \"delete_user\", inside it could set the status to DELETED and remove all pii fields from the entity and call also delete the unique key",
      "We are not deleting the relation from table, so if we delete user then we may not able to identify the relations. so Madan suggested to remove only PII from user entity"
    ]
  },
  {
    "size": 8,
    "representative": "both updating the entity and deleting unique key should be done in a single transaction.",
    "top_paths": [
      "users/src/dal.py",
      "common/src/db/schema.py",
      "applications/src/apicloud_app_sync.py",
      "common/src/db/app.py"
    ],
    "comments": [
      "Understood. We can address this as a separate ticket for resolution. The unique key that I have included here was previously added, but unfortunately, it was removed in one of the pull requests by mistake. Therefore, it has been re-added in this update.",
      "Could you plz be more specific like what sort of entity is going to be deleted...",
      "Why we need to get and delete the entity? its two db calls its unncessary one. Just remove the entity is fine I guess. since as per the REST standard simply delete make sense and its idempotent. So getting the entity and deleting doesn't make sense. \nSo reduce the DB operations as much as possible.\nIf anything needs to be audited then please do it in the db streams would be the right place.",
      "i think we should keep the unique key (as it is hashed) as we are not removing the actual entity, otherwise this entity will be data inconsistency problem",
      "As captured in the design docs, it is agreed to delete the unique key.",
      "Both updating the entity and deleting unique key operations should be done transactional way",
      "both updating the entity and deleting unique key should be done in a single transaction.",
      "updating the entity with empty payload itself is deleting the unique_key, so removed the delete_unique_key method"
    ]
  },
  {
    "size": 8,
    "representative": "Consider pinning the action to a specific commit hash instead of using @v1 to prevent potential supply chain attacks and ensure reproducible builds.\n```suggestion\n        uses: actions/create-github-app-token@b7b6c6e2e1e6e6e2e1e6e6e2e1e6e6e2e1e6e6e2\n```",
    "top_paths": [
      ".github/workflows/trigger_iam_release.yml",
      ".github/workflows/prestage_sam_apply_PR.yml",
      ".github/workflows/deployment_scheduler.yml",
      ".github/workflows/performance_testing_blu_pipeline.yml",
      ".github/workflows/performance_test_regression_pipeline.yml"
    ],
    "comments": [
      "Consider pinning the action to a specific commit SHA instead of using @v1 tag for better security and reproducibility. For example: `uses: actions/create-github-app-token@f2acddfb5195534d487896a656232b016a682f3c`\n```suggestion\n      - uses: actions/create-github-app-token@f2acddfb5195534d487896a656232b016a682f3c\n```",
      "Consider pinning the action to a specific commit hash instead of using @v1 to prevent potential supply chain attacks and ensure reproducible builds.\n```suggestion\n        uses: actions/create-github-app-token@c7e6e2e2e2e2e2e2e2e2e2e2e2e2e2e2e2e2e2e2\n```",
      "Consider pinning the action to a specific commit hash instead of using @v1 to prevent potential supply chain attacks and ensure reproducible builds.\n```suggestion\n        uses: actions/create-github-app-token@b7e3b6e7e2e2e7e2e2e7e2e2e7e2e2e7e2e2e7e2 # v1.2.4\n```",
      "Consider pinning the action to a specific commit hash instead of using @v1 to prevent potential supply chain attacks and ensure reproducible builds.\n```suggestion\n        uses: actions/create-github-app-token@e6c1e6e2e6e6e6e6e6e6e6e6e6e6e6e6e6e6e6e6\n```",
      "Consider pinning the action to a specific commit hash instead of using @v1 to prevent potential supply chain attacks and ensure reproducible builds.\n```suggestion\n        uses: actions/create-github-app-token@b7b6c6e2e1e6e6e2e1e6e6e2e1e6e6e2e1e6e6e2\n```",
      "Consider pinning the action to a specific commit hash instead of using @v1 to prevent potential supply chain attacks and ensure reproducible builds.\n```suggestion\n        uses: actions/create-github-app-token@b7b6c6e2e1e6e2c1e1e6e2c1e1e6e2c1e1e6e2c1\n```",
      "The `if: always()` conditional should be placed after the step name and ID. In GitHub Actions, the standard order is: name, id, if, uses/run. Move `if: always()` to after `id: app_token`.\n```suggestion\n        id: app_token\n        if: always()\n```",
      "The `if: always()` conditional should be placed after the step name and ID. In GitHub Actions, the standard order is: name, id, if, uses/run. Move `if: always()` to after `id: app_token`."
    ]
  },
  {
    "size": 8,
    "representative": "@raahavm we are searching already the device_cdh account association is there in iam records, both device:in_account and account:has_device. we need to compare it's _ref too. because device has multiple accounts and account has multiple devices",
    "top_paths": [
      "setup/infra/migration/data-synchronizer/layers/transformers.py",
      "scripts/px_account_membership/src/apply_px_data_in_core_service.py"
    ],
    "comments": [
      "@raahavm if the device cdh account association is already there in iam we'll not migrate.",
      "@raahavm if we are not calling here, we don't know the _ref to delete {account_id}:has_device. so we need this.",
      "@raahavm we are searching already the device_cdh account association is there in iam records, both device:in_account and account:has_device. we need to compare it's _ref too. because device has multiple accounts and account has multiple devices",
      "from the first record itself you have accountId and deviceId, the {account_id}:has_device can be formed. Primary key is enough for deletion",
      "in device:in_account record, _ref consists of accountId",
      "we are passing device id and cdh account id and comparing with iam records, you are particularly specifying for which case",
      "Add a docstring comment explaining why UUID validation was added and when a CDH account ID would already be in UUID format. This context is important for understanding the integration environment behavior where IAM returns UUIDs directly.",
      "Better to cross check the account id format and then decide like int (CDH) or uuid (from IAM)"
    ]
  },
  {
    "size": 8,
    "representative": "These steps switch the custom domain mapping using `aws apigatewayv2 update-api-mapping`, but Apollo GraphQL is wired as a REST API (v1) with Terraform-managed `aws_api_gateway_base_path_mapping`. Switch to the v1 `aws apigateway update-base-path-mapping` API (and pass `--base-path graphql-apollo`) so the mapping update actually affects the REST API domain mapping.",
    "top_paths": [
      "setup/infra/terraform_iac/applayer/main.tf",
      ".github/workflows/release_graphql_apply.yml",
      "graphql/template.yaml",
      "setup/infra/terraform_iac/applayer/variables.tf"
    ],
    "comments": [
      "The URI format appears incorrect. AWS API Gateway integration URIs for AppSync should follow the pattern `https://{appsync-api-id}.appsync-api.{region}.amazonaws.com/graphql`. The current format `arn:aws:apigateway:${AWS::Region}:${ApiId}.appsync-api:path/graphql` is not a valid integration URI.\n```suggestion\n          Uri: !Sub \"https://${AppSyncApiId}.appsync-api.${AWS::Region}.amazonaws.com/graphql\"\n          # Pass AppSyncApiId as a parameter for substitution\n          AppSyncApiId: !GetAtt AppSyncApi.ApiId\n```",
      "The count condition uses 'enable_apollo_graphql_mapping' but should use 'enable_apollo_graphql_test_mapping' since this is the test domain mapping resource. This inconsistency means the test mapping will be created when the main Apollo GraphQL mapping is enabled, rather than being independently controlled by its dedicated flag.",
      "The conditional check for apollo_graphql_test_domain_mapping resource uses enable_apollo_graphql_mapping variable instead of enable_apollo_graphql_test_mapping. This means the test domain mapping will be created whenever the main mapping is enabled, even if enable_apollo_graphql_test_mapping is set to false. This should use enable_apollo_graphql_test_mapping to properly control test domain mapping creation.",
      "These steps switch the custom domain mapping using `aws apigatewayv2 update-api-mapping`, but Apollo GraphQL is wired as a REST API (v1) with Terraform-managed `aws_api_gateway_base_path_mapping`. Switch to the v1 `aws apigateway update-base-path-mapping` API (and pass `--base-path graphql-apollo`) so the mapping update actually affects the REST API domain mapping.",
      "`enable_apollo_graphql_test_mapping` is declared and defaulted to `true`, but it is not referenced anywhere in this Terraform module (no usages found in `main.tf`). Either wire this variable into the Apollo test-domain mapping `count` expression (to allow disabling it independently) or remove it to avoid a misleading configuration toggle.",
      "`apollo_graphql_test_domain_mapping` is gated by `enable_apollo_graphql_mapping`, not the dedicated `enable_apollo_graphql_test_mapping` variable. As a result, the test-domain mapping cannot be disabled independently (and the `enable_apollo_graphql_test_mapping` variable becomes dead config). Update the `count` to use the correct variable if independent control is intended.",
      "`${ApiId}` here is populated from the `apigatewayv2 get-api-mappings` response earlier, but `aws apigateway create-deployment` expects a REST API id (v1). If `ApiId` is an API Gateway v2 id, these deployments will fail. After switching to v1 `get-base-path-mappings`, ensure you pass the REST API id into this command.\n```suggestion\n          aws apigatewayv2 create-deployment --api-id \"${TestApiId}\" --stage-name \"${{ inputs.STACK }}-blue\" --region \"${{ inputs.AWS_REGION }}\"\n          sleep 5 # Api gateway throws too many requests in case of continuous deployments, so added a delay\n          aws apigatewayv2 create-deployment --api-id \"${TestApiId}\" --stage-name \"${{ inputs.STACK }}-green\" --region \"${{ inputs.AWS_REGION }}\"\n```",
      "This step uses `aws apigatewayv2 get-api-mappings`, but the GraphQL gateway in this PR is an API Gateway REST API (`AWS::ApiGateway::RestApi`) and the Terraform side uses `aws_api_gateway_base_path_mapping` (v1). Use the v1 `aws apigateway get-base-path-mappings` API instead, otherwise the returned IDs/stage values will be wrong.\n```suggestion\n          Mappings=$(aws apigateway get-base-path-mappings --domain-name \"$FullDomain\" --region ${{ inputs.AWS_REGION }} --output json)\n          Result=$(echo \"$Mappings\" | jq '[.items[] | select(.basePath==\"graphql-apollo\") | {ApiId:.restApiId,ApiMappingId:.basePath,Stage:.stage}][0]')\n```"
    ]
  },
  {
    "size": 8,
    "representative": "`reg_api_rest_body_json_file` points to `configuration/templates/api-swagger-yaml/iam-api-ec1-stage.yaml`, but that file does not exist in the repo (the templates directory currently only contains `iam-api-ec1-prod.yaml`). Terraform will fail when the APIGW module tries to read the OpenAPI document. Add the missing stage OpenAPI file (or update this path to an existing spec).",
    "top_paths": [
      "setup/infra/terraform_iac/internalGateway/configuration/templates/internal-swagger-yaml/iam-internal-api.yaml",
      "organizations/src/app.py",
      "setup/infra/terraform_iac/applayer/configuration/stage/eu/eu-central-1/stage.tfvars",
      "setup/infra/terraform_iac/applayer/configuration/qa/eu/eu-central-1/qa.tfvars",
      "setup/infra/terraform_iac/applayer/configuration/dev/eu/eu-central-1/dev.tfvars"
    ],
    "comments": [
      "Missing forward slash in $ref paths. The correct OpenAPI syntax is '#/components/responses/...' not '#components/responses/...'\n```suggestion\n          $ref: '#/components/responses/Unauthorized.yaml'\n```",
      "The response model in the OpenAPI documentation is incorrectly specified as \"AccountToOrganizationList\" for a 200 response. This endpoint returns users, not accounts, so the model should be \"UserReferenceList\" to match the actual return type used in the handler (line 400).\n```suggestion\n            'content': {'application/json': {'model': UserReferenceList}},\n```",
      "The new response schema uses external $ref paths like `../components/schemas/AccountUserListLimited.yaml` and `../components/schemas/Links.yaml`, but there is no `components/` directory under `setup/infra/terraform_iac/internalGateway/configuration/templates/` and these files don\u2019t exist in the repo. This will leave the OpenAPI references unresolved. Please switch these to the same in-spec references used elsewhere in this file (e.g., `#/components/schemas/...`) or add the referenced schema files to the repo at the expected path.\n```suggestion\n                      $ref: '#/components/schemas/AccountUserListLimited'\n                  links:\n                    $ref: '#/components/schemas/Links'\n        '400':\n          $ref: '#/components/responses/BadRequest'\n        '401':\n          $ref: '#/components/responses/Unauthorized'\n        '403':\n          $ref: '#/components/responses/Forbidden'\n        default:\n          $ref: '#/components/responses/Unauthorized'\n```",
      "The error response $ref entries point to external files (e.g., `../components/responses/BadRequest.yaml`, `Unauthorized.yaml`, `Forbidden.yaml`), but those files/that directory are not present in this repo and other endpoints in this spec reference responses via `#/components/responses/...`. These refs will be broken unless the pipeline injects them somehow. Please align with the existing reference style or add the missing response definition files at the referenced locations.\n```suggestion\n                      $ref: '#/components/schemas/AccountUserListLimited'\n                  links:\n                    $ref: '#/components/schemas/Links'\n        '400':\n          $ref: '#/components/responses/BadRequest'\n        '401':\n          $ref: '#/components/responses/Unauthorized'\n        '403':\n          $ref: '#/components/responses/Forbidden'\n        default:\n          $ref: '#/components/responses/Unauthorized'\n```",
      "`reg_api_rest_body_json_file` points to `configuration/templates/api-swagger-yaml/iam-api-ec1-stage.yaml`, but that file does not exist in the repo (the templates directory currently only contains `iam-api-ec1-prod.yaml`). Terraform will fail when the APIGW module tries to read the OpenAPI document. Add the missing stage OpenAPI file (or update this path to an existing spec).",
      "`reg_api_rest_body_json_file` points to `configuration/templates/api-swagger-yaml/iam-api-ec1-qa.yaml`, but that file is not present in the repository. Terraform will error when loading the API definition. Add the missing QA OpenAPI file (or update the path to an existing spec).",
      "`reg_api_rest_body_json_file` points to `configuration/templates/api-swagger-yaml/iam-api-ec1-dev.yaml`, but that file is not present in the repository. Terraform will error when loading the API definition. Add the missing dev OpenAPI file (or update the path to an existing spec).",
      "This file appears to be the Swagger Petstore example (e.g., `petstore.swagger.io` server URL, Petstore tags/paths) rather than the IAM API spec. If this file is used to create/update the API Gateway in eu-central-1 prod, it will deploy the wrong routes and contract. Replace this with the actual IAM OpenAPI document (or use the same source/generation approach as other regions)."
    ]
  },
  {
    "size": 8,
    "representative": "These new keys are not defined as parameters in `events/template.yml`, but the standard `events` SAM deploy path passes all entries from `events-env.json` as `--parameter-overrides`. This will cause regular `events` deployments to fail with an unknown-parameter error unless the main template is updated to accept them (e.g., add parameters with safe defaults) or these values are moved to a separate env file used only by the geo-isolation stack.\n```suggestion\n\n```",
    "top_paths": [
      "events/template.yml",
      "setup/infra/migration/data-synchronizer/template.yaml",
      "setup/infra/sam/configuration/integ/us/us-west-2/events-env.json",
      "setup/infra/sam/configuration/dev3/us/us-west-2/events-env.json",
      ".github/workflows/release_common_sam_apply.yml"
    ],
    "comments": [
      "New template parameters `AossUsersCentralIngestorMaxConcurrency` / `AossUsersRegionalIngestorMaxConcurrency` have no `Default`, which makes them required for every deployment of this stack. In this PR I only see overrides added in the dev us-west-2 `events-env.json`, so other environments/regions will fail stack updates unless their parameter override files (or SAM deploy command) are updated, or defaults are provided here.\n```suggestion\n    Type: Number\n    Default: 5\n  AossUsersRegionalIngestorMaxConcurrency:\n    Type: Number\n    Default: 5\n```",
      "`IsPrimary` is introduced as a String parameter, but its `AllowedValues` are currently unquoted YAML scalars (`true`/`false`). In YAML those parse as booleans, which can cause CloudFormation parameter validation/type-mismatch issues when the incoming parameter value is the string `'true'`/`'false'` (as provided by the env json files and the `!Equals [ !Ref IsPrimary, 'true' ]` condition). Quote the allowed values (or switch to a parameter type/pattern that matches the actual string values) to keep the template consistent and avoid deploy-time failures.\n```suggestion\n      - 'true'\n      - 'false'\n```",
      "`IsPrimary` is a newly required template parameter with no default. Any existing deployment path that doesn't pass this new parameter will now fail at deploy time. For example, `setup/infra/sam/configuration/prestage/us/us-east-1/profiles-iam-sync-env.json` currently has no `IsPrimary` key, but the live-sync deployment workflow reads that file and forwards all keys as `--parameter-overrides`. Consider adding a safe `Default` for `IsPrimary` (to preserve current behavior) and/or ensuring all relevant `profiles-iam-sync-env.json` files supply it.\n```suggestion\n    Type: String\n    Default: 'false'\n```",
      "`APPLICATION_HOME_AZPS: !Ref APPLICATION_HOME_AZPS` references a CloudFormation parameter that is not declared anywhere in this template. This will cause stack updates to fail. Add an `APPLICATION_HOME_AZPS` parameter (ideally with a safe default like an empty string) or change the reference to a hard-coded/default value so environments whose *events-env.json* don\u2019t yet define the key don\u2019t break deployments.",
      "Renaming the CloudFormation/SAM parameter from `APPLICATION_HOME_AZPS` to `ApplicationHomeAzps` will break `sam deploy` because the deployment pipeline passes every key from `setup/infra/sam/configuration/**/events-env.json` as a `--parameter-overrides` entry. Those JSON files still contain `APPLICATION_HOME_AZPS`, which will cause a \"Parameters: [APPLICATION_HOME_AZPS] do not exist\" failure. Update the relevant `events-env.json` keys to `ApplicationHomeAzps` (or remove them if the new default is sufficient).\n```suggestion\n    Type: String\n  APPLICATION_HOME_AZPS:\n    Type: String\n    Default: \"\"\n    Description: \"Deprecated: retained for backward compatibility. Use ApplicationHomeAzps.\"\n```",
      "These new keys are not defined as parameters in `events/template.yml`, but the standard `events` SAM deploy path passes all entries from `events-env.json` as `--parameter-overrides`. This will cause regular `events` deployments to fail with an unknown-parameter error unless the main template is updated to accept them (e.g., add parameters with safe defaults) or these values are moved to a separate env file used only by the geo-isolation stack.\n```suggestion\n\n```",
      "These new keys are not defined as parameters in `events/template.yml`, but the standard `events` SAM deploy path passes all entries from `events-env.json` as `--parameter-overrides`. This will cause regular `events` deployments to fail with an unknown-parameter error unless the main template is updated to accept them (e.g., add parameters with safe defaults) or these values are moved to a separate env file used only by the geo-isolation stack.\n```suggestion\n  \"AccountManagerRoleId\": \"2c2bc0c7-ceaf-4260-b988-52da800dd915\"\n```",
      "This job builds `--parameter-overrides` by passing *all* keys from `events-env.json`. Since the same `events-env.json` is also used by the normal `events` deployment path (composite action), adding geo-isolation-specific keys there will cause regular `events` stack deployments to fail unless the main `events/template.yml` also defines those parameters. Consider using a dedicated env JSON for `events-geoisolation-tmp` or filtering parameters before calling `sam deploy`."
    ]
  },
  {
    "size": 8,
    "representative": "Type hints for `_send_user_removal_notification` declare `admin_role_id`, `user_email_template_id`, and `admin_email_template_id` as non-optional `str`, but `remove_user_from_group` passes `Optional[str]` values. This will fail mypy (and obscures the intended \u201coptional notification\u201d behavior). Update the signature (and docstring) to accept `Optional[str]` and handle `None` cleanly.",
    "top_paths": [
      "groups/src/service.py",
      "groups/tests/test_service.py"
    ],
    "comments": [
      "In `_send_user_removal_notification`, `removed_user_initial` is being set from `admin_last_name` instead of the removed user\u2019s last name (`last_name`). This will populate templates with incorrect user information. Use the removed user\u2019s last name/initial for `removed_user_initial` (and similarly ensure `removing_admin_initial` is actually an initial if that\u2019s what templates expect).",
      "`remove_user_from_group` calls `_send_user_removal_notification` unconditionally after the transactional removal. When both template IDs are `None` (the default), this still performs multiple DAL reads (user/group/account lookups) and can raise errors even though no notifications should be sent. Gate this call (or early-return inside `_send_user_removal_notification`) when neither `admin_email_template_id` nor `user_email_template_id` is provided.\n```suggestion\n                if admin_email_template_id is not None or user_email_template_id is not None:\n                    self._send_user_removal_notification(\n                        group_id=group_id,\n                        removed_user_id=user_id,\n                        calling_admin_id=token_data.sub,\n                        admin_role_id=admin_role_id,\n                        user_email_template_id=user_email_template_id,\n                        admin_email_template_id=admin_email_template_id,\n                    )\n```",
      "`EmailRequest.template_variables` is a non-optional `dict` (see `email_service_wrapper.EmailRequest`), but `_send_notification_to_user` accepts `email_template_variables=None` and forwards it into `EmailRequest(...)`. Passing `None` will trigger Pydantic validation errors. Make the parameter `Optional[dict]` and default to/convert to an empty dict before constructing `EmailRequest`.",
      "`_send_notification_to_group_admins` also forwards `email_template_variables` directly into `EmailRequest(...)` but the parameter defaults to `None`. Since `EmailRequest.template_variables` is a required dict, `None` will cause validation errors. Ensure a dict is always passed (e.g., default/normalize to `{}`) before constructing `EmailRequest`.",
      "This test now asserts `_validate_group` is called twice because `_send_user_removal_notification` runs during `remove_user_from_group`. That couples the test to the notification side-effect and will become incorrect if notifications are correctly skipped when templates aren\u2019t provided. Prefer patching `_send_user_removal_notification` in this test (or asserting `_validate_group` is called once for the core removal flow).",
      "Type hints for `_send_user_removal_notification` declare `admin_role_id`, `user_email_template_id`, and `admin_email_template_id` as non-optional `str`, but `remove_user_from_group` passes `Optional[str]` values. This will fail mypy (and obscures the intended \u201coptional notification\u201d behavior). Update the signature (and docstring) to accept `Optional[str]` and handle `None` cleanly.",
      "This is handled inside '_send_user_removal_notification' method",
      "`EmailRequest.template_variables` by definition will initiate an empty dict if not provided by pydantic."
    ]
  },
  {
    "size": 7,
    "representative": "Please remove commented out code",
    "top_paths": [
      "applications/src/main.py",
      "setup/infra/terraform_iac/utilities/generate_swagger_yaml.sh",
      "analytics/src/event_bridge_receivers/central/users_to_accounts/event_handler.py",
      "tests/automation/tests/sync/test_tid_profiles_iam_sync.py",
      "tests/automation/tests/sync/test_cdh_sync.py"
    ],
    "comments": [
      "Remove commented out code throughout",
      "Delete commented out lines of code.",
      "Remove commented out code if it is not needed",
      "Remove commented out lines of code.",
      "Should remove commented out code.",
      "Remove commented code or include remark for keeping.",
      "Please remove commented out code"
    ]
  },
  {
    "size": 7,
    "representative": "This logger is also not required",
    "top_paths": [
      "users/src/users_appsync_handler.py",
      "roles/src/dal.py",
      "organizations/script/organization_seeding/ingestion_script.py",
      "applications/src/app.py",
      "common/src/common/authorizer_helper.py"
    ],
    "comments": [
      "This logger is also not required",
      "we can add logger here",
      "Will update this to logger",
      "can we add logger for this operation ?",
      "This log is not required.",
      "logger info should be fine here.",
      "do not use any Logger in common."
    ]
  },
  {
    "size": 7,
    "representative": "Is `time.time_ns()` timezone aware. It might be better to use `datetime.now(timezone.utc)`",
    "top_paths": [
      "users/src/constants.py",
      "accounts/src/accounts/models/meta.py",
      "common/src/db/iam_database.py",
      "users/src/service.py",
      "common/src/db/pagination/strategies/search_driven_cursor.py"
    ],
    "comments": [
      "I see that the timezones are according to the standards defined in https://github.com/unicode-org/cldr/blob/latest/common/bcp47/timezone.xml",
      "Is `time.time_ns()` timezone aware. It might be better to use `datetime.now(timezone.utc)`",
      "use with timezone utc\r\ndatetime.now(timezone.utc)",
      "The datetime.now() call is missing timezone awareness. This should use datetime.now(timezone.utc) to match the timezone-aware datetime created on line 533, preventing potential comparison issues between naive and timezone-aware datetimes.\n```suggestion\n                expiry_days_timestamp = datetime.now(timezone.utc) + timedelta(days=user.expiryDays)\n```",
      "for safer side, can we set timezone utc here? not to depend on system timezone.",
      "`request_time.astimezone(timezone.utc)` will raise if `request_time` is timezone-naive (e.g., an ISO-8601 string without an offset). Either enforce timezone-aware datetimes in validation or default naive values to UTC before calling `astimezone`.",
      "These can be done in a more pythonic way\n```\ncurrent_time = time.time_ns() // 1_000_000\ndt = datetime.fromtimestamp(current_time / 1000, tz=timezone.utc)\n\n```\nInstead using \n\n`datetime.now(timezone.utc)`"
    ]
  },
  {
    "size": 7,
    "representative": "in my opinion, only the error code and its detail message could be part of the Error enum class. The http status code could be set when raising the ApplicationError.",
    "top_paths": [
      "users/src/service.py",
      "devices/src/models.py",
      "users/src/error_codes.py",
      "applications/tests/test_app.py",
      "groups/src/error_codes.py"
    ],
    "comments": [
      "ApplicationError also not needed here. it will be handled automatically",
      "ApplicationError should have a generic message. should not expose internal details. just raise the Exception for any unknown exception. it will be interpreted as 'server error occurred'",
      "ValueError is fine here. It will be taken care by common exception handler",
      "in my opinion, only the error code and its detail message could be part of the Error enum class. The http status code could be set when raising the ApplicationError.",
      "The error code being checked ('APP_500') doesn't exist in the ErrorCode enum. All defined error codes in the enum start with 'APP_' followed by a 3-digit number (APP_001 through APP_031), but none correspond to HTTP 500 status codes.\n```suggestion\n    assert \"APP_031\" in error_response.type  # Use the actual error code for HTTP 500 from the ErrorCode enum\n```",
      "Inconsistent status code definition. Other error codes in this enum use `HTTPStatus.BAD_REQUEST` without `.value`. This should be `HTTPStatus.BAD_REQUEST` to match the pattern used throughout the file.\n```suggestion\n        HTTPStatus.BAD_REQUEST,\n        \"Role cannot be empty or spaces when provided, must be list of non-empty strings\",\n        \"This error occurs when the role field is provided but is empty, which is not allowed\"\n    )\n    TEMPLATE_EMPTY_WHEN_PROVIDED = (\n        \"GRP_048\",\n        HTTPStatus.BAD_REQUEST,\n```",
      "`HTTPStatus.INTERNAL_SERVER_ERROR` is an enum, but other error codes use `HTTPStatus.<...>.value`. This will likely propagate a non-int status into `ApplicationError(status=...)`. Change it to `HTTPStatus.INTERNAL_SERVER_ERROR.value` for consistency and correct typing.\n```suggestion\n        HTTPStatus.INTERNAL_SERVER_ERROR.value,\n```"
    ]
  },
  {
    "size": 7,
    "representative": "This string is repeated multiple times. Can we move it to a constant and refer it whereever it required.",
    "top_paths": [
      "users/src/service.py",
      "users/src/app.py",
      "setup/infra/migration/data-synchronizer/layers/transformers.py",
      "setup/docker/mocks/routes/oauth/token.js",
      "accounts/src/accounts/serialization/graphql.py"
    ],
    "comments": [
      "This value is repeated so many times. Can we make it as a variable and use it wherever it applies?",
      "Better assign the repeated access of values into an variable and use it.",
      "Repeating this value throughout, maybe better to set a const or something that can be referenced instead of repeating the string.  This would make it less error prone to change and also give it a meaningful name if this is bound to some environment and/or just a fake value, either way, the const name would be self-documenting.",
      "can we assign it to a variable and use it to avoid repetition",
      "Better move it as a constant string and refer it here.",
      "Plz move the complete string to a constant thats what I meant.",
      "This string is repeated multiple times. Can we move it to a constant and refer it whereever it required."
    ]
  },
  {
    "size": 7,
    "representative": "Keep match statement and handle additional logic in the base(`_`) case",
    "top_paths": [
      "accounts/src/accounts/dynamodb/account_repository/relation_repository/relation_mapper.py",
      "setup/infra/sam/configuration/dev1/us-west-2/users-env.json",
      "accounts/src/accounts/dynamodb/account_repository/account_repository_impl.py",
      "groups/src/service.py",
      "accounts/src/accounts/dynamodb/account_repository/reference_repository/reference_repository_impl.py"
    ],
    "comments": [
      "can we please add `_` between the words for better readability",
      "Can we simplify this match statement to something like:\r\n```\r\ncase Status.updated:\r\n    ... # update call\r\ncase Status.loaded:\r\n    pass\r\ncase _:\r\n    raise ValueError(\"Unexpected status\")\r\n```",
      "if full relation isn't going to be used we can just use '_' and ignore it",
      "can these isinstance checks be changed to match statements?",
      "can we move this logic inside wild_card_match function ?",
      "Lets keep match because the static checkers have a harder time with the `isinstance` function. The status check and others can be added to the base case (`case _`)",
      "Keep match statement and handle additional logic in the base(`_`) case"
    ]
  },
  {
    "size": 7,
    "representative": "this condition not required due to below reccommended",
    "top_paths": [
      "common/src/common/context/context_graph_dal.py",
      "common/src/db/iam_database.py",
      "users/template.yaml",
      "setup/infra/migration/data-synchronizer/layers/transformers.py",
      "users/src/service.py"
    ],
    "comments": [
      "It should be or condition just copy paste issue pardon me. The goal is fail fast if the required value is missing.",
      "this condition not required due to below reccommended",
      "These conditions are not required now. So plz revert it.",
      "should this be 'and' condition here ??",
      "@ragulkarthi Can confirm whether this condition is required.",
      "why this condition is added ?",
      "After carefull consideration this conditions will be delete"
    ]
  },
  {
    "size": 7,
    "representative": "Should this be renamed CdhAccountEntity? And should it inherit from a CDH base entity?",
    "top_paths": [
      "accounts/src/accounts/models/core/account/account.py",
      "accounts/src/accounts/cdh_sync/cdh_query_handler.py",
      "setup/infra/sam/configuration/cdh-iam-users-roles-live-sync-env/cdh-iam-stage.json",
      "live_sync/iam_to_cdh_sync/src/accounts_sync/entities.py",
      "accounts/cdh-migration/export_scripts/export/cdh_iam_account_osr_migration.py"
    ],
    "comments": [
      "instead of making this new method, we could translate the cdh account entity into a create model. that way, we are maintaining separation between external sources (like the rest api and cdh, in this case) and our application internals",
      "Seems like the `AccountCreateModel` is already kind of tied to the payload from the rest API and wouldn't really maintain the separation you are describing by translating from `CdhAccountEntity` to that. I guess if we don't want to define these helper class methods on the core models any longer a translation function can be added to the cdh sync module that will directly create create the `Account` model from the `CdhAccountEntity`.",
      "@rajkumar-somasundaram can check if Cdhsyncclientid value need to be corrected",
      "Should this be renamed CdhAccountEntity? And should it inherit from a CDH base entity?",
      "CdhSyncAccountUpdateHandler needs the entire CdhAccountEntity, kept fields",
      "Kind of nitpicking but I think we can go ahead and name this CdhAccountRepository now as I think it resembles that pattern closely enough and will align better with the IamAccountRepository that currently exists",
      "I have changed the class to CdhAccountRepository"
    ]
  },
  {
    "size": 7,
    "representative": "what is the account that's getting updated in IAM if the integrator is still using CDH? and I think we might be able to handle this smoothly with the existing account update method",
    "top_paths": [
      "accounts/src/accounts/cdh_sync/account_controller/update.py",
      "setup/infra/terraform_iac/dd-monitors/cdh_to_iam_sync_metrics_dashboard.tf",
      "tests/automation/tests/sync/test_cdh_sync_account_and_address.py",
      "live_sync/iam_to_cdh_sync/src/accounts_sync/accounts_controller/update.py",
      "reverse_sync/iam_to_cdh_sync_accounts/src/accounts_sync/utils/external_systems_utils.py"
    ],
    "comments": [
      "If someone is trying to maintain the account in both CDH and IAM I guess this could happen. Ideally an integrator shouldn't be modifying accounts in both CDH and IAM at the same time. Seems like a complex edgecase that would be difficult for us to handle and would be better to advise integrators to not maintain the account by utilizing both systems at the same time.",
      "what is the account that's getting updated in IAM if the integrator is still using CDH? and I think we might be able to handle this smoothly with the existing account update method",
      "The goal of the CDH sync is to keep accounts in IAM that were migrated from CDH synced while integrators are moved off of CDH. Integrators really shouldn't be updating the same account via CDH and IAM. The CDH sync operation is a replacement of the account data. We could probably overload the current update method but it was easier and straight forward to explicitly update the account. This way we also don't have to worry about tracking an internal state during the course of the operation",
      "Here too, the query shows CDH to IAM account sync but the title is misleading.",
      "Could you please assert that account changes are reflected in the CDH before checking the IAM?",
      "something @davemunson brought up in standup: we will need to go back into IAM and find the cdh external system reference for the case that this is an account that was originally created in cdh",
      "i think i'm a little confused on what's going on here. is the intent to update the IAM external system reference to point to the CDH account id? i thought we werent going to sync those external system references back to CDH"
    ]
  },
  {
    "size": 7,
    "representative": "it is better not to log the pii data like 'email, name etc in logs for compliance",
    "top_paths": [
      "applications/src/app.py",
      "users/src/apply_px_data_in_core_service.py",
      "organizations/src/app.py",
      "events/src/ingestions/consumers/central/handlers.py",
      "events/src/ingestions/consumers/central/user_handler.py"
    ],
    "comments": [
      "Why we need to log the entire event payload?,\r\nI guess users blu also does the same. It will log the passwords and others too. Here it will log app's entire details. So it should not be.\r\n@PrabuMonk and @MadankumarSakthivel we should not log the entire request payloads. plz suggest here.\r\nSimilarly users BLU. as well does the same and its a security issue to log the passwords and PII details. @Vishalisenniappan27 could you plz create a ticket to remove those event logging in user's BLU so that it wont open any security violation issues.",
      "Gone through the code and only certain attributes are extracted from the event payload and placed in the log.\r\nSo no more sensitive data are logged.",
      "it is better not to log the pii data like 'email, name etc in logs for compliance",
      "> it is better not to log the pii data like 'email, name etc in logs for compliance\r\n\r\nthanks for pointing this. Added as part of the debugging purposes only. Replaced with sanitized output.",
      "Since it can have email address, phone number, etc thought logging would not be a good idea",
      "do we really need to log the entire event ?? is there any pii or sensitive data in the event ??",
      "User event will contain PII information, logging entire event is not preferred"
    ]
  },
  {
    "size": 7,
    "representative": "Missing error handling for the environment variable. If `INHERITOR_ROLE_ID` is not set, this could cause runtime errors. Consider adding validation or a default value.\n```suggestion\nINHERITOR_ROLE_ID = get_env_var('INHERITOR_ROLE_ID')\nif not INHERITOR_ROLE_ID:\n    raise RuntimeError(\"Environment variable 'INHERITOR_ROLE_ID' is not set. Please set it before running the application.\")\n```",
    "top_paths": [
      "common/src/util/helper.py",
      "groups/src/service.py",
      "roles/src/dal.py",
      "roles/src/service.py",
      "scripts/px_account_membership/src/apply_px_dataset.py"
    ],
    "comments": [
      "Missing assignment of the return value from `process_roles_for_member` to `role_names`. This should be `role_names = self.process_roles_for_member(...)`\n```suggestion\n            role_names = self.process_roles_for_member(operations, role_ids, group_id, application_id,\n```",
      "Missing error handling for the environment variable. If `INHERITOR_ROLE_ID` is not set, this could cause runtime errors. Consider adding validation or a default value.\n```suggestion\nINHERITOR_ROLE_ID = get_env_var('INHERITOR_ROLE_ID')\nif not INHERITOR_ROLE_ID:\n    raise RuntimeError(\"Environment variable 'INHERITOR_ROLE_ID' is not set. Please set it before running the application.\")\n```",
      "Missing whitespace after ':' in type annotation 'entity_type:str' should be 'entity_type: str'.\n```suggestion\n                             entity_id: str, entity_type: str, role_entity: FullRole) \\\n```",
      "The warning log level is inappropriate for normal operation. Loading an environment variable is expected behavior, not a warning condition. Consider using `logger.debug()` or `logger.info()` instead, or remove this log statement entirely if it's only needed for debugging.\n```suggestion\n        logger.info(f\"Loading INHERITOR_ROLE_ID from environment variable\")\n```",
      "Extra whitespace before 'get_inheritor_role_id()'. Should be single space after 'return'.\n```suggestion\n    return get_inheritor_role_id() in role_ids\n```",
      "The config key should be \"end_user\" not \"user\" based on the role_config structure defined in the config files (which has \"owner\", \"end_user\", \"developer\", and \"default_role\" keys).\n```suggestion\nIAM_USER_ROLE_ID: str = role_configs.get(\"end_user\")\n```",
      "Missing trailing comma after the last argument can fail formatting/linting (and makes diffs noisier on future edits). Add a trailing comma after `role_id=...` for consistency with the other call site above.\n```suggestion\n            role_id=get_roles[\"endUserRoleId\"],\n```"
    ]
  },
  {
    "size": 7,
    "representative": "[nitpick] Extra blank line after function definition should be removed for consistency with the rest of the codebase.",
    "top_paths": [
      "roles/src/appsync_handler.py",
      "users/tests/test_helpers.py",
      "accounts/src/accounts/serialization/account.py",
      "users/src/appsync_handler.py",
      "devices/src/device_service.py"
    ],
    "comments": [
      "[nitpick] Extra blank lines after function definition should be removed for consistency with the rest of the codebase.",
      "[nitpick] Extra blank line after function definition should be removed for consistency with the rest of the codebase.",
      "Extra blank line should be removed to maintain consistent code formatting.",
      "Missing blank line before function definition. There should be two blank lines between the end of the previous function (line 144) and this function definition to follow PEP 8 conventions.",
      "Missing blank line before function definition. There should be two blank lines between the end of the previous function (line 237) and this function definition to follow PEP 8 conventions.",
      "Trailing whitespace at the end of line 224. Remove the extra spaces after the function definition.",
      "Trailing whitespace found. Remove the extra whitespace at the end of this line."
    ]
  },
  {
    "size": 7,
    "representative": "Missing docstring for public method. Add documentation explaining the purpose, parameters, and return value of this parallel listing operation.\n```suggestion\n    def parallel_list_relations(list_requests: List[ListRequest]) -> List[Tuple[ListRequest, Page[Tuple[FullEntity, FullRelation]]]]:\n        \"\"\"\n        Perform parallel listing of relations for the provided list of ListRequest objects.\n\n        :param list_requests: A list of ListRequest objects specifying the relations to list in parallel.\n        :return: A list of tuples, each containing the original ListRequest and a Page of (FullEntity, FullRelation) tuples.\n        \"\"\"\n```",
    "top_paths": [
      "users/src/dal.py",
      "common/src/db/app.py",
      "organizations/src/dal.py",
      "users/tests/test_users_dal.py"
    ],
    "comments": [
      "The return type annotation specifies Page[Tuple[FullEntity, FullRelation]] but the function builds tuples of (ListRequest, Page[FullRelation]); this mismatch can mislead users and static type checkers. Update the annotation to List[Tuple[ListRequest, Page[FullRelation]]] (or adjust the implementation to match).\n```suggestion\n) -> List[Tuple[ListRequest, Page[FullRelation]]]:\n```",
      "The return type annotation is malformed with incorrect parentheses placement. It should be `-> List[Tuple[ListRequest, Page[Tuple[FullEntity, FullRelation]]]]:`\n```suggestion\n    def parallel_list_relations(self, list_requests: List[ListRequest]) -> List[Tuple[ListRequest, Page[Tuple[FullEntity, FullRelation]]]]:\n```",
      "Missing docstring for public method. Add documentation explaining the purpose, parameters, and return value of this parallel listing operation.\n```suggestion\n    def parallel_list_resources(list_requests: List[ListRequest]) -> List[Tuple[ListRequest, Page[Tuple[FullEntity, FullRelation]]]]:\n        \"\"\"\n        Perform a parallel listing of resources based on the provided list requests.\n\n        :param list_requests: A list of ListRequest objects specifying the resources to list.\n        :return: A list of tuples, each containing the original ListRequest and a Page of (FullEntity, FullRelation) results.\n        \"\"\"\n```",
      "Missing docstring for public method. Add documentation explaining the purpose, parameters, and return value of this parallel listing operation.\n```suggestion\n    def parallel_list_relations(list_requests: List[ListRequest]) -> List[Tuple[ListRequest, Page[Tuple[FullEntity, FullRelation]]]]:\n        \"\"\"\n        Perform parallel listing of relations for the provided list of ListRequest objects.\n\n        :param list_requests: A list of ListRequest objects specifying the relations to list in parallel.\n        :return: A list of tuples, each containing the original ListRequest and a Page of (FullEntity, FullRelation) tuples.\n        \"\"\"\n```",
      "parallel_list_relations docstring still states it raises AssertionError when too many requests are provided, but the implementation now raises ValueError. Update the docstring to match the actual exception type (and/or keep the exception type consistent with the documented contract).\n```suggestion\n    :raises ValueError: If more than 10 requests are provided\n```",
      "`reduce(...)` will raise `TypeError` when `list_requests` is empty (because `batches`/the mapped list is empty). This makes `parallel_list_relations([])` crash; handle the empty-input case explicitly (e.g., return an empty list early, or provide an initializer to the reduction).",
      "This test verifies that >10 requests are split into multiple calls, but it doesn\u2019t assert that no batch exceeds the DAL limit (e.g., first call gets 10 requests) and it doesn\u2019t cover the empty-input case (which currently crashes due to `reduce` on an empty sequence). Add assertions on `mock_parallel_list_relations.call_args_list` (batch sizes/contents) and include a `parallel_list_relations([])` test expectation."
    ]
  },
  {
    "size": 7,
    "representative": "This patch target likely doesn't exist: the module under test is `common.lambda_helper`, not `src.common.lambda_helper`. Using the wrong import path will make the patch a no-op or raise at runtime. Patch the actual module path that `get_target_details` resolves `get_application_id` from (e.g., `common.lambda_helper.get_application_id`).",
    "top_paths": [
      "common/tests/unit/common/test_lambda_helper.py",
      "organizations/tests/test_single_tier_hierarchical_strategy.py",
      "groups/tests/conftest.py",
      "organizations/tests/conftest.py",
      "common/tests/conftest.py"
    ],
    "comments": [
      "[nitpick] The patch target should use a more specific import path. Consider patching 'common.lambda_helper.get_meta_data' or using the module path where it's actually imported from to make the patch more explicit and maintainable.",
      "The `__initialized` attribute is set but never checked or used elsewhere in the MockConfigManager class. This suggests incomplete singleton initialization logic or dead code that should be removed.\n```suggestion\n\n```",
      "The `mock_config_manager` fixture is defined twice (lines 129-131 and 135-141), with the second definition overriding the first. This creates unreachable code and the first fixture will never be used. Remove the first definition (lines 129-131) to eliminate the duplicate and ensure only the patched version is active.",
      "The patch targets `'config_layer.CONFIG_MGR.get_global_config'` and `'config_layer.CONFIG_MGR.get_configs'` as strings, but this may not patch correctly if the module is imported before the fixture runs. These patches should target the fully qualified module path (e.g., `'common.src.config_layer.CONFIG_MGR.get_global_config'`) or use object-based patching to ensure they apply correctly across all test modules.\n```suggestion\n    with patch('common.src.config_layer.CONFIG_MGR.get_global_config', mock_manager.get_config), \\\n            patch('common.src.config_layer.CONFIG_MGR.get_configs', mock_manager.get_configs):\n```",
      "The patch target looks incorrect/inconsistent with the module import path used elsewhere in the test (`from common.feature_flags import ...`). Patching `\\\"src.common.feature_flags.ConfigManager\\\"` is likely to fail at runtime (ModuleNotFoundError / patching the wrong symbol), which would break the test suite. Prefer creating a `Mock(spec=ConfigManager)` directly (no patch needed), or patch the symbol in the module namespace actually used by the code under test (e.g., `\\\"common.feature_flags.ConfigManager\\\"` if that\u2019s the import path).",
      "This patch target likely doesn't exist: the module under test is `common.lambda_helper`, not `src.common.lambda_helper`. Using the wrong import path will make the patch a no-op or raise at runtime. Patch the actual module path that `get_target_details` resolves `get_application_id` from (e.g., `common.lambda_helper.get_application_id`).",
      "Same patch-target issue as above: `@patch(\"src.common.lambda_helper.get_application_id\")` is unlikely to resolve to the function used by `get_target_details`. Patch `common.lambda_helper.get_application_id` (or the correct imported alias) so the test behaves deterministically."
    ]
  },
  {
    "size": 7,
    "representative": "Multiple test methods reference the global `user_account_id` variable which is only set in the first test. These tests will fail with NameError if run independently or if the first test is skipped.\n```suggestion\n                assert get_account_details.status_code == 200\n                # Extract user_account_id from the integration_user2_token\n                user_account_id = decode_jwt(integration_user2_token)[\"accountId\"]\n```",
    "top_paths": [
      "tests/automation/tests/devices/test_device_integration_user_context.py",
      "tests/automation/tests/devices/test_device_integration_app_context.py"
    ],
    "comments": [
      "Using global variables is generally discouraged as it can lead to test interdependencies and unpredictable behavior. Consider storing `app_account_id` as a class attribute (e.g., `self.app_account_id`) or passing it as needed between test methods.\n```suggestion\n        self.app_account_id = decode_jwt(app_token6)[\"account_id\"]\n```",
      "Using global variables in test methods is an anti-pattern. Consider storing this value as an instance variable (e.g., `self.user_account_id`) or passing it as a fixture/parameter.\n```suggestion\n        logger.info(\"Get user token details from jwt\")\n        self.user_account_id = decode_jwt(integration_user1_token)[\"account_id\"]\n```",
      "This test references `user_account_id` which is set in a different test method (`test_device_provisioning_with_user_and_owner_role_iam`). If tests run independently or in different order, this will fail with NameError.\n```suggestion\n                assert get_account_details.status_code == 200\n                # Extract user_account_id from the integration_user2_token\n                user_account_id = decode_jwt(integration_user2_token)[\"accountId\"]\n```",
      "Multiple test methods reference the global `user_account_id` variable which is only set in the first test. These tests will fail with NameError if run independently or if the first test is skipped.\n```suggestion\n                assert get_account_details.status_code == 200\n                # Extract user_account_id from the integration_user2_token\n                user_account_id = decode_jwt(integration_user2_token)[\"accountId\"]\n```",
      "Multiple test methods reference the global `user_account_id` variable which is only set in the first test. These tests will fail with NameError if run independently or if the first test is skipped.",
      "Multiple test methods reference the global `user_account_id` variable which is only set in the first test. These tests will fail with NameError if run independently or if the first test is skipped.\n```suggestion\n            assert get_account_details.status_code == 200\n            user_account_id = get_account_details.json()['items'][0]['id']\n```",
      "Using global variables in test methods is an anti-pattern. Consider storing this value as an instance variable (e.g., `self.app_account_id`) or passing it as a fixture/parameter.\n```suggestion\n        logger.info(\"Get app 6 token details from jwt\")\n        self.app_account_id = decode_jwt(app_token6)[\"account_id\"]\n```"
    ]
  },
  {
    "size": 7,
    "representative": "Use `let` instead of `var` for block-scoped variable declarations. Change to `let headers = {`.\n```suggestion\n    let headers = {\n```",
    "top_paths": [
      "tests/performance/k6/scripts/users.js",
      "tests/performance/k6/scripts/accounts.js"
    ],
    "comments": [
      "Use `let` or `const` instead of `var` for block-scoped variable declarations. Since `accountUUID` is reassigned later, use `let accountUUID = ''`.\n```suggestion\n    let accountUUID = \"\";\n```",
      "Use `let` instead of `var` for block-scoped variable declarations. Change to `let headers = {`.\n```suggestion\n    let headers = {\n```",
      "Reassigning `headers` without `let` or `const` is acceptable here since it was declared earlier, but for consistency and clarity, consider declaring it with `let` each time it's used in a new block scope to avoid confusion about variable reuse.\n```suggestion\n    let headers = {\n```",
      "Similar to the previous reassignment, consider declaring `headers` with `let` in each block scope where it's used to improve code clarity and maintainability.",
      "Use `let` or `const` instead of `var` for variable declarations. Since `checks` is reassigned, use `let` here for block-scoped variable declaration consistent with modern JavaScript practices.\n```suggestion\n    let checks = true;\n```",
      "New code uses `var` and loose comparisons (`!=`). Prefer `let`/`const` for block scoping and `!==` for strict comparisons to avoid coercion-related surprises (especially in loop conditions).\n```suggestion\n    let pUrl = `/users?accountId=${accountUUID}&sortBy=${sortBy}&sortOrder=${sortOrder}`;\n\n    while (pUrl !== null) {\n```",
      "New code uses `var` and loose comparisons (`!=`). Prefer `let`/`const` for block scoping and `!==` for strict comparisons to avoid coercion-related surprises (especially in loop conditions)."
    ]
  },
  {
    "size": 7,
    "representative": "The Accounts AppSyncResolver lambda (accounts/src/accounts/graphql/resolver.py) currently only defines `Query.cdhSyncAccount` and `Mutation.migrateResources`; there are no resolvers for `Query.cdhSyncSite`, `Query.cdhSyncSiteSourceSystem`, or `Query.cdhSyncAccountSourceSystem`. Wiring these Apollo Query fields to `dataSources.accounts` will therefore fail at runtime unless the backing lambda has been updated to support those fields (or they are routed to a different data source).\n```suggestion\n\n```",
    "top_paths": [
      "graphql/apollo-server/template.yaml",
      "graphql/apollo-server/src/resolvers.ts",
      "graphql/apollo-server/src/data-sources/accounts.ts"
    ],
    "comments": [
      "The ACCOUNTS_LAMBDA_ARN environment variable is incorrectly set to the authorizer function ARN. It should reference the AccountsGraphqlFunction ARN instead. This will cause the GraphQL server to invoke the wrong Lambda function when attempting to execute account-related operations.\n```suggestion\n          ACCOUNTS_LAMBDA_ARN: !GetAtt AccountsGraphqlFunction.Arn\n```",
      "GraphqlAuthorizersLib and GraphQLAuthorizerFunction names are also hard-coded with the \"uw2\" suffix, which breaks multi-region deployments. Align these names with the pattern used elsewhere (e.g., ${Environment}-${Region}).\n```suggestion\n      FunctionName: !Sub iam-graphql-authorizer-apollo-${Environment}-${Region}\n```",
      "Several resources are hard-coded to the \"uw2\" suffix (e.g., iam-graphql-apollo-${Environment}-uw2). This will cause naming collisions or incorrect names when deploying outside us-west-2. Use the existing Region parameter (or ${AWS::Region} / ${Region}) consistently in resource and layer names.\n```suggestion\n      FunctionName: !Sub iam-graphql-apollo-${Environment}-${Region}\n```",
      "The Accounts AppSyncResolver lambda (accounts/src/accounts/graphql/resolver.py) currently only defines `Query.cdhSyncAccount` and `Mutation.migrateResources`; there are no resolvers for `Query.cdhSyncSite`, `Query.cdhSyncSiteSourceSystem`, or `Query.cdhSyncAccountSourceSystem`. Wiring these Apollo Query fields to `dataSources.accounts` will therefore fail at runtime unless the backing lambda has been updated to support those fields (or they are routed to a different data source).\n```suggestion\n\n```",
      "These explicit type-field resolvers (`CdhSyncAccount.sites/sourceSystems` / `CdhSyncSite.sourceSystems`) override default field resolution. The Accounts lambda doesn\u2019t currently register AppSync resolvers for these type fields, and `Query.cdhSyncAccount` already returns `sites` + `sourceSystems` in its payload. As-is, `cdhSyncAccount { sites ... }` will trigger an extra lambda invoke for a resolver that doesn\u2019t exist and can break the query; consider removing these type-field resolvers or adding corresponding resolvers in the backing lambda.\n```suggestion\n\n```",
      "`Query.cdhSyncAccount` in the Accounts AppSyncResolver asserts `event.info.selectionSetGraphQL` is present (it parses nested selection args like `sites(first/after)`). This payload only sets `info.parentTypeName`/`fieldName`, so the lambda will error unless `selectionSetGraphQL` is also provided. Consider extending the payload to include `selectionSetGraphQL` derived from Apollo\u2019s resolver `info` parameter.",
      "`AUTHORIZER_LAMBDA_ARN` is being set to a Lambda ARN that points to `iam-graphql-...` and contains a literal `${stageVariables.DeploymentEnvironment}` placeholder. This is not the authorizer function ARN, and stage variables will never be resolved inside a Lambda environment variable\u2014so anything that tries to invoke this ARN will fail. Pass the authorizer function ARN explicitly (e.g., via a parameter/Export/SSM) and ensure it references the authorizer Lambda, not the GraphQL Lambda.\n```suggestion\n          AUTHORIZER_LAMBDA_ARN: !Sub arn:aws:lambda:${AWS::Region}:${AWS::AccountId}:function:iam-authorizer-${Environment}-${DeploymentEnvironment}-${Region}\n```"
    ]
  },
  {
    "size": 7,
    "representative": "I think this resolver is also implemented and is working fine, could you check this?",
    "top_paths": [
      "graphql/apollo-server/src/data-sources/groups.ts",
      "tests/automation/tests/graphql/test_groups_graphql_resolvers.py",
      "graphql/apollo-server/src/resolvers.ts",
      "graphql/schema.graphql"
    ],
    "comments": [
      "I'm a little hesitant to be adding resolvers for things that don't actually exist yet. I guess it will be okay. Just can't end to end test",
      "currently none of the resolvers are implemented. As a result, neither the applications or the application resolver are used currently. This will not break queries.",
      "This resolver is already working fine, so why are we skipping it if the response is not as expected?",
      "I think this resolver is also implemented and is working fine, could you check this?",
      "Why aren't we using batching for this resolver?",
      "At least currently it will only ever be a batch of one. I can change this to use batching but when I had it batched, appsync wasn't able to find the resolver.",
      "It is now a batch resolver"
    ]
  },
  {
    "size": 7,
    "representative": "`mock_address` is assigned but never used in this test, which will trigger Ruff/Pyflakes F841. Rename it to `_` (or use it) to avoid a lint failure.",
    "top_paths": [
      "accounts/test/cdh_sync/address_controller/test_update.py",
      "events/tests/ingestions/test_central_device_handler.py",
      "groups/tests/graphql/test_groups_appsync_handler.py",
      "applications/tests/test_dao.py",
      "devices/tests/test_helper.py"
    ],
    "comments": [
      "`Meta` and `Subject` are imported but never used in this test, which will fail Ruff's unused-import check. Remove these imports (or use the real `Meta/Subject` objects instead of `MagicMock`).\n```suggestion\n\n```",
      "`Mock` is imported but never used in this test module; this will fail Ruff/F401. Please remove the unused import.\n```suggestion\nfrom unittest.mock import MagicMock, patch\n```",
      "`EntityTypes` is imported but never used in this test module; this will fail Ruff/F401. Please remove the unused import.\n```suggestion\n\n```",
      "`MagicMock` is imported but never used in this test module; this will fail Ruff/flake8 unused-import checks. Remove the import (or use it in a test if intended).\n```suggestion\n\n```",
      "`mock_address` is assigned but never used, which will trigger Ruff/Pyflakes F841 (unused local). Rename it to `_` (or otherwise use it) to keep lint passing.\n```suggestion\n    mock_account, _ = _create_mock_account_with_address()\n```",
      "`mock_address` is assigned but never used in this test, which will trigger Ruff/Pyflakes F841. Rename it to `_` (or use it) to avoid a lint failure.\n```suggestion\n    mock_account, _ = _create_mock_account_with_address()\n```",
      "`mock_address` is assigned but never used in this test, which will trigger Ruff/Pyflakes F841. Rename it to `_` (or use it) to avoid a lint failure."
    ]
  },
  {
    "size": 6,
    "representative": "we may not need this label \"async_events_handler\" as it is dedicated to async event handling ?\r\nwe only need to add event-type like below. we wil add more event type one by one when needed\r\nallowed_events_by_handler = {\r\n    GROUPS: [\"REMOVE\"]\r\n}",
    "top_paths": [
      "events/src/async_processors/regional_events/async_events_handler.py",
      "events/src/async_processors/regional_events/groups_service.py",
      "iam-accounts/iam_api/accounts/service/delete.py"
    ],
    "comments": [
      "See comment above but I'm thinking maybe it would be better to have these functions return the list of items and then make a single call to delete_items.",
      "we may not need this label \"async_events_handler\" as it is dedicated to async event handling ?\r\nwe only need to add event-type like below. we wil add more event type one by one when needed\r\nallowed_events_by_handler = {\r\n    GROUPS: [\"REMOVE\"]\r\n}",
      "lets have it as a generic service to handle all types of event like INSERT, MODIFY, DELETE etc. we may need to handle INSERT and MODIFY in future. so lets flexible\r\n\r\ngroups_service = GroupsService()",
      "refactor => groups_service.handle_event(detail, event_name)\r\ninside handle_event, call the respective function based on the event_name\r\n\r\nFor ex, \r\nif event_name == \"REMOVE\":\r\n    handle_delete_event(event)",
      "Do we really need this?\nIf nothing is there means just skip it. I guess it will be skipped at the `async_events_handler` itself. In such case how come other events will come here.",
      "The Async events handler filters events from entities other than GROUPS, whereas in this case, we are specifically filtering out events with an event name other than REMOVE."
    ]
  },
  {
    "size": 6,
    "representative": "This DynamoDB get_item call should include error handling for potential exceptions like ResourceNotFoundException, ProvisionedThroughputExceededException, or network issues.",
    "top_paths": [
      "events/src/roles_permissions/delta_roles_permissions.py",
      "iam-accounts/iam_api/accounts/service/utils/account_reader_util.py",
      "scripts/roles_permissions_migration/extract_roles_permission.py",
      "events/src/entity_delta_sync/user_sync_update_delta/user_update_delta_sync.py"
    ],
    "comments": [
      "I can agree with the exception piece.  I would see this akin to any exception that Dynamo or any other database might throw at us, so yes, the caller should handle the exception.",
      "This DynamoDB get_item call should include error handling for potential exceptions like ResourceNotFoundException, ProvisionedThroughputExceededException, or network issues.",
      "This DynamoDB put_item call should include error handling for potential exceptions like ProvisionedThroughputExceededException, ValidationException, or network issues.",
      "This DynamoDB delete_item call should include error handling for potential exceptions like ResourceNotFoundException, ProvisionedThroughputExceededException, or network issues.",
      "exception handling for all s3 and dynamodb operation. log errors for troubleshooting",
      "`safe_dynamodb_call` swallows DynamoDB exceptions (including conditional-check failures) and `handle_insert_modify` logs \"Successfully updated\" unconditionally. In an SQS-triggered Lambda, swallowing the exception will also cause the message to be treated as successfully processed and removed from the queue, losing updates. Prefer letting errors propagate (or returning a success/failure flag) and only logging success when `update_item` actually succeeds; explicitly treat `ConditionalCheckFailedException` as a non-error skip case if desired."
    ]
  },
  {
    "size": 6,
    "representative": "this function may not be needed. this can be invoked separately. it should be handled inside dal layer when entity is updated with ttl value the corresponding unique key record also updated with the same ttl",
    "top_paths": [
      "iam-api/src/roles/permissions_service.py",
      "accounts/src/accounts/service/addresses/delete.py",
      "common/src/db/iam_database.py",
      "common/src/db/app.py",
      "users/src/dal.py"
    ],
    "comments": [
      "Can we do this once we have transactional support in DAL layer, Manually reverting the db operations based on error will be complicated approach to implement.",
      "I may not understand the purpose of adding this here. Since the code is used after we return from the dal, could this not be isolated to that code to prevent redefining these for each function?",
      "this will only happen automatically for records that need unique keys, which was already transactional in the dal. now you can also call the transactional operations functions to do any sequence of operations transactionally too",
      "i think we should not expose this function in dal app. this should be implicitly done when an entity is updated with ttl ?",
      "this function may not be needed. this can be invoked separately. it should be handled inside dal layer when entity is updated with ttl value the corresponding unique key record also updated with the same ttl",
      "call only update_resource with ttl. updating unique key should be done internally in dal"
    ]
  },
  {
    "size": 6,
    "representative": "move this validation to service",
    "top_paths": [
      "users/src/tid_sync_handler.py",
      "groups/src/app.py",
      "users/src/app.py",
      "organizations/src/app.py"
    ],
    "comments": [
      "move this validation to service class and throw ApplicationError",
      "Use ApplicationError instead of ValueError. Better to move this validation to Model class",
      "move this validation to model class",
      "move this validation to service",
      "move this validations into service layer",
      "move this validation inside service"
    ]
  },
  {
    "size": 6,
    "representative": "delete_relation requires 6 parameters. The existing one is correct.",
    "top_paths": [
      "users/tests/test_users_app.py",
      "users/src/dal.py",
      "roles/src/dal.py",
      "groups/src/apicloud_group_sync.py",
      "users/src/service.py"
    ],
    "comments": [
      "delete_relation requires 6 parameters. The existing one is correct.",
      "write a generic function 'delete_relation' for reusability (similar to find_relation method)",
      "can we define one common wrapper function for calling dal.delete_relation ? i see it is duplicated with different function names.\r\n\r\nAlso try to do this in transactional way/batch instead of calling it in a for loop",
      "Can we have update_relation instead of deleting and recreating the same?",
      "The new `delete_resource` call should be placed before the `delete_relation` call to maintain proper cleanup order. If `delete_relation` fails, the resource deletion would leave an orphaned invitation resource. Consider moving this line to after line 2929 and before the relation deletion.\n```suggestion\n            self.dal.delete_relation(subject, user_id, constants.USERS_TYPE_REF, account_id,\n                                        constants.ACCOUNTS_TYPE_REF, constants.USER_ACCOUNT_RELATION)\n            self.dal.delete_resource(subject, invitation_id, constants.INVITATIONS_TYPE_REF)\n```",
      "In `_handle_user_deletion_if_expired`, the relation is updated to `STATUS_EXPIRED` before calling `users_service.is_last_pending_account_invitation(source_id)`, but `is_last_pending_account_invitation()` only counts relations where `status==PENDING`. If this was the *only* pending invitation, the pending count will be 0 after the update and the user will not be deleted (regressing prior behavior). Consider either checking for pending invitations *before* updating the relation status, or changing the condition for the expiry path to delete when there are **no remaining pending** account invitations (e.g., pending_count == 0) rather than `== 1`."
    ]
  },
  {
    "size": 6,
    "representative": "cant this use the get_account_relations method on the account class?",
    "top_paths": [
      "users/src/dal.py",
      "users/src/service.py",
      "accounts/src/accounts/service/relation.py",
      "groups/src/service.py",
      "accounts/src/accounts/dynamodb/account_repository/account_repository_impl.py"
    ],
    "comments": [
      "Remove 'get_relation_between_user_and_account' method. Use the existing find_relation method",
      "do not hard code 'account_to_user' here. Use the common constants. may be you can pass the target relation type as \"users\" instead of 'account_to_user'",
      "cant this use the get_account_relations method on the account class?",
      "Missing variable assignment. The function appears to return `account_relations[0]` but `account_relations` is never defined in this function scope.",
      "Can we make this any more specific since we have the `get_account_user_relation` maybe we should follow that pattern and define functions for each target relation type",
      "This function fetches relation between 2 accounts, it was named get_account_relation based on the suggestion it was renamed to get_account_account_relation following the convention like get_account_user_relation already present"
    ]
  },
  {
    "size": 6,
    "representative": "@mariasekartrimble , we have replication to DR already in place for Profiles S3 bucket . This S3 bucket is created and maintained as part of Profiles IAC .",
    "top_paths": [
      "setup/infra/sam/configuration/dev/us-west-2/users-env.json",
      "auditLog/template.yaml",
      "setup/infra/terraform_iac/picture_resize_lambda/configuration/integ/us/us-west-2/integ.tfvars"
    ],
    "comments": [
      "What is the replication strategy from DR point of view for S3 buckets. Not only this. Since this one only I came across so asking this. Please update this from DR and replication point of view\r\n@shaju-g and @kedharTrmb plz add your views",
      "@shaju-g could you plz create a ticket for the S3 replication changes as well.",
      "@mariasekartrimble , we have replication to DR already in place for Profiles S3 bucket . This S3 bucket is created and maintained as part of Profiles IAC .",
      "Why we have to use profiles S3 bucket instead of new one specifically for IAM alone?",
      "I think to use the profile pictures already set by users, we are using the same bucket in IAM. otherwise we need to migrate all the existing pictures to the new bucket",
      "Environment variables reference 'dev' bucket name ('profiles-image-dev') but this is an integration environment configuration. Consider using environment-specific bucket names or variables to maintain consistency between configuration and actual environment.\n```suggestion\n  \"PICTURES_BUCKET_NAME\" : \"profiles-image-integ\"\n  \"PICTURES_FOLDER_PATH\" : \"profileimages/users\"\n}\n\nprofiles_images_bucket             = \"profiles-image-integ\"\npic_s3_bucket                      = \"iam-prod-ci-cd-us-west-2\"\npic_s3_key                         = \"pictures_lambda/1956_d746e49.zip\"\npic_tags                           = { tier = \"dev\", Project = \"iam\", environment = \"integ\", product = \"iam\", Name = \"iam-users-picture-integ-uw2\" }\npic_lambda_role                    = \"arn:aws:iam::387675059062:role/ProfilesDevIAM-IAMAPILambdaRole-un8ia5vQ2tFV\"\npic_bucket_arn                     = \"arn:aws:s3:::profiles-image-integ\"\npic_bucket_id                      = \"profiles-image-integ\"\n```"
    ]
  },
  {
    "size": 6,
    "representative": "This should return FullUser object. user_list parameter may not be needed here",
    "top_paths": [
      "users/src/service.py",
      "roles/src/service.py",
      "users/src/app.py"
    ],
    "comments": [
      "instead check ``` if not account_id: ``` for none and empty strings as well and also it will be more like readable statement..",
      "do not use get_full_account, it needs to be removed later. Use 'get_resource' for checking if user and account exist",
      "get_user_and_account is ambiguous. lets use the existing functions for this\r\n1. get_user to get the FullUser object\r\n2. validate_account to check the account is valid or not",
      "user_list returned from service is List[FullUser], but in dump we are using List[User]",
      "This is not fixed yet. user_list should not be a parameter.",
      "This should return FullUser object. user_list parameter may not be needed here"
    ]
  },
  {
    "size": 6,
    "representative": "The wildcard wrapping logic differs between `like_match()` and the `like_operator()` function in v2/rsql.py. In `like_match()`, a pattern is wrapped with `*` if it contains no wildcards, but in `like_operator()`, it checks whether the pattern starts or ends with `*`. This inconsistency could lead to different behavior: `like_match('test', 'te*st')` would not wrap the pattern, while `like_operator('test', 'te*st')` would wrap it as `*te*st*`. Both functions should use the same logic.",
    "top_paths": [
      "common/src/db/parser/rsql_parser.py",
      "tests/automation/tests/accounts/list_accounts/list_accounts_param_test.py",
      "common/tests/unit/db/parser/test_rsql_parser.py",
      "common/src/db/parser/v2/rsql.py",
      "common/tests/unit/db/parser/test_rsql_parser_v2.py"
    ],
    "comments": [
      "Looking at the syntax defined here: https://github.com/jirutka/rsql-parser I'd like to see a test that exercises more fo the RSQL implemention. I.e. not-equals, in-stynax, etx.",
      "The wildcard wrapping logic differs between `like_match()` and the `like_operator()` function in v2/rsql.py. In `like_match()`, a pattern is wrapped with `*` if it contains no wildcards, but in `like_operator()`, it checks whether the pattern starts or ends with `*`. This inconsistency could lead to different behavior: `like_match('test', 'te*st')` would not wrap the pattern, while `like_operator('test', 'te*st')` would wrap it as `*te*st*`. Both functions should use the same logic.",
      "Same inconsistency as in `like_match()`: `ilike_match()` checks if `*` exists anywhere in the pattern, while v2's `like_operator()` checks if the pattern starts or ends with `*`. The two implementations should use identical wildcard detection logic.\n```suggestion\n    pattern = (\n        received_lower\n        if received_lower.startswith(\"*\") or received_lower.endswith(\"*\")\n        else f\"*{received_lower}*\"\n    )\n```",
      "The test validates that a full wildcard pattern `*` is rejected, but there's no corresponding test coverage in `test_rsql_parser_v2.py` to ensure the v2 parser also rejects this pattern. Add a similar test case to maintain parity between the two parser implementations.",
      "This logic wraps the pattern even when wildcards appear in the middle (e.g., `'te*st'`), which differs from the original parser's behavior. A pattern like `'te*st'` would be wrapped as `'*te*st*'` here, but not in `like_match()` which checks `if '*' in received`. The two implementations should have consistent behavior.\n```suggestion\n    if '*' not in pattern:\n```",
      "`parsed_q1` is assigned but never used, which will likely fail linting (e.g., Ruff F841) and also doesn\u2019t actually assert the new apostrophe-containing email case. Consider either removing the assignment and just calling `parse(...)` (so the test fails if parsing raises), or using `parsed_q1` in an assertion (e.g., pass it to `extract_ids_from_rsql` / `satisfies` and assert expected behavior).\n```suggestion\n    ids_with_apostrophe = extract_ids_from_rsql(parsed_q1)\n    assert len(ids) == 3\n    assert len(ids_with_apostrophe) == 3\n    assert set(ids_with_apostrophe) == set(ids)\n```"
    ]
  },
  {
    "size": 6,
    "representative": "as_uuid should be used here",
    "top_paths": [
      "tests/automation/tests/roles/test_roles_apptoken.py",
      "tests/migrationValidator/tests/test_user_account_relations.py",
      "setup/infra/migration/data-synchronizer/layers/transformers.py",
      "applications/tests/conftest.py",
      "tests/automation/tests/sync/tests_sync_devices/device_verification.py"
    ],
    "comments": [
      "as_uuid should be used here",
      "we should convert this id into uuid, as like other places",
      "for readability keep the existing id or create a variable with that name and value as uuid. use the variable wherever needed",
      "I have added the UUID generation code",
      "Please avoid hardcoding. If trying to have a uuid, please use uuid.uuid4()",
      "Please avoid any uuid hardcoding. If needed take it from the config file."
    ]
  },
  {
    "size": 6,
    "representative": "rename 'timestamp' variable to 'ttl\" for better clarity. update the function doc accordingly",
    "top_paths": [
      "users/src/service.py",
      "accounts/test/models/core/test_meta.py",
      "applications/src/apicloud_app_sync.py",
      "common/src/db/iam_database.py",
      "groups/src/appsync_handler.py"
    ],
    "comments": [
      "yeah the metadata timestamp model is only accurate to the millisecond so I'm making sure enough time has passed. I can change the test if this is a concern",
      "the timestamp variale not used further so directly use`application_meta_data.time` in the return",
      "rename 'timestamp' variable to 'ttl\" for better clarity. update the function doc accordingly",
      "update ttl should not be done in \"update_default_account\" function scope.",
      "function rename? update_ttl_for_reserved_user\r\ndo we need to pass update_ttl_for_reserved_user parameter? caller can invoke this function based on check_and_set_expiry return value\r\nif ttl_update:\r\n   update_ttl_for_reserved_user()",
      "I main reason was to make it json-safe as the metadata comes with datetime values, this can be improved adding the \"json\" mode on the dump."
    ]
  },
  {
    "size": 6,
    "representative": "I would like to keep this as simple for now. If the scope of the script grows, I will change the argument parsing.",
    "top_paths": [
      ".github/actions/assistant/extract_function.py",
      "tests/migrationValidator/scripts/s3FileDownloader.py",
      "setup/infra/migration/data-synchronizer/scripts/populate_scopedto/scoped_to_updator.py",
      "common/src/common/invitation/invitation_wrapper.py",
      "groups/src/service.py"
    ],
    "comments": [
      "This is generally acceptable practice with scripts. But it would be better to change this to be usable with cmd line arguments using `argparse` or something similar.",
      "I'd be tempted to use argparse or something, but for such small script, maybe it's not the worst thing to keep it simple like this \ud83e\udd14",
      "I would like to keep this as simple for now. If the scope of the script grows, I will change the argument parsing.",
      "Use arguments parser which will provide good docs and options too check it out and try it out.",
      "nit: If this method's argument list continues to grow, consider using a dataclass, TypedDict, or at least kwargs for better readability",
      "Suggestion is to model the argument as a dataclass and reference it here."
    ]
  },
  {
    "size": 6,
    "representative": "Should this check be done before account id check?",
    "top_paths": [
      "roles/src/service.py",
      "groups/src/service.py",
      "organizations/src/service.py",
      "tests/automation/tests/accounts/dealer_optin/test_dealer_optin.py"
    ],
    "comments": [
      "then i'll remove the accountId checks",
      "Should this check be done before account id check?",
      "Can we include a check whether the accountId is in DB or not?",
      "do we need this check. definitely the calling subject is part of the account id from token ??",
      "Using the correct account id only.",
      "can we keep the existing logic unchanged and set accountId to None if include_account is False ?"
    ]
  },
  {
    "size": 6,
    "representative": "IAMLogger should be removed. use standard logging instead",
    "top_paths": [
      "organizations/src/app.py",
      "users/src/service.py",
      "common/src/db/search/providers/provider.py",
      "common/src/common/feature_flags.py",
      "common/src/db/cross_region_fetch.py"
    ],
    "comments": [
      "use the IAM custom logger standard",
      "This Logger import not needed. It is already using IAMLogger",
      "[nitpick] Accessing `logger.logger.level` exposes internal implementation details of IAMLogger. Consider adding a public method like `is_debug_enabled()` to IAMLogger to encapsulate this check.",
      "probably best to use the IAMLogger from logger_util, since that will pre-populate with some additional metadata",
      "do not use IAMLogger. use python standard logging module if needed",
      "IAMLogger should be removed. use standard logging instead"
    ]
  },
  {
    "size": 6,
    "representative": "Missing space after comma in f-string. Should be `app_token, f\\\"devices/...\\\"` for consistency with Python style conventions.\n```suggestion\n            device_transfer_history_response = devices.list_devices(app_token, f\"devices/{existing_device_id}/transfer_history\")\n```",
    "top_paths": [
      "roles/src/dal.py",
      "tests/automation/tests/devices/test_device_role_assignment.py",
      "tests/automation/tests/devices/test_iam_bootstrap.py",
      "tests/automation/tests/devices/test_get_device.py",
      "tests/automation/tests/devices/test_device_integration_app_context.py"
    ],
    "comments": [
      "Missing space after comma before 'DEVICE'. Should be 'APPLICATION, DEVICE' to follow Python style conventions.\n```suggestion\nfrom constants import USER, APPLICATION, DEVICE\n```",
      "[nitpick] Missing space after comma in the method calls. Should be 'owner_token, f'devices...' and 'token, f'groups..., add_group_resp...' for consistent formatting.\n```suggestion\n            devices.delete_relation(token, f'groups/{group_id}/devices', add_group_resp.json()[\"id\"])  # Remove device group relation\n```",
      "Missing space after comma in f-string. Should be `f'{create_device_resp.json()[\"id\"]}?unlockCertRotation=true'` with a space after the comma for consistency with Python formatting standards.\n```suggestion\n            unlock_device_response = devices.update_device1(token, \"devices\", f'{create_device_resp.json()[\"id\"]}?unlockCertRotation=true')\n```",
      "The error message is formatting the entire JSON response object instead of just the device ID. This should be `create_device_resp.json()['id']` to display only the device ID.\n```suggestion\n                    f\"Device with ID {create_device_resp.json()['id']} not found in the response\")\n```",
      "Missing space after comma in function arguments. Should be `None, setup_client_details` instead of `None,setup_client_details` for consistency with Python style guidelines.\n```suggestion\n                device_token = self.test_common.get_device_token(device_id, associate_cert_resp, None, setup_client_details)\n```",
      "Missing space after comma in f-string. Should be `app_token, f\\\"devices/...\\\"` for consistency with Python style conventions.\n```suggestion\n            device_transfer_history_response = devices.list_devices(app_token, f\"devices/{existing_device_id}/transfer_history\")\n```"
    ]
  },
  {
    "size": 6,
    "representative": "In the exception handler, `raise e` resets the traceback context, making debugging harder. Use a bare `raise` to preserve the original traceback while still propagating the exception.\n```suggestion\n        raise\n```",
    "top_paths": [
      "events/src/ingestions/consumers/regional/handlers.py",
      "events/src/geo_isolation/user_delta_handler.py",
      "events/src/ingestions/consumers/central/user_handler.py",
      "events/src/analytics/regional/user_created_timestamp/event_handler.py"
    ],
    "comments": [
      "Use `raise` instead of `raise e` to preserve the original traceback. Re-raising with `raise e` creates a new traceback that loses the original exception context.\n```suggestion\n        raise\n```",
      "Use `raise` instead of `raise e` to preserve the original traceback. Re-raising with `raise e` creates a new traceback that loses the original exception context.\n```suggestion\n        raise\n    except Exception as e:\n        logger.error(f\"Error processing event: {repr(event)} with exception: {repr(e)}\")\n        raise\n```",
      "Using `raise e` discards the original traceback context. Prefer bare `raise` here so the original stack trace is preserved.\n```suggestion\n        raise\n```",
      "Same issue as above: `raise e` loses the original traceback. Use bare `raise` to preserve error context for debugging and observability.\n```suggestion\n        raise\n```",
      "In the exception handler, `raise e` resets the traceback context, making debugging harder. Use a bare `raise` to preserve the original traceback while still propagating the exception.\n```suggestion\n        raise\n```",
      "`raise e` resets the traceback context, making debugging harder. Prefer using a bare `raise` after logging so the original stack trace is preserved.\n```suggestion\n            LOGGER.error(f\"Error processing user creation event: {e}\", exc_info=True)\n            raise\n```"
    ]
  },
  {
    "size": 6,
    "representative": "EventBridge Rule target is incorrectly pointing to the Lambda handler. Should be `!GetAtt OrganizationRegionalPublishEventsSourceQueue.Arn` to follow the SQS pattern.\n```suggestion\n          Arn: !GetAtt OrganizationRegionalPublishEventsSourceQueue.Arn\n```",
    "top_paths": [
      "events/template.yml"
    ],
    "comments": [
      "The condition restricts access to a specific EventBridge rule, but the lambda function sending messages to the DLQ also needs permission. Consider adding the lambda function ARN as an additional allowed source.\n```suggestion\n                aws:SourceArn: !Sub 'arn:aws:events:${AWS::Region}:${AWS::AccountId}:rule/iam-central-eventbus-${Environment}-${Region}/tid-sync-rule-v2'\n          - Effect: Allow\n            Principal:\n              Service: \"lambda.amazonaws.com\"\n            Action: 'sqs:SendMessage'\n            Resource: !GetAtt V2TidSyncDLQ.Arn\n            Condition:\n              ArnEquals:\n                aws:SourceArn: !GetAtt V2TIDSyncHandler.Arn\n```",
      "EventBridge Rule target is incorrectly pointing to the Lambda handler. Should be `!GetAtt RegionalPublishEventsSourceQueue.Arn` to follow the SQS pattern used in other handlers like CentralPublishEventRule.\n```suggestion\n          Arn: !GetAtt RegionalPublishEventsRetryQueue.Arn\n```",
      "EventBridge Rule target is incorrectly pointing to the Lambda handler. Should be `!GetAtt AccountSourceSystemSourceQueue.Arn` to follow the SQS pattern used in CentralPublishEventRule.\n```suggestion\n          Arn: !GetAtt AccountSourceSystemSourceQueue.Arn\n```",
      "EventBridge Rule target is incorrectly pointing to the Lambda handler. Should be `!GetAtt UserAccountsMembershipSourceQueue.Arn` to follow the SQS pattern established in other handlers.\n```suggestion\n          Arn: !GetAtt UserAccountsMembershipSourceQueue.Arn\n```",
      "EventBridge Rule target is incorrectly pointing to the Lambda handler. Should be `!GetAtt EmailUserMappingAnalyticsSourceQueue.Arn` to follow the SQS pattern.\n```suggestion\n          Arn: !GetAtt EmailUserMappingAnalyticsSourceQueue.Arn\n```",
      "EventBridge Rule target is incorrectly pointing to the Lambda handler. Should be `!GetAtt OrganizationRegionalPublishEventsSourceQueue.Arn` to follow the SQS pattern.\n```suggestion\n          Arn: !GetAtt OrganizationRegionalPublishEventsSourceQueue.Arn\n```"
    ]
  },
  {
    "size": 6,
    "representative": "The variable `app_token_value` is not defined in the visible code context. The test method signature shows `app_token` as a parameter. This should likely be `app_token() if callable(app_token) else app_token` to handle both fixture types, or the variable should be defined earlier in the function.",
    "top_paths": [
      "tests/automation/tests/sync/test_tid_profiles_iam_sync.py",
      "tests/performance/k6/controllers/controllerGraphQL.js",
      "tests/automation/tests/devices/test_device_role_assignment.py",
      "tests/automation/tests/groups/test_cross_account_group_invitation_workflow.py"
    ],
    "comments": [
      "The variable `appToken` is used but not declared in the baseline test function. It should be declared with `var` or `let` to avoid creating an implicit global variable.",
      "The variable `get_user_token` is used without being defined in this function's scope. It appears to be a global variable that may not be initialized at this point in the test execution.",
      "[nitpick] The variable name 'app_token5' is inconsistent with the test method parameter 'privileged_app_token'. Consider using a more descriptive name like 'admin_token' or directly use the parameter name.",
      "The variable `owner_token_value` is not defined in the visible code context. Based on the test method signature, it should be `owner_token()` (with parentheses to call the fixture) or the fixture should be stored in a variable first.",
      "The variable `app_token_value` is not defined in the visible code context. The test method signature shows `app_token` as a parameter. This should likely be `app_token() if callable(app_token) else app_token` to handle both fixture types, or the variable should be defined earlier in the function.",
      "`owner_token` is normalized at runtime but not used anywhere in this test after the recent logic change, which is likely to trigger an unused-variable lint failure (e.g., Ruff F841) and adds confusion. Remove the `owner_token` parameter/assignment here if it\u2019s no longer needed, or use it for a request in this test."
    ]
  },
  {
    "size": 6,
    "representative": "@ragulkarthi Please remove this while merging",
    "top_paths": [
      ".github/workflows/release_live_sync_apply.yml",
      "tests/automation/tests/sync/test_iam_cdh_roles_sync.py",
      "bootstrap/src/iam_service.py",
      "authorization/version-config.yaml",
      "events/geo-isolation-template-tmp.yml"
    ],
    "comments": [
      "@nonoratoDEV Can you comment on this before merging?",
      "@ragulkarthi Remove this before merge",
      "@ragulkarthi Please remove this while merging",
      "make sure we remove this later if not now, @ragulkarthi @raahavm",
      "Can merge as seperate PR @ravi1933",
      "Will be merged from other PR"
    ]
  },
  {
    "size": 6,
    "representative": "`_search_based_sorting_strategy_async` now uses `sort_params` from `kwargs`, but `QueryContext` (TypedDict used by `Unpack`) still defines `sort_by`/`sort_order` and does not include `sort_params`. This will cause mypy/type-checking errors when calling this function with `sort_params`. Update `QueryContext` to include `sort_params` (and consider removing the unused `sort_by`/`sort_order` entries) so the type contract matches the new API.",
    "top_paths": [
      "common/src/db/iam_database.py",
      "common/src/db/search/providers/opensearch/queries.py",
      "common/src/db/search/conditions.py",
      "common/tests/int/db/test_iam_database.py"
    ],
    "comments": [
      "The comment mentions 'Use run_coroutine instead of asyncio.run' but this line doesn't show any code change related to this statement. The comment appears to be a leftover or incorrectly placed note that should be removed or moved to the appropriate location where the actual change occurs.\n```suggestion\n\n```",
      "When we support nested_filter, we can build the SortOption as \nnested_filter = ConditionNode(\n            field=f\"{source_type}.id\",\n            operator=MatchOperator.EQ,\n            values=[source_entity]\n        )\nSortOptions(sort_by, sort_order, sort_type, nested_filter)",
      "nested sort build should be generic",
      "The `Args:` section for `build_sort_options` lists `source_type` before `target_type`, but the function signature is `(sort_params, target_type, source_type, ...)`. Update the docstring to match the actual parameter order to avoid callers mixing up target/source types.",
      "`_search_based_sorting_strategy_async` now uses `sort_params` from `kwargs`, but `QueryContext` (TypedDict used by `Unpack`) still defines `sort_by`/`sort_order` and does not include `sort_params`. This will cause mypy/type-checking errors when calling this function with `sort_params`. Update `QueryContext` to include `sort_params` (and consider removing the unused `sort_by`/`sort_order` entries) so the type contract matches the new API.",
      "This test calls `_search_based_sorting_strategy_async` from a synchronous context and then asserts `asyncio.run` was called, but `_search_based_sorting_strategy_async` is an async function and will just return a coroutine here (no `asyncio.run` invocation). Either make this test async and `await` the call, or introduce/use a real synchronous wrapper (e.g., `run_async(...)`) and assert against that wrapper instead."
    ]
  },
  {
    "size": 6,
    "representative": "The release workflow now references reusable workflows via a mutable branch ref (`@dev/IAM-4796-1`). For release/deployment workflows, this is risky (supply-chain/reproducibility) because the referenced workflow can change over time. Prefer an immutable ref (tag) or a commit SHA, or switch back to `@main` if that\u2019s the intended stable source.\n```suggestion\n    uses: Trimble-Cloud-Core-Platform/iam/.github/workflows/reusable_deployment_info.yml@main\n```",
    "top_paths": [
      ".github/workflows/release_sam_apply.yml",
      "scripts/error_page_generator/requirements.txt",
      ".github/workflows/release_error_codes.yml"
    ],
    "comments": [
      "The workflow reference should use '@main' instead of '@dev/IAM-4789'. Using a feature branch reference in the release workflow creates a dependency on a temporary branch and will break when the branch is deleted after merge. This pattern is repeated across multiple deployment jobs in this file.",
      "The `needs: deployment-info` dependency still references the removed `deployment-info` job. This will cause the workflow to fail. Remove this line to fix the pipeline.\n```suggestion\n\n```",
      "The release workflow now references reusable workflows via a mutable branch ref (`@dev/IAM-4796-1`). For release/deployment workflows, this is risky (supply-chain/reproducibility) because the referenced workflow can change over time. Prefer an immutable ref (tag) or a commit SHA, or switch back to `@main` if that\u2019s the intended stable source.\n```suggestion\n    uses: Trimble-Cloud-Core-Platform/iam/.github/workflows/reusable_deployment_info.yml@main\n```",
      "The release workflow now references reusable workflows via a mutable branch ref (`@dev/IAM-4796-1`). For release/deployment workflows, this is risky (supply-chain/reproducibility) because the referenced workflow can change over time. Prefer an immutable ref (tag) or a commit SHA, or switch back to `@main` if that\u2019s the intended stable source.",
      "This new `requirements.txt` doesn\u2019t appear to be referenced anywhere (e.g., `.github/workflows/release_error_codes.yml` still installs `pydantic`/`boto3` directly, and the generator README doesn\u2019t mention `pip install -r requirements.txt`). If the intent is to include the full dependency set for the workflow, update the workflow (and/or README) to install from this file so the change actually takes effect.",
      "Referenced in the github workflow"
    ]
  },
  {
    "size": 6,
    "representative": "The page object constant is named `USERNAME1_AS_XPATH` but it's being used with `By.CSS_SELECTOR`, not `By.XPATH`. The naming suggests XPath but the selector is CSS. This should be renamed to `USERNAME1_AS_CSS` or `USERNAME1_AS_CSS_SELECTOR`.\n```suggestion\n            expected_conditions.visibility_of_element_located((By.CSS_SELECTOR, TIDUserPage.USERNAME1_AS_CSS_SELECTOR))\n        )\n        username_input.clear()\n        username_input.send_keys(username)\n\n        password_input = WebDriverWait(driver, 100).until(\n            expected_conditions.visibility_of_element_located((By.CSS_SELECTOR, TIDUserPage.PASSWORD1_AS_CSS_SELECTOR))\n        )\n        password_input.clear()\n        password_input.send_keys(password)\n\n        sign_in_button = WebDriverWait(driver, 100).until(\n            expected_conditions.visibility_of_element_located((By.CSS_SELECTOR, TIDUserPage.SUBMIT1_AS_CSS_SELECTOR))\n```",
    "top_paths": [
      "tests/automation/base/driver.py"
    ],
    "comments": [
      "The page object constant is named `USERNAME1_AS_XPATH` but it's being used with `By.CSS_SELECTOR`, not `By.XPATH`. The naming suggests XPath but the selector is CSS. This should be renamed to `USERNAME1_AS_CSS` or `USERNAME1_AS_CSS_SELECTOR`.\n```suggestion\n            expected_conditions.visibility_of_element_located((By.CSS_SELECTOR, TIDUserPage.USERNAME1_AS_CSS_SELECTOR))\n        )\n        username_input.clear()\n        username_input.send_keys(username)\n\n        password_input = WebDriverWait(driver, 100).until(\n            expected_conditions.visibility_of_element_located((By.CSS_SELECTOR, TIDUserPage.PASSWORD1_AS_CSS_SELECTOR))\n        )\n        password_input.clear()\n        password_input.send_keys(password)\n\n        sign_in_button = WebDriverWait(driver, 100).until(\n            expected_conditions.visibility_of_element_located((By.CSS_SELECTOR, TIDUserPage.SUBMIT1_AS_CSS_SELECTOR))\n```",
      "The page object constants are named with `_AS_XPATH` suffix but are used with `By.CSS_SELECTOR`. These should be renamed to use `_AS_CSS` suffix to match their actual usage.\n```suggestion\n            expected_conditions.visibility_of_element_located((By.CSS_SELECTOR, TIDUserPage.USERNAME1_AS_CSS))\n        )\n        username_input.clear()\n        username_input.send_keys(username)\n\n        password_input = WebDriverWait(driver, 100).until(\n            expected_conditions.visibility_of_element_located((By.CSS_SELECTOR, TIDUserPage.PASSWORD1_AS_CSS))\n        )\n        password_input.clear()\n        password_input.send_keys(password)\n\n        sign_in_button = WebDriverWait(driver, 100).until(\n            expected_conditions.visibility_of_element_located((By.CSS_SELECTOR, TIDUserPage.SUBMIT1_AS_CSS))\n```",
      "Incorrect usage of locator type. The constant `TIDUserPage.NEW_PASSWORD_TEXTBOX_AS_XPATH` is an XPath selector (\"//input[@type='password' and @name='new_password']\"), but it's being used with `By.NAME`. This should use `By.XPATH` instead, or use the correct constant `TIDUserPage.NEW_PASSWORD_TEXTBOX_AS_NAME` which contains \"new_password\".",
      "Incorrect usage of locator type. The constant `TIDUserPage.CONFIRM_PASSWORD_TEXTBOX_AS_XPATH` is an XPath selector (\"//input[@type='password' and @name='confirm_password']\"), but it's being used with `By.NAME`. This should use `By.XPATH` instead, or use the correct constant `TIDUserPage.CONFIRM_PASSWORD_TEXTBOX_AS_NAME` which contains \"confirm_password\".",
      "In `user_sign_up_though_invitation`, the `except` block calls `handle_selenium_timeout(...)`, which quits the WebDriver in its `finally`, and then immediately calls `retry_user_creation_on_failure(driver, ...)` using the same (now-quit) driver session. This is likely to raise an invalid session / driver error and also prevents the `@retry_on_exception` decorator from managing retries and driver reinitialization. Consider re-raising the exception and letting `retry_on_exception` reinitialize the driver, or explicitly create a fresh driver before invoking the retry flow.",
      "`retry_user_creation_on_failure` uses mismatched Selenium locator strategies: it waits for `TIDUserPage.USERNAME_TEXTBOX_AS_NAME` using `By.XPATH` even though the constant is a NAME, and it finds `TIDUserPage.FORGOT_PASSWORD_LINK_AS_XPATH` using `By.NAME` even though the constant is an XPATH. These will cause element lookups to fail at runtime. Use `By.NAME` for `*_AS_NAME` constants and `By.XPATH` for `*_AS_XPATH` constants consistently."
    ]
  }
]